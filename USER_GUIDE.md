# 사용 안내 (v1.0)

> 상품 이미지를 넣으면 **상세페이지 초안**이 나오고, 사람이 검토해
> 발행하는 것까지가 이 제품입니다.

---

## 한 장으로 보는 흐름

```
① 프로젝트 만들기
      ↓
② 이미지 올리기            ─── 상품 하나에 여러 장
      ↓
③ 글자 읽기 (OCR)          ─── 이미지 속 문구·성분·주의사항
      ↓
④ 상품 분석                ─── 이미지 + 읽은 글자를 함께 봅니다
      ↓
⑤ Product Object 확정      ─── 사람이 고쳐서 READY로
      ↓
⑥ 상세페이지 생성          ─── 회사 지식·금지어를 반영
      ↓
⑦ 검토 → 발행              ─── 거버넌스를 통과해야 나갑니다
```

**⑤에서 사람이 확정하지 않으면 ⑥으로 갈 수 없습니다.** AI가 읽은 것을
사람이 한 번 보게 하려고 일부러 끊어 둔 자리입니다.

---

## 시작하기

### 로그인

관리자에게 계정을 받으세요. 역할은 셋입니다.

| 역할 | 할 수 있는 것 |
| --- | --- |
| **VIEWER** | 보기 |
| **EDITOR** | 만들고 고치고 발행 요청 |
| **ADMIN** | 운영 설정 · 비용 · 사용자 |

### ① 프로젝트

브랜드나 카테고리 단위로 하나 만듭니다. **비용이 프로젝트별로 집계**되므로,
나중에 "어디에 얼마 썼는가"를 묻고 싶은 단위로 나누는 것이 좋습니다.

### ② 이미지 올리기

업로드할 때 **소속 프로젝트를 골라 주세요.** 비워 두면 "공용"이 아니라
**"모른다"** 가 되고, 그 이미지로 쓴 비용은 어느 프로젝트에도 잡히지
않습니다.

### ③ 글자 읽기 (OCR)

이미지 속 글자를 읽습니다. 여러 장이면 `/admin/jobs`의 **묶음 OCR**이
빠릅니다 — 중간에 멈춰도 **끝난 장은 다시 처리하지 않습니다.**

읽지 못한 이미지는 **건너뛰고 목록에 남습니다.** 전부 건너뛰었으면
"결과를 얻은 항목이 하나도 없습니다"라고 분명히 말합니다 — 아무것도 못
얻은 실행이 성공처럼 보이지 않게 하려는 것입니다.

### ④ 상품 분석

이미지와 읽은 글자를 함께 넣어 이름·설명·속성을 뽑습니다.
`apply`를 켜면 결과를 상품에 바로 반영합니다.

### ⑤ Product Object 확정

**여기가 사람의 자리입니다.** 뽑힌 값을 보고 고칩니다.

- 빠진 필수 항목이 있으면 **무엇이 빠졌는지** 화면이 말합니다.
- 다 채우면 **READY**가 됩니다. READY가 아니면 상세페이지를 만들 수
  없습니다.
- 고칠 때마다 버전이 올라갑니다 — 어느 버전으로 만든 콘텐츠인지 남습니다.

### ⑥ 상세페이지 생성

READY인 버전을 골라 만듭니다. 이때 **Company Brain**이 함께 들어갑니다:

- **지식** — 브랜드 톤, 자주 쓰는 표현
- **결정** — "이건 이렇게 쓰기로 했다"
- **금지어** — 쓰면 안 되는 표현

여러 버전을 한꺼번에 만들려면 `/admin/jobs`의 **묶음 생성**을 쓰세요.

### ⑦ 검토와 발행

```
DRAFT  →  REVIEW  →  PUBLISHED  →  ARCHIVED
```

- `REVIEW`로 올리는 것은 **검토를 요청**하는 것입니다.
- `PUBLISHED`로 가려면 **거버넌스 판정을 통과**해야 합니다:
  금지어 · 필수 고지 · 상품 상태.
- 막히면 **무엇이 막았는지 그대로** 보여 줍니다. 고쳐서 다시 올리세요.
- 보관(`ARCHIVED`)한 것도 되살려 다시 발행할 수 있고, 그때 **거버넌스를
  다시 거칩니다.**

> 발행은 되돌리기가 가장 번거로운 동작입니다. 그래서 게이트가 여기 있고,
> **강제로 여는 방법은 없습니다.**

---

## 알아 두면 좋은 것

### 실패했을 때 화면이 말하는 것

- **"잠시 후 다시 시도해 주세요"** → 다시 눌러도 됩니다.
- **"관리자에게 알려 주세요"** → 눌러도 같습니다. 설정 문제입니다.
- **"지금은 이 작업을 실행할 수 없습니다"** → 예산이나 게이트에 막힌
  것입니다. 기다린다고 열리지 않습니다.
- **"이어할 수 있습니다"** → 끝난 단계는 다시 처리하지 않습니다.

원문 오류 메시지는 화면에 나오지 않습니다 — 거기 무엇이 들어 있는지 미리
알 수 없기 때문입니다. 관리자는 로그에서 볼 수 있습니다.

### "판정 유보"는 정상이라는 뜻이 아닙니다

표본이 기준에 못 미치면 **판정하지 않습니다.** 빠르다는 뜻도, 문제없다는
뜻도 아니고 **아직 판단할 근거가 모자라다**는 뜻입니다. 쓰다 보면
해소됩니다.

### AI 호출에는 돈이 듭니다

OCR · 분석 · 생성은 실제로 과금됩니다. 묶음 작업은 한 번에 **최대
50건**입니다 — 상한이 없으면 한 번의 요청이 얼마를 쓸지 아무도 모르기
때문입니다.

예산 상한에 닿으면 새 호출이 **차단**됩니다. 관리자에게 문의하세요.

---

## 화면 지도

| 화면 | 무엇을 하는가 |
| --- | --- |
| `/` | 홈 — 여기서 시작 |
| `/projects` | 프로젝트 |
| `/upload` | 이미지 올리기 |
| `/admin/jobs` | 묶음 작업 · 이어하기 |
| `/admin/overview` | 운영 상태 한눈에 (ADMIN) |
| `/admin/ops` | 설정·환경 (ADMIN) |
| `/admin/users` | 사용자 (ADMIN) |

---

## 지금 못 하는 것

정직하게 적습니다. 자세한 것은 `KNOWN_LIMITATIONS.md`.

- **발행한 콘텐츠를 마켓·CMS로 자동으로 보내지 못합니다.** 화면에서
  가져가야 합니다.
- **실제 AI Provider로 검증된 적이 없습니다.** 기본 설정은 `mock`이며,
  그때 나오는 결과는 **진짜 분석이 아닙니다.** 화면이 어떤 엔진이 도는지
  표시하므로 확인하세요.
- 상품 목록에 페이지네이션이 없습니다 — 많아지면 느려집니다.

---

## 막혔을 때

1. 화면에 적힌 문장을 그대로 읽어 보세요 — 대부분 다음에 할 일이 적혀
   있습니다.
2. 그래도 모르겠으면 관리자에게 **화면에 뜬 문장과 시각**을 알려 주세요.
   오류에는 요청 id가 붙어 있어 그 값 하나로 기록을 찾을 수 있습니다.
