# RELEASE_CHECKLIST — 정식 `v1.0.0` 출시 점검표

> TASK-5001 → TASK-5201 → **TASK-5301 갱신** · 실측일 2026-08-04
>
> **2026-08-04 갱신 (Windows 로컬 환경에서 재실측)**: 이 아래 값은
> **TASK-5201의 Linux 컨테이너 값이 아니라 현재 실행 환경의 실측값**입니다.
> 두 환경은 상태가 다르므로 섞어 읽지 마십시오.
>
> 바뀐 것 둘:
>
> - **`api.openai.com`이 열렸습니다** — `401` + `realm="OpenAI API"`.
>   프록시가 아닌 진짜 OpenAI임을 확인했습니다. 2번 항목 **해제**입니다.
> - **8번 항목이 새로 생겼습니다** — 제품이 "development는 전환 대상이
>   아니다"라고 판정합니다. **자격 증명을 넣어도 로컬에서는 출시할 수
>   없습니다.** 이것이 지금 최상위 저지 항목입니다.
>
> 자격 증명은 여전히 **이 환경에 도착하지 않았습니다** — 관련 환경변수 11개
> 전부 비어 있고 `apps/api/.env`는 존재하지도 않았습니다. 전달 방법은
> [`CREDENTIALS_HANDOFF.md`](CREDENTIALS_HANDOFF.md)에 있습니다.
>
> **사람이 준비해야 하는 항목은 이 표로만 관리합니다.** 자동으로 우회하거나
> mock으로 대체하지 않습니다 (CTO 지시 2).

---

## 한 장 요약

| | 항목 | 상태 | 누가 |
| --- | --- | --- | --- |
| **8** | **출시 환경 등급** | ❌ **development — 전환 대상이 아님** | 사람 |
| 1 | Provider Credential | ⚠️ **계정 준비됨 · 이 환경에 미도착** | 사람 |
| 2 | Network Allowlist | ✅ **`api.openai.com` 도달 — 해제** | — |
| 3 | Validation Environment | ❌ **없음** | 사람 |
| 4 | Production Host Inventory | ❌ **미확정** — 미선언 6개 | 사람 |
| 5 | Amazon S3 | ❌ **MinIO (로컬)** | 사람 |
| 6 | Backup Chain | ❌ **18.0시간 공백** (24시간에 6/24회) | 시간(호스트 24h 가동) |
| 7 | Recovery Drill | ✅ **수행 완료** (2026-08-05, 개발 호스트) | — |
| **9** | **운영 필수 환경변수** | ❌ **7개 누락** | 사람 |

**9개 중 2개 충족.** 나머지는 **전부 코드로 풀리지 않습니다** — 사람이 주는
것 5건, 시간이 지나야 되는 것 2건입니다.

> **순서가 바뀌었습니다.** 8번이 1번보다 앞섭니다 — 환경을 정하지 않으면
> 자격 증명을 어디에 넣을지도 정해지지 않습니다.

---

## 8. 출시 환경 등급 — ❌ **신규 · 최상위**

`pnpm cutover`가 이 환경에서 직접 말합니다:

> 이 환경(development)은 전환 대상이 아닙니다 — 실 Provider 전환은
> **production·staging에서 수행**합니다 (CTO 정책 3501-①).
> 위 판정은 **참고용**입니다.

즉 **development 등급에서는 운영 활성화가 영원히 0/3입니다.** 자격 증명을
넣어도, 네트워크가 열려도, S3를 붙여도 바뀌지 않습니다 —
`RELEASE_READINESS.md`가 정한 v1.0 조건 4번(`cutover` 3/3)이 **구조적으로
충족 불가**입니다.

**우회하지 않습니다.** `NODE_ENV`만 `production`으로 바꿔 등급을 속이는 방법이
있지만, 그러면 운영이 아닌 곳에서 얻은 성공 기록이 운영의 증거로 남습니다 —
이 저장소가 쉰 스프린트 동안 막아 온 바로 그 일입니다.

- [ ] 출시를 수행할 **production 또는 staging 등급의 실제 호스트** 확정
- [ ] 그 호스트에 1·3·4·5·6번을 적용 (이 로컬 머신이 아닙니다)

---

## 1. Provider Credential — ❌

**무엇이 필요한가**: `OPENAI_API_KEY` 등 실 Provider 키, 그리고
`LLM_PROVIDER`를 실 Provider로 변경.

**지금**: `LLM_PROVIDER=mock`.

> mock입니다 — 결과는 나오지만 Provider를 부르지 않습니다.
> **이 상태의 성공 기록은 연결의 증거가 아닙니다.**

**이것이 없으면**: 검증을 시작할 수 없고, 지금까지의 모든 초록은 **우리
스텁을 상대로 얻은 것**입니다.

- [ ] 실 Provider 키 발급
- [ ] `LLM_PROVIDER`를 실 Provider로 변경
- [ ] `GOOGLE_VISION_ENDPOINT`를 비워 공식 주소로 되돌리기

---

## 2. Network Allowlist — ✅ 해제 (2026-08-04)

공식 주소에 직접 붙어 확인했습니다:

| 주소 | 응답 (2026-08-04) |
| --- | --- |
| **`api.openai.com`** | ✅ **401** (도달) — 이전엔 `curl exit 56` |
| `api.anthropic.com` | 404 (도달) |
| `vision.googleapis.com` | 404 (도달) |
| `s3.amazonaws.com` | 307 (도달) |
| `sts.amazonaws.com` | 302 (도달) |

**프록시가 아니라 진짜 OpenAI입니다.** 응답 헤더로 확인했습니다:

```
GET https://api.openai.com/v1/models   → HTTP 401
www-authenticate: Bearer realm="OpenAI API"
openai-version: 2020-10-01
x-request-id: 8f16b716-f369-4365-a9fc-b13d4cf81ed9
```

`401`은 **길이 열렸고 키만 없다**는 뜻입니다. 12스프린트 동안 막혀 있던
항목이 처음 풀렸습니다.

- [x] `api.openai.com` 도달 확인
- [ ] 어떤 Provider를 쓸지 확정 (제품 판정에 반영되려면 실 키가 필요합니다)

> **주의**: 이것은 **이 로컬 머신**에서 열린 것입니다. 실제 출시 호스트(8번)
> 에서 다시 확인해야 합니다 — 여기서 열렸다는 것이 거기서 열린다는 뜻은
> 아닙니다.

---

## 3. Validation Environment — ❌

**지금**: 검증 대상이 정해지지 않았습니다.

> 어디에 돌릴지 모르는 채로 시작할 수 없습니다.

**운영 DB에 대고 검증하지 않습니다** — 검증은 실제 호출을 하고 돈을 씁니다.

- [ ] 검증용 환경 확보(운영과 분리)
- [ ] 그 환경의 `DATABASE_URL`·저장소·호스트 확정

---

## 4. Production Host Inventory — ❌

| | 값 |
| --- | --- |
| 선언됨(`PRODUCTION_HOSTS`) | 2개 |
| **쓰이는데 목록에 없음** | **6개** |
| 목록에 있으나 이번에 안 보임 | 1개 |

미선언 6개: `cdn.acos.example` · `api.internal` · `m.acos.example` ·
`v6-notation.acos.example` · `shop.acos.example` · `old.acos.example`

**자동으로 넣지 않습니다.** 넣으면 스테이징까지 운영으로 올라가 정작 검증
대상이 막힙니다. **무엇이 운영인지는 사람만 압니다.**

- [ ] 6개 각각이 운영인지 판정
- [ ] `PRODUCTION_HOSTS` 갱신

---

## 5. Amazon S3 — ❌

**지금**: MinIO(`localhost:9000`, 개발 전용). 이 환경에는
`AWS_ACCESS_KEY_ID`가 **아예 없습니다**(TASK-5201의 더미 키조차 없음).

남는 대가:

- `storage-standard` **manual** — "저장소 엔드포인트가 설정되지 않았습니다"
- `storage-protection` · `backup-bucket-protection` **warn** —
  버전 관리·복제가 꺼져 있습니다

> **정정 (2026-08-04)**: 이전 판(결정 1801-④)은 개발 저장소가 보호 상태를
> 조회할 수 없어 이 두 항목이 **"영영 manual로만 남는다"** 고 적었습니다.
> **틀렸습니다** — MinIO에서는 조회가 되어 `manual`이 아니라 `warn`(꺼져
> 있음)으로 판정됩니다. 조회 불가는 **s3rver의 한계였고 저장소 계층의 한계가
> 아닙니다.** 개발 환경에서도 실제 보호 상태를 판정할 수 있습니다.

- [ ] AWS 계정·자격 증명 발급
- [ ] 이미지 버킷 / **백업 버킷을 따로** 생성 (같으면 한 버킷이 사라질 때 둘 다 사라집니다)
- [ ] `S3_ENDPOINT`를 `https://s3.<region>.amazonaws.com`으로 변경
- [ ] 절차: `docs/operations/s3-migration.md`

---

## 6. Backup Chain — ❌ 18.0시간 공백

> **2026-08-05 실측**: 관측 구간이 24시간을 넘어서면서 **판정 유보가 fail로
> 확정**됐습니다 — 최근 24.0시간에 **6/24회**, **18.0시간 공백**입니다.
>
> 08-04에는 "관측 구간이 짧아 판정할 수 없음"이었습니다. 그때 이것을 통과로
> 읽지 않은 것이 맞았습니다.
>
> **두 환경이 같은 결론에 도달했습니다.** 컨테이너 18.4시간 · 이 머신
> 18.0시간. 이 저지 항목은 **환경 탓이 아니라 실재합니다.**
>
> 그 구간은 복구할 수 없습니다 — 개별 백업이 모두 성공이어도, **돌지 않은
> 백업은 아무 데도 기록되지 않습니다.**

이건 자격 증명 문제가 아닙니다. **예약 백업이 도는 호스트가 24시간 이상
살아 있어야** 해소됩니다. 로컬 개발 머신은 그 호스트가 아닙니다.

원격 복제(`BACKUP_OFFSITE`)도 **꺼져 있습니다** — 백업이 데이터베이스와
같은 곳에만 있어, **그 곳이 사라지면 백업도 함께 사라집니다.**

- [ ] 운영 호스트에서 `OPS_SCHEDULED_CHECKS` 켜기
- [ ] 24시간 이상 돌려 사슬 채우기
- [ ] `BACKUP_OFFSITE` 켜기 + 백업 버킷 지정

---

## 7. Recovery Drill — ✅ 수행 완료 (2026-08-05)

`docs/operations/recovery-guide.md` §2 절차를 밟고, **복원에서 멈추지 않고
복원본으로 애플리케이션을 기동**했습니다 — 복원이 "파일이 돌아왔다"가 아니라
**"서비스가 돌아왔다"** 인지 확인하려면 그래야 합니다.

| 확인 | 값 |
| --- | --- |
| 백업 | 118,484바이트 · **269항목** · 367ms · 체크섬 · 무결성 통과 |
| 복원 | 별도 DB `acos_restore`에 **49테이블** · 1,404ms |
| 스키마 | 마이그레이션 **57건** up to date |
| **복원본으로 기동** | 포트 4100 · `/health` **200** · **4.1초** |
| 로그인 | ADMIN 성공 — id가 **운영과 동일** |
| 조회 | `/projects` 200 |
| 행 수 대조 | `users` 1=1 · `user_audit_log` 0=0 · `kpi_snapshots` 9=9 |

판정: `drill: pass` · `objectives`(RPO/RTO) `pass` · `db-major-change` 요구
해소. Go-Live **0/8 → 2/8**.

**다만 이 리허설이 증명하지 않는 것**:

- 개발 호스트에서 했습니다 — **운영 호스트의 RTO는 모릅니다.**
- 이 DB는 거의 비어 있습니다(프로젝트 0건) — **TASK-4901의 리허설보다 약한
  검증**입니다.
- 원격 복제가 꺼져 있어 **호스트가 사라지는 시나리오는 검증하지 못했습니다.**
- 6.1초는 **측정된 하한**이며, 장애를 알아차리고 결정하는 시간은 포함하지
  않습니다.

**리허설이 결함 하나를 찾아냈습니다** — `BACKUP_RESTORE_DB_URL`을
`DATABASE_URL`과 같게 둬도 **개발 등급에서는 기동이 막히지 않습니다.**
자세한 것은 `reports/CTO_REPORT.md` §4-7(N-8)에 있습니다. 운영에서는 막히므로
출시 저지 항목은 아닙니다.

- [ ] 출시 호스트(8번)에서 리허설을 다시 수행 — 개발 호스트의 RTO는 운영의
      RTO가 아닙니다

---

## 9. 운영 필수 환경변수 — ❌ 7개 누락

코드의 단일 원천(`ENV_SPECS` **96개**)에서 뽑은 목록입니다. 운영에서는 이
7개가 없으면 **기동 자체가 거부됩니다.**

`S3_ENDPOINT` · `S3_BUCKET` · `S3_ACCESS_KEY` · `S3_SECRET_KEY` ·
`AUTH_ADMIN_EMAIL` · `AUTH_ADMIN_PASSWORD` · `BACKUP_DIR`

추가로 **운영 권고 9건**이 있고, 그중 `ALERT_WEBHOOK_URL`은 권고가 아니라
**Go-Live 8항목 중 하나**입니다.

전체 표와 설명: [`CREDENTIALS_HANDOFF.md`](CREDENTIALS_HANDOFF.md) §7

---

## 이 표를 다 채운 뒤에 할 일

**8번(환경)이 정해진 그 호스트에서** 수행합니다. 로컬에서는 3번째 줄이
영원히 오지 않습니다.

```bash
POST /ops/validation-run/execute     # validation_runs 기록이 생겨야 합니다
pnpm cutover                          # 운영 활성화 3/3 (development에서는 불가)
git tag -a v1.0.0 ...                 # 그다음이 태그입니다
```

**순서를 바꾸지 않습니다.** 강제로 여는 방법은 없고, 있으면 그것은 게이트가
아닙니다.
