# 검증 환경 준비 안내 (Production Validation Sprint)

> TASK-4301, Sprint 43 — CTO 정책 4301-⑤⑥
>
> 이 문서는 **운영 담당자가 무엇을 주어야 검증 스프린트가 시작되는지**를
> 적습니다. 여섯 스프린트 연속 같은 항목이 남아 있고, 그 이유는 이것들이
> 코드로 해결되지 않기 때문입니다.

## 지금 무엇이 막고 있는가

명령 하나로 확인합니다:

```bash
API_BASE=https://<api-host> GATE_EMAIL=... GATE_PASSWORD=... \
  pnpm validation:preflight
```

- `exit 0` — 준비 단계가 전부 끝났습니다.
- `exit 1` — 아직입니다. 무엇이 막고 있는지 한 줄씩 출력합니다.
- `exit 2` — **판정하지 못했습니다.** 통과가 아닙니다.

**강제로 통과시키는 옵션은 없습니다.** 두면 그것이 기본 사용법이 되기
때문입니다(정책 4101-⑥). 막는 조건 자체를 없애는 것이 유일한 길입니다.

화면으로 보려면 `/admin/readiness`입니다. 그 화면은 다른 판정을 **인용만**
하고 여기서 다시 판정하지 않습니다.

> **본보기 파일**: `config/validation.env.example` — 채워야 하는 칸의 이름과
> 이유만 적혀 있고 **비밀은 하나도 없습니다.** 예시 값에 진짜처럼 보이는
> 키를 적어 두면 그것이 그대로 복사돼 들어가고, 나중에 "왜 이 키였지"에
> 답할 수 없게 됩니다.
>
> **절차**: `docs/operations/production-activation-runbook.md` — 시작한 뒤
> 무엇을 어떤 순서로 하고 잘못되면 어떻게 되돌리는지 적었습니다.

## 사람이 주어야 하는 것 (4건)

### 1. 실 Provider 자격 증명

| | |
| --- | --- |
| 무엇 | `OPENAI_API_KEY` · `ANTHROPIC_API_KEY` · `GEMINI_API_KEY` 중 검증 대상 |
| 왜 | 실 호출 없이 재는 모든 숫자는 스텁의 숫자입니다 |
| 확인 | `GET /ops/activation`의 `credentials` 조건이 충족으로 바뀝니다 |

키를 넣기 전에 **단가가 승인·적용돼 있는지** 확인하세요(정책 3101-①).
가격표에 없는 모델은 비용이 `null`(미산정)로 남고, 그 호출의 금액은 예산
계산에서 빠집니다.

### 2. 공식 주소로 나가는 길

| | |
| --- | --- |
| 무엇 | `api.openai.com` 등 Provider 공식 도메인에 대한 아웃바운드 허용 |
| 왜 | 지금 이 환경의 프록시가 막고 있습니다 (HTTP 403) |
| 확인 | `GET /ops/cutover`의 도달 점검이 `unreachable`에서 벗어납니다 |

**키가 틀린 것과 길이 막힌 것은 다릅니다.** 길이 막혀 있으면 올바른 키를
넣어도 실패하고, 그 실패는 "키가 잘못됐다"로 잘못 읽힙니다.

### 3. 검증용 환경 (스테이징)

| | |
| --- | --- |
| 무엇 | 검증 대상 주소 + 그 환경에 배포 1회 + `VALIDATION_TARGET_URL` · `VALIDATION_TARGET_ACK` |
| 왜 | 실 호출은 돈이 나가고 외부에 흔적을 남깁니다. 개발자 노트북에서 운영 키로 스모크를 돌리는 것은 검증이 아니라 사고입니다 |
| 확인 | `GET /ops/validation-run`의 `target.verdict`가 `accepted` |

이 값은 **형식만 맞으면 통과하지 않습니다**(정책 4101-①):

| 적은 값 | 판정 | 결과 |
| --- | --- | --- |
| 운영 호스트 목록에 있는 주소 | `production` | 확인을 받아도 **거부** |
| 지금 이 인스턴스 | `self` | 거부 |
| 사설망·노트북 | `local` | 거부 |
| `VALIDATION_TARGET_ACK` 불일치 | `unacknowledged` | 거부 |
| 그 외 | `accepted` | 통과 |

주소를 **적는 것**과 그곳에 돈이 나가는 호출을 **돌려도 된다고 말하는
것**은 다른 행동입니다. 그래서 확인 값(`VALIDATION_TARGET_ACK`)을 따로
받습니다 — 배포 설정을 복사하다 딸려 온 값과 사람이 의도해서 넣은 값을
가릴 방법이 그것밖에 없습니다.

또한 `DEPLOY_TIER=staging`으로 선언했다면 `NODE_ENV`도 `production`이어야
합니다(정책 4101-③). 개발 모드로 도는 환경에서 잰 값은 운영의 값이
아니고, 그러면 검증 결과가 제품의 것인지 설정의 것인지 나중에 가릴 수
없습니다.

### 4. 운영 호스트 목록

| | |
| --- | --- |
| 무엇 | `PRODUCTION_HOSTS`에 운영 도메인을 쉼표로 나열 |
| 왜 | 검증 대상 보호는 이 목록과 대조해서 동작합니다. 비어 있으면 보호가 지켜 주는 것이 아니라 **그냥 통과시키고 있는 것**입니다 |
| 확인 | `GET /ops/hosts`의 `undeclared`가 0개 |

**목록은 사람만 바꿉니다.** 시스템은 실제 트래픽의 `Host` 헤더를 관측해
"목록에 없는데 쓰이는 호스트"를 묻기만 합니다(정책 4301-①). 자동으로
채우지 않는 이유는 둘입니다:

1. 자동 등록은 이 보호를 스스로 무력하게 만듭니다 — 지금까지는 "우리가
   몰라서" 통과시켰다면, 그때부터는 "우리가 등록해서" 통과시킵니다.
2. `Host` 헤더는 **요청하는 쪽이 적는 값**입니다. 자동 등록은 바깥에서 우리
   보호 목록에 글을 쓰게 하는 것과 같습니다.

관측이 상한(50개)을 넘어 잘리면 `/ops/hosts`가 "관측 불완전"이라고
말합니다. 그 상태에서 "목록에 없는 호스트 0개"는 사실이 아니라 **우리가 더
안 본 것**입니다.

### 5. (선택) 신뢰하는 프록시 — TASK-4401 정책 ①

| | |
| --- | --- |
| 무엇 | `TRUSTED_PROXY_IPS`에 프록시 주소를 쉼표로(단일 IP 또는 CIDR) |
| 왜 | 리버스 프록시가 Host를 바꿔 전달하는 구성에서는 우리가 보는 `Host`가 내부 주소이고, 사용자가 실제로 친 도메인은 `X-Forwarded-Host`에 있습니다 |
| 확인 | `GET /ops/hosts`의 `trustedProxy.viaProxy`가 0보다 큽니다 |

**선언이 없으면 전달 헤더를 보지 않습니다** — 기본값은 언제나 "안
믿는다"입니다. 선언 없이 보면 누가 적었는지 모르는 이름이 관측에 들어가고,
그것은 관측이 아니라 **바깥에서 온 글**입니다.

세 가지를 더 조심합니다:

- **신뢰하지 않는 상대가 보낸 전달 헤더는 버리되 셉니다.** 조용히 버리면
  "누군가 프록시인 척했다"는 사실까지 함께 사라집니다.
- **값이 여러 개면 추측하지 않습니다.** 덧붙이는 프록시인지 덮어쓰는
  프록시인지 모르는 상태에서 하나를 고르면 고른 이유가 없는 값이 관측에
  들어갑니다 — 프록시가 이 헤더를 **덮어쓰도록** 설정해 주세요.
- **`0.0.0.0/0`은 맞지 않는 것으로 봅니다.** 그건 신뢰 경계가 아니라 경계를
  없앤 것입니다.

## 우리 쪽에 남은 것

### KPI 기준선

검증 전후를 비교하려면 시작 시점의 점이 있어야 합니다. 스냅샷은 하루 1회
쌓이며, **두 점이 최소 20시간은 떨어져 있어야** 기준선으로 셉니다. 몇 분
간격으로 두 번 찍어 이 단계를 통과시키면 그건 기준선이 아니라 **초록을 산
것**입니다.

## 검증을 시작한 뒤

`POST /ops/validation-run`은 준비가 끝나지 않으면 **403**과 함께 막는
이유를 돌려줍니다. 시작한 뒤의 순서는 7단계이며 **되돌릴 수 없는 것이 뒤에**
있습니다 — 실제로 돈이 나가고 외부에 흔적이 남는 것은 4단계(스모크)부터
입니다.

검증 기간에는 스테이징에서 실패를 일부러 만들기도 합니다. 그 실패는
`warning`으로 나가고 `critical`로 울리지 않습니다(정책 4101-③) — 그러지
않으면 진짜 운영 장애가 그 속에 묻힙니다.

## 관련 문서

- `docs/operations/production-cutover.md` — 운영 전환 절차
- `docs/operations/real-provider-smoke.md` — 실 호출 스모크
- `docs/operations/production-runbook.md` — 운영 런북
- `reports/CTO_REPORT.md` — 스프린트별 준비 상태 보고
