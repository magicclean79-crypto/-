# Disaster Recovery (TASK-1601 · 1701 · 1801 · 1901 · 2001, Sprint 16~20)

**지금 무너지면 되살릴 수 있는가**를 다루는 문서입니다.
배포 절차는 [production-runbook.md](production-runbook.md), 장애 대응 순서는
[recovery-guide.md](recovery-guide.md)를 참고하세요.

> 첫 화면은 `/admin/operations`(ADMIN)입니다 — 백업·복원 검증·재해 복구
> 체크리스트·Redis·SMTP가 한 화면에서 판정됩니다.
> API로는 `GET /ops/readiness`가 같은 판정을 돌려줍니다.

---

## 0. 원칙

- **복원해 보지 않은 백업은 백업이 아닙니다.** 그래서 백업 이력과 복원 검증
  이력을 한 쌍으로 판정합니다. 복원 검증 이력이 없으면 `missing`이고,
  그것은 통과가 아닙니다.
- **미구성과 실패는 다릅니다.** 복원 대상 DB(`BACKUP_RESTORE_DB_URL`)가 없으면
  "미구성"으로 표시하지, 실패로 세지 않습니다.
- **운영 DB에는 복원하지 않습니다.** 복원 검증은 반드시 별도 DB에서 합니다 —
  검증하려다 데이터를 잃는 것이 최악입니다.
- **자동 판정이 불가능한 항목은 `직접 확인`으로 남깁니다.** 이 문서를 읽고
  절차가 유효한지 확인하는 일(체크리스트의 `runbook` 항목)이 그렇습니다.

---

## 1. 무엇이 백업되는가

| 대상 | 방법 | 주기 |
| --- | --- | --- |
| PostgreSQL 전체 | `pg_dump --format=custom` | **1시간 간격** `OPS_CHECK_BACKUP_INTERVAL` (CTO 결정 1701-①) |
| 복원 검증 | `pg_restore` → 별도 DB → 테이블 수 확인 | 매일 `OPS_CHECK_RESTORE_AT` (기본 03:30 로컬) |
| 이미지(S3) | 저장소 제공자의 버전 관리·복제 | 저장소 정책 (**직접 확인**) |

이미지 저장소는 이 시스템이 백업하지 않습니다 — S3 호환 저장소의 버킷 정책
(버전 관리·교차 리전 복제)에 의존합니다. 그 정책이 켜져 있는지는 사람이
확인해야 합니다.

### 시간대

일 1회 점검은 **운영 서버 로컬 시각**으로 돕니다 (CTO 결정 1501-①).
`TZ`를 지정하지 않으면 컨테이너 기본값(대개 UTC)을 따르므로,
"새벽 3시"가 운영자가 생각하는 새벽이 아닐 수 있습니다.

```bash
TZ=Asia/Seoul
```

---

## 2. 설정

| 변수 | 뜻 | 기본값 |
| --- | --- | --- |
| `BACKUP_DIR` | 덤프를 두는 디렉터리 — **운영 필수**(미설정 시 기동 차단, 결정 1601-①) | `/var/backups/acos` (개발 전용) |
| `BACKUP_RETENTION_DAYS` | 보관 일수 | 14 |
| `BACKUP_KEEP_MINIMUM` | 기간이 지나도 남길 최소 개수 | 3 |
| `BACKUP_TIMEOUT_MS` | `pg_dump`/`pg_restore` 제한 시간 | 600000 |
| `BACKUP_RESTORE_DB_URL` | **복원 검증 전용** DB | 없음(미구성) |
| `OPS_CHECK_BACKUP_INTERVAL` | 백업 간격 (`15m`·`1h`) | `1h` (결정 1701-①) |
| `OPS_CHECK_RESTORE_AT` | 복원 검증 시각 HH:MM | `03:30` |
| `OPS_CHECK_ARCHIVE_AT` | 경보·Dead Letter 보관 시각 | `04:00` |
| `BACKUP_OFFSITE` | 덤프를 오브젝트 저장소에도 올린다 (`on`/`off`) | `off` |
| `BACKUP_BUCKET` | **백업 전용 버킷** — 이미지 버킷과 분리 (결정 1701-②) | `<S3_BUCKET>-backups` |
| `BACKUP_OFFSITE_PREFIX` | 원격 복제 키 접두사 | `backups/` |
| `OPS_DRILL_INTERVAL_DAYS` | 복구 리허설 주기 (결정 1701-⑤) | 90 (분기 1회) |
| `OPS_DRILL_GRACE_DAYS` | 리허설 기한 초과 후 경보 유예 | 14 |
| `ALERT_COOLDOWN_BACKUP_PERF_MS` | 백업 성능 경보 재알림 간격 | 전체 기본값 |
| `ALERT_COOLDOWN_BACKUP_INTEGRITY_MS` | 사슬·원격·규모 경보 재알림 간격 (2001) | 전체 기본값 |
| `BACKUP_MAX_AGE_HOURS` | 백업 신선도 한계 | 48 (2일, 결정 1601-⑤) |
| `RESTORE_MAX_AGE_HOURS` | 복원 검증 신선도 한계 | 192 (8일, 결정 1601-⑤) |
| `BACKUP_RPO_HOURS` | 허용 최대 손실 구간 | **백업 간격의 2배** (기본 2시간) |
| `BACKUP_RTO_MINUTES` | 복원 허용 시간 | 30 |

> `BACKUP_DIR`는 **컨테이너 밖 볼륨**을 가리켜야 합니다. 기본값 그대로 두고
> 볼륨을 붙이지 않으면 컨테이너가 재시작할 때 백업이 함께 사라집니다.

> `BACKUP_RESTORE_DB_URL`에 **운영 DB를 절대 넣지 마십시오.**
> 복원은 `--clean`으로 대상 스키마를 지우고 씁니다.
> TASK-1701부터 **서버가 이를 강제합니다** (CTO 결정 1601-②): 호스트·포트·DB
> 이름이 `DATABASE_URL`과 같으면 **기동하지 않으며**, 실행 시점에도 복원을
> 거부합니다. 자격 증명이 달라도 같은 DB는 같은 DB로 봅니다.

`BACKUP_KEEP_MINIMUM`은 정리가 마지막 백업까지 지우는 것을 막습니다 —
보관 기간이 지났더라도 최근 N개는 남습니다.

---

## 3. 판정 기준

`GET /ops/readiness`가 돌려주는 판정입니다.

### 백업

| 판정 | 조건 | 의미 |
| --- | --- | --- |
| `ok` | 최근 성공, 크기 정상 | 복구 지점이 있습니다 |
| `stale` | 마지막 성공이 2일 초과 | 백업 예약이 도는지 확인 |
| `failed` | 마지막 시도 실패, 또는 크기 1KB 미만 | 즉시 수동 백업 |
| `missing` | 이력 없음 | **복구할 지점이 없습니다** |

크기 검사가 있는 이유: `pg_dump`는 부분 실패에도 0에 가까운 파일을 남길 수
있고, 그것을 "성공"으로 세면 복구 계획이 통째로 거짓이 됩니다.

### 복원 검증

| 판정 | 조건 |
| --- | --- |
| `ok` | 최근 8일 안에 복원 성공, 테이블 확인 |
| `stale` | 마지막 성공이 8일 초과 |
| `failed` | 복원 실패 또는 복원 후 테이블 0개 |
| `missing` | 이력 없음 — 복원해 본 적이 없습니다 |

### 재해 복구 체크리스트

| 항목 | 복구 가능성 좌우 | 자동 판정 |
| --- | --- | --- |
| 백업 존재·신선도 | ○ | 예 |
| 복원 검증 | ○ | 예 |
| 데이터베이스 연결 | ○ | 예 |
| 이미지 저장소 접근 | ○ | 예 |
| **덤프 무결성** (1701) | ○ | 예 |
| **복원 대상 분리** (1701) | ○ | 예 |
| **백업 원격 복제** (1701) | × | 예 |
| **이미지 저장소 보호** (1701) | × | 예 / **직접 확인** |
| **복구 목표 (RPO·RTO)** (1701) | × | 예 / **직접 확인** |
| **복구 리허설 (분기 1회)** (1801) | × | 예 / **직접 확인** |
| **백업 버킷 보호** (1901) | × | 예 / **직접 확인** |
| **백업 소요 시간** (1901) | × | 예 / **직접 확인** |
| **백업 사슬 연속성** (2001) | ○ | 예 / **직접 확인** |
| **원격 사본 무결성** (2001) | × | **직접 확인** (수동 실행) |
| **운영 저장소 표준** (2001) | × | 예 |
| 분산 잠금(Redis) | × | 예 |
| 경보 전달 채널 | × | 예 |
| 복구 절차 숙지·연락 체계 | × | **직접 확인** |

### 덤프 무결성 · 원격 복제 · 복구 목표 (TASK-1701)

- **무결성**: 백업 직후 `pg_restore --list`로 목차를 읽고 SHA-256을 기록합니다.
  읽히지 않으면 **복구 불가**입니다 — 크기가 정상이어도 복원할 수 없는 파일이면
  복구 지점이 아닙니다.
- **원격 복제**(`BACKUP_OFFSITE=on`): 덤프를 오브젝트 저장소에도 올립니다.
  복제 실패는 백업을 실패시키지 않되(덤프는 이미 있습니다) 화면에 드러납니다.
  원격 사본이 온전한지는 기록된 SHA-256과 대조해 확인합니다.
- **RPO**: 마지막 성공 백업 이후 경과 = 지금 무너지면 잃을 최대 구간.
- **RTO**: **마지막 복원 검증에 실제로 걸린 시간**(측정치). 다만 이는 **하한**
  입니다 — 장애를 알아차리고 결정하고 인프라를 준비하는 시간은 포함하지
  않습니다. 측정치가 없으면 `직접 확인`이며 통과가 아닙니다.

### 이미지 저장소 보호 (CTO 결정 1601-④ · 1701-③)

**애플리케이션은 이미지를 백업하지 않습니다.** 저장소의 버전 관리·복제 상태를
읽어 체크리스트에 표시만 합니다.

| 상태 | 개발 | 운영 |
| --- | --- | --- |
| Versioning 꺼짐 | 주의 | **실패 — 운영 필수** (결정 1701-③) |
| Replication 꺼짐 | 주의 | 주의 — 운영 권장 |
| 조회 불가 | **직접 확인** | **직접 확인** |

조회를 지원하지 않는 저장소를 "정상"이라고 말하면 그 화면은 거짓이 됩니다 —
확인하지 못한 것을 "꺼짐"이나 "정상"으로 단정하지 않습니다.

### 백업 소요 시간 (CTO 결정 1801-①)

`backup_runs.durationMs`에 기준을 붙였습니다. **자동으로 간격을 바꾸지
않습니다** — 시스템이 스스로 백업을 드물게 만들면 손실 구간이 조용히
늘어납니다. 그 판단은 사람이 합니다.

| 소요 | 판정 | 경보 |
| --- | --- | --- |
| 2초 미만 | 정상 | 없음 |
| 2~10초 | 주의 | **없음** — 화면에만 드러냅니다 |
| 10초 초과 **연속 3회** | 실패 | warning |
| 30초 초과 (1회) | 실패 | **critical** |

연속 3회를 요구하는 이유는 **한 번 느린 것은 흔하기 때문**입니다. 30초는 한
번만 넘어도 심각으로 봅니다 — 그 정도면 일시적 경합으로 설명되지 않습니다.
실패한 백업은 성능 판정에서 제외합니다(실패는 백업 항목이 이미 잡아냅니다).

### 저장소 표준 (CTO 결정 1801-④ · 1901-③)

**Sprint 20부터 운영 저장소 표준은 Amazon S3입니다.** MinIO·s3rver는
**개발 전용**이며, 버전 관리·복제 조회를 지원하지 않아 보호 상태를 자동으로
확인할 수 없습니다.

체크리스트 항목 `storage-standard`가 이를 판정합니다.

| 상황 | 판정 |
| --- | --- |
| 운영 · `*.amazonaws.com` | 통과 |
| 운영 · 그 외 호스트 | **실패** |
| 운영 · 미설정 | **실패** |
| 개발 · 아무 호스트 | 통과 (`개발에서는 정상입니다`) |
| 해석할 수 없는 주소 | 직접 확인 |

**개발자에게 S3를 요구하지 않습니다** — 개발에서 개발 저장소를 쓰는 것은
정상입니다. 표준은 운영에만 적용합니다.

백업 버킷도 이미지 버킷과 **같은 규칙**으로 판정합니다 (결정 1801-③).

### 백업 사슬 연속성 (TASK-2001)

**개별 백업이 모두 성공이어도 사슬은 끊길 수 있습니다.** 실패는 이력에
남지만 **돌지 않은 백업은 아무 데도 기록되지 않기** 때문입니다. 서버가
멈춰 있던 동안에는 실패 기록조차 없어, 백업 이력만 보면 전부 성공으로
보입니다.

그래서 **있어야 할 개수와 실제 개수를 비교합니다.**

- 관측 창: 기본 24시간 (`DEFAULT_CHAIN_WINDOW_MS`)
- 한계: **백업 간격의 2배** — 한 번 걸러도 경보하지 않습니다
- 창 시작 ~ 첫 백업, 백업 사이, 마지막 백업 ~ 지금을 모두 공백으로 셉니다
- 공백이 한계를 넘으면 **실패**이고, `backup-chain`은 **복구 필수 항목**입니다
  — 그 구간은 되돌아갈 수 없기 때문입니다
- 심각 경보 `backup-integrity:chain`이 나갑니다

**이력이 전혀 없는 첫 도입**에서는 판정하지 않습니다 (`직접 확인`) — 처음
켠 서버를 "사슬이 끊겼다"고 말할 수는 없습니다. 다만 **기동 전에 이미
백업이 돌던 시스템이라면 꺼져 있던 동안의 공백은 진짜 공백입니다** —
재기동이 공백을 지우지 않습니다.

### 원격 사본 무결성 (CTO 결정 1701-④ 후속)

원격에 올렸다는 **기록**과 원격에 사본이 **실제로 있다**는 것은 다릅니다.
`POST /ops/backup/verify-remote`는 마지막 원격 사본을 **내려받아** SHA-256을
기록된 체크섬과 대조합니다.

- **기본으로 돌지 않습니다** — 전송 비용이 듭니다 (결정 1301-⑤의 연장).
  확인하지 않은 상태는 통과가 아니라 `직접 확인`입니다.
- `missing` — 올렸다고 기록됐지만 실제로는 없습니다. 저장소 수명 주기 정책이나
  삭제 여부를 확인하십시오.
- `mismatch` — 업로드가 잘렸거나 나중에 덮어써졌습니다. **이 사본으로는 복구를
  장담할 수 없습니다.**
- 실패는 심각 경보 `backup-integrity:remote`입니다. 로컬 덤프로 복구는 되므로
  **복구 필수 항목은 아닙니다.**

### 데이터베이스 규모 재평가 (CTO 결정 1901-④)

백업 성능 기준(2초·10초·30초)은 **지금 규모에서 잰 값**입니다. 운영 DB가
아래에 도달하면 **실측으로 다시 재십시오.**

| 기준 | 경보 키 |
| --- | --- |
| 10GB | `backup-integrity:scale:10737418240` |
| 50GB | `backup-integrity:scale:53687091200` |
| 100GB | `backup-integrity:scale:107374182400` |

기준마다 키가 다릅니다 — 10GB를 해소하고 50GB에서 다시 알려야 하기
때문입니다. **기준을 자동으로 바꾸지는 않습니다** — 재평가는 사람이 합니다.

### 복구 리허설 (CTO 결정 1701-⑤ · 1801-⑤)

**분기 1회 실제 복구를 해 보는 것이 운영 표준입니다.** §5의 절차를 그대로
수행하고 `/admin/operations`의 "리허설 기록"에 결과를 남기십시오.

- **실패한 리허설도 기록합니다** — 오히려 그쪽이 값집니다. 절차가 깨졌다는
  사실을 사고 전에 알아낸 것이기 때문입니다.
- **수행자를 반드시 적습니다** — 누가 확인했는지 남지 않으면 기록이 아닙니다.
- 기한을 넘기면 주의 경보, 유예(14일)까지 넘기면 실패로 봅니다. **리허설이
  실패한 경우는 심각 경보**입니다 — 절차가 깨진 것이 확인됐기 때문입니다.
- 기록이 없는 상태는 경보하지 않습니다 — 아직 시작하지 않은 것이지 규칙을
  어긴 것이 아닙니다. 다만 화면에는 `직접 확인`으로 그대로 드러납니다.
- 리허설이 밀린 것은 **복구 가능 판정을 막지 않습니다** — 지금 복구가
  불가능한 것과 다릅니다.
- **배포 게이트에는 넣지 않습니다** (결정 1801-②) — 리허설이 밀렸다는 이유로
  긴급 배포가 막히면 안 됩니다. 경보로만 다룹니다.

#### 변경 후 즉시 추가 리허설 (CTO 결정 1801-⑤)

90일 주기는 **아무것도 바뀌지 않았을 때**의 간격입니다. 아래가 발생하면
주기와 무관하게 즉시 리허설을 수행하십시오 — **마지막 리허설이 검증한 것은
지금의 시스템이 아니기** 때문입니다.

| 사건 | `trigger` | 등록 |
| --- | --- | --- |
| 재해 복구 절차 변경 | `dr-change` | 운영자 직접 |
| 데이터베이스 대규모 변경 | `db-major-change` | **자동** (Major Migration) |
| PITR 도입 | `pitr-adoption` | 운영자 직접 |

```bash
curl -X POST http://localhost:4000/ops/drills/require -b cookies.txt \
  -H 'content-type: application/json' \
  -d '{"trigger":"dr-change","description":"복구 절차 개정","registeredBy":"홍길동"}'
```

등록하면 기한이 남아 있어도 리허설 항목이 **실패**로 바뀌고 주의 경보가
납니다. **성공한 리허설만 이 요구를 해소합니다** — 실패한 리허설은
"확인했다"가 아니라 "안 되더라"는 뜻입니다.

##### Major Migration 자동 등록 (CTO 결정 1901-①)

마이그레이션이 전부 대규모 변경은 아닙니다. 컬럼 하나 추가할 때마다 리허설을
요구하면 **부를 일이 아닌데 부르는 것**이고, 그러면 정작 불러야 할 때 오지
않습니다. 그래서 `db-major-change`는 **지정된 Major Migration에 한해서만**
자동 등록합니다.

지정은 `apps/api/prisma/major-migrations.json`에 둡니다.

```json
{ "majorMigrations": ["20260729210000_enterprise_backup"] }
```

기동할 때 `_prisma_migrations`와 대조해 **적용된 Major Migration**에 대해
요구를 등록합니다. `dr-change`와 `pitr-adoption`은 코드가 알 수 없는
사건이므로 **운영자가 직접 등록합니다.**

##### 중복 등록 금지 (CTO 결정 1901-⑤)

같은 `trigger`가 **미해소 상태이면 다시 등록하지 않습니다** (`409 Conflict`).
리허설 1회로 함께 해소되므로 쌓을 이유가 없고, 쌓이면 목록만 길어져 정작
무엇이 남았는지 보이지 않습니다.

##### 취소는 되고 삭제는 안 됩니다 (CTO 결정 1901-②)

```bash
curl -X POST http://localhost:4000/ops/drills/requirements/<id>/cancel -b cookies.txt \
  -H 'content-type: application/json' \
  -d '{"cancelledBy":"홍길동","reason":"절차 변경이 철회됨"}'
```

- `cancelledBy`와 `reason`은 **필수**입니다 — 왜 취소했는지가 남지 않으면
  기록이 아닙니다.
- 이미 리허설로 해소된 요구는 취소할 수 없습니다 (`409`).
- 취소된 요구는 목록에서 사라지지 않고 **사유와 함께 남습니다.** 지운 요구는
  왜 지웠는지 남지 않지만, 취소는 남습니다.
- 취소 후에는 같은 `trigger`를 다시 등록할 수 있습니다.

백업 방식(WAL·PITR·증분) 검토는 [backup-strategy.md](backup-strategy.md)에
있습니다.

### 복구 가능 판정

`복구 가능`은 위 ○ 항목에 실패가 하나도 없을 때만 표시됩니다.
Redis 장애는 복구 가능성을 낮추지 않습니다 — 예약 점검만 멈추고 LLM 호출은
계속됩니다 (CTO 결정 1501-②).

---

## 4. 수동 실행

화면(`/admin/operations`)의 버튼과 같은 동작입니다. 모두 ADMIN 전용입니다.

```bash
# 지금 백업 받기
curl -X POST http://localhost:4000/ops/backup/run -b cookies.txt

# 지금 복원 검증하기 (별도 DB에 복원)
curl -X POST http://localhost:4000/ops/backup/verify-restore -b cookies.txt

# 재해 복구 상태 확인
curl http://localhost:4000/ops/readiness -b cookies.txt

# 메일 경로 확인 (연결·인증만, 메일은 보내지 않음)
curl -X POST http://localhost:4000/ops/notifications/verify-smtp -b cookies.txt

# 원격 사본 내려받아 대조 (전송 비용이 듭니다 — 기본으로 돌지 않습니다)
curl -X POST http://localhost:4000/ops/backup/verify-remote -b cookies.txt

# 리허설 요구 목록 (취소된 것도 남아 있습니다)
curl http://localhost:4000/ops/drills/requirements -b cookies.txt
```

Provider Smoke는 **실제 과금**되므로 기본으로 돌지 않습니다
(CTO 결정 1301-①). `/ops/checks/run` 같은 "지금 점검"도 꺼져 있는 작업은
건너뜁니다. 켜려면 `OPS_CHECK_SMOKE_AT`에 시각을 지정하십시오.

---

## 5. 실제 복구 절차

### 5.1 데이터베이스를 되살린다

1. **먼저 현재 상태를 보존합니다.** 망가진 DB라도 지우지 마십시오 —
   원인 분석과 부분 복구의 유일한 근거입니다.
   ```bash
   pg_dump --format=custom --file /var/backups/acos/incident-$(date +%s).dump "$DATABASE_URL"
   ```
2. 복원할 덤프를 고릅니다. `/admin/operations`의 백업 이력에서 파일명과
   크기·시각을 확인합니다.
3. **빈 데이터베이스**를 만들고 복원합니다.
   ```bash
   createdb acos_restored
   pg_restore --clean --if-exists --no-owner --dbname "postgresql://.../acos_restored" \
     /var/backups/acos/<파일명>
   ```
4. 테이블 수와 핵심 데이터를 확인합니다.
   ```bash
   psql --tuples-only --no-align --command \
     "SELECT count(*) FROM information_schema.tables WHERE table_schema='public'" \
     "postgresql://.../acos_restored"
   ```
5. 확인이 끝나면 `DATABASE_URL`을 복원된 DB로 바꾸고 재기동합니다.
6. 마이그레이션 상태를 맞춥니다.
   ```bash
   pnpm --filter api exec prisma migrate deploy
   ```

> 덤프에는 `DATABASE_URL`의 `?schema=public` 같은 Prisma 전용 파라미터를
> 넣지 마십시오 — `pg_dump`가 `invalid URI query parameter`로 거부합니다.
> 서버는 이 파라미터를 자동으로 걷어냅니다.

### 5.2 잃은 구간을 파악한다

마지막 백업 이후의 데이터는 복구되지 않습니다.
`/admin/operations`의 "마지막 백업 N분 전"이 곧 **최대 손실 구간**입니다.
그 구간에 무엇이 있었는지는 실행 이력(`/executions`)과 감사 로그로
역추적합니다.

### 5.3 이미지 저장소

DB만 복원하면 콘텐츠에 걸린 이미지 참조가 깨질 수 있습니다.
저장소 버킷을 같은 시점으로 되돌리거나, 깨진 참조를 목록으로 뽑아
재업로드해야 합니다. 이 절차는 저장소 제공자에 따라 다릅니다 —
사용 중인 제공자의 절차를 여기에 추가하십시오.

---

## 6. 분기 1회 복구 리허설 (운영 표준, 결정 1701-⑤)

**§5의 복구 절차를 실제로 수행하고 결과를 기록하는 것**이 리허설입니다.
자동 판정으로 대신할 수 없으므로, 기록하지 않으면 체크리스트의
`복구 리허설`과 `복구 절차 숙지·연락 체계`가 `직접 확인`으로 남습니다.

```bash
curl -X POST http://localhost:4000/ops/drills -b cookies.txt \
  -H 'content-type: application/json' \
  -d '{"ok":true,"performedBy":"홍길동","durationMs":900000,"findings":"3단계 권한 누락"}'
```

- [ ] 이 문서의 명령을 **실제로 한 번 실행**해 봤는가 (읽기만 한 것은 아닌가)
- [ ] `BACKUP_DIR`가 컨테이너 밖 볼륨을 가리키는가
- [ ] `BACKUP_RESTORE_DB_URL`이 운영 DB가 **아닌지** 다시 확인했는가
- [ ] 이미지 저장소의 버전 관리·복제 정책이 켜져 있는가
- [ ] 장애 시 연락 대상과 순서가 최신인가
- [ ] 경보 채널(`/admin/operations`의 Slack·Email·Webhook)이 실제로 도착하는가
- [ ] 복원 검증이 최근 8일 안에 통과했는가
- [ ] 원격 사본의 SHA-256이 기록된 체크섬과 일치하는가
- [ ] 백업 버킷이 이미지 버킷과 **분리**되어 있는가 (결정 1701-②)
- [ ] **리허설 결과를 기록**했는가 (`POST /ops/drills`)

---

## 7. 증상별 대응

| 증상 | 원인 | 조치 |
| --- | --- | --- |
| 백업 `missing` | 예약이 한 번도 돌지 않음 | `OPS_SCHEDULED_CHECKS`·`OPS_CHECK_BACKUP_AT` 확인, 수동 실행 |
| 백업 `stale` | 예약이 멈춤 | 예약 점검 정지 경보 확인, 스케줄러 로그 |
| 백업 `failed` (크기 이상) | 디스크 부족·권한 | `BACKUP_DIR` 여유 공간, 프로세스 권한 |
| 복원 `missing` | 검증 대상 DB 미구성 | `BACKUP_RESTORE_DB_URL` 설정 |
| 복원 `failed` | 덤프 손상·버전 불일치 | `pg_restore` 로그, PostgreSQL 버전 확인 |
| Redis 30분 이상 장애 | 캐시 서버 다운 | Critical 경보 반복 — 예약 점검이 멈춘 상태, LLM 호출은 계속됨 |
| SMTP `미구성` | `SMTP_HOST`·`ALERT_EMAIL_TO` 없음 | 설정하거나, 메일을 쓰지 않기로 명시 |
| SMTP 검증 실패 | 인증·방화벽 | 자격 증명과 포트(587/465) 확인 |
| 무결성 `실패` | 덤프 손상·디스크 오류 | **즉시 수동 백업**, 디스크 상태 확인 |
| 원격 복제 `실패` | 저장소 자격 증명·용량 | 저장소 접근 확인 — 덤프 자체는 로컬에 있습니다 |
| 저장소 보호 `직접 확인` | 조회 미지원 저장소 | 제공자 콘솔에서 버전 관리·복제 확인 |
| 기동 실패 `BACKUP_DIR` | 운영 필수 미설정 | 볼륨 경로 지정 (결정 1601-①) |
| 기동 실패 `BACKUP_RESTORE_DB_URL` | 운영 DB를 가리킴 | 별도 DB 지정 (결정 1601-②) |
| 리허설 `기한 초과` | 분기 1회를 놓침 | 일정을 잡고 §6 수행 후 기록 |
| 리허설 `실패` | 절차가 깨짐 | 발견 사항대로 절차·권한을 고치고 다시 수행 |
| 저장소 보호 `실패`(운영) | Versioning 꺼짐 | 제공자 콘솔에서 버전 관리 활성 (결정 1701-③) |
| 백업 소요 `주의` | 2~10초 | 조치 불필요 — 추세만 관찰 |
| 백업 소요 `실패` | 10초 3회 연속 또는 30초 초과 | 간격·데이터 크기 검토 (자동 변경 없음) |
| 리허설 `변경 후 미수행` | DR·DB·PITR 변경 등록됨 | 즉시 리허설 수행 후 성공으로 기록 |
