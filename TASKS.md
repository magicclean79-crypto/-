# TASKS — AI Product Content OS 작업 대장

> AGENTS.md와 함께 작업의 기준이 되는 문서다.
> - CTO(아키텍트)가 TASK 스펙을 이 문서(또는 채팅 지시)로 내려주면 **미완료** 섹션에 등록된다.
> - "다음 진행해" 지시 시 **미완료 섹션의 최상단 TASK**를 구현한다.
> - 새로운 기능을 임의로 추가하지 않는다 — 스펙 없는 항목은 "제안(스펙 대기)"에만 둔다.
> - TASK 완료 = 품질 게이트(Build/Test/TS/ESLint) 통과 + `/reports/CTO_REPORT.md` 갱신.

## 미완료 (스펙 확정, 구현 대기)

- (없음 — TASK-4201 완료. CTO 리뷰/승인 대기 중이며 승인 전 다음 TASK를 시작하지 않는다)

## 완료 — Sprint 42

- [x] **TASK-4201 — Enterprise Cost & Neglect Intelligence Platform**: CTO 정책 4201-①~⑥ 반영. 주제는 **보호와 지표가 스스로 낡는 것을 막는다**. **운영 호스트 자동 검증**(정책 4201-① — `host-verification.ts` 신규. TASK-4101의 검증 대상 보호는 `PRODUCTION_HOSTS`와 대조해 동작하는데, 그 목록은 **사람이 손으로 적습니다.** 도메인이 늘거나 바뀌면 목록은 그대로 남고, **빠진 호스트는 아무 신호도 내지 않습니다** — 보호가 지켜 주는 것이 아니라 그냥 통과시키고 있을 뿐이고, 사고가 난 다음에야 "그 호스트가 목록에 없었네요"를 알게 됩니다. 그래서 관측(설정값)과 대조해 **묻습니다.** 다만 **자동으로 채우지 않습니다**: 그러면 보호가 스스로 무력해지고("우리가 몰라서" 통과시키던 것이 "우리가 자동 등록해서" 통과시키는 것이 됩니다), 게다가 **검증용 스테이징 호스트까지 운영으로 올려** 정작 검증 대상이 막힙니다. 실행 기록의 `baseUrl`도 넣지 않습니다 — Provider 도메인은 우리 운영 도메인이 아닙니다) + **연속 실패 기간과 방치**(정책 4201-② — `neglect.ts` 신규. TASK-4101의 비교는 **직전 1회**와만 해서 "3주째 같은 실패"가 `persisting`이라는 한 단어로만 보였습니다. 어제 시작된 실패는 아직 대응 중일 수 있지만 **석 달 된 실패는 대응하지 않기로 한 것**이고, 그것을 "실패 1건"으로 세는 화면은 그 사실을 감춥니다. 실패 수는 나쁜 일이 몇 개인지 말하고 **방치는 그것을 얼마나 오래 두었는지** 말합니다 — 실패 0건인 팀과 실패 1건을 석 달 둔 팀 중 후자가 더 나쁜데 실패 수만 보면 후자가 나아 보입니다. 그리고 **관측이 끊긴 구간을 "괜찮았던 구간"으로 세지 않습니다**: 창의 처음부터 나빴으면 실제로는 더 오래됐을 수 있으므로 **최소값**이라고 적고, 관측 공백도 함께 말합니다. 경보는 **기준을 넘을 때 한 건으로 묶어** 냅니다 — 시작은 이미 진단 경보가 냈고, 방치가 여러 개라는 것 자체가 하나의 소식입니다) + **진단 이력 보존**(정책 4201-③ — `retention.ts`에 `diagnostics` 추가, 기본 90일. **바닥이 14일인 이유**: 이력의 쓸모는 어제와의 비교(4101-②)와 연속 실패 기간(4201-②)인데, 이틀로 줄이면 **모든 연속 실패가 "2일째"** 로 보이고 석 달 방치된 항목이 어제 시작된 것과 똑같아집니다. 그건 보존 설정이 아니라 방치 지표를 끄는 것입니다) + **프로젝트별 운영 비용**(정책 4201-④ — `project-cost.ts` 신규, `executions.projectId`·`ocr_results.projectId`. 네 스프린트째 답할 수 없던 질문입니다. **이 기능이 거짓말하는 방식은 하나**입니다: 미배분을 프로젝트 비율로 나눠 얹는 것. 그러면 합계가 맞고 표가 깔끔해지지만 **배분할 수 없는 것을 배분하면 그 숫자는 관측이 아니라 만들어낸 것**이고, 그걸로 팀에 비용을 청구하게 됩니다. 미배분은 자기 칸에 그대로 두고, **비율의 분모도 미배분을 포함한 전체**로 씁니다(빼고 나누면 각 프로젝트의 몫이 실제보다 커 보입니다). **미산정**(금액을 모른다)과 **미배분**(주인을 모른다)도 가릅니다 — 한 칸에 넣으면 어느 쪽을 고쳐야 하는지 알 수 없습니다. 옛 기록은 전부 `null`이고 **`null`은 "공용"이 아니라 "모른다"** 입니다. 경보는 금액이 크다고 내지 않고 **귀속률이 낮을 때** 냅니다: 알리고 싶은 것은 "이 표를 믿을 수 없다"입니다) + **검증 준비에 호스트 목록 단계 추가**(정책 4201-⑤⑥ — 보호가 대조할 것이 정확해야 준비가 끝난 것입니다. 목록이 비어 있으면 준비도 끝나지 않았고, 실행 잠금(4101-⑥)은 그대로 유지됩니다) + 라이브 결함 3건 수정(**`localhost`를 두고 "이것이 운영 호스트라면 목록에 넣어 주세요"라고 물었다** — 그 제안을 따르면 검증 대상 보호가 모든 로컬 대상을 운영으로 보고 거부하고, 명백히 틀린 질문을 하는 목록은 곧 아무도 안 읽는다 · 한 시간 된 연속이 배지에서 "0일째"로 보였다 · **비용 귀속 경보가 한 번도 호출되지 않고 있었다** — 함수도 시험도 화면도 정상인데 부르는 곳만 없었고, 단위 시험은 함수를 직접 부르므로 전부 초록이었다. `cost-forecast` 예약 작업에 붙였다) (마이그레이션 50 · `20260818000000_project_cost`)

## 완료 — Sprint 41

- [x] **TASK-4101 — Enterprise Validation Governance Platform**: CTO 정책 4101-①~⑥ 반영. 주제는 **검증을 시작하기 전에 스스로를 막는다**. TASK-4001이 준비 상태를 판정했다면, 이번에는 **그 판정이 틀렸을 때 일어날 일**을 막는다. **검증 대상 보호**(정책 4101-① — `validation-target.ts` 신규. TASK-4001은 이 값을 **형식만** 검사했다. 그런데 이 주소가 하는 일을 생각하면 문제가 분명하다 — **실 자격 증명으로 과금되는 호출을 돌릴 곳**이다. 잘못 적으면 설정 실수가 아니라 사고다: 운영을 적으면 검증 스프린트가 **운영 데이터에 장애를 만들려고 시도**하고, 자기 자신을 적으면 **검증하다 죽었을 때 그 사실을 보고할 주체도 함께 죽고**, 노트북을 적으면 운영 키가 개발 기기에서 나간다. 셋 다 **한 글자 차이로** 일어나고 셋 다 일어난 다음에야 드러난다. 그래서 주소를 검사하고(운영 호스트 목록 대조·자기 자신·사설망), **따로 확인을 받는다**(`VALIDATION_TARGET_ACK`) — 주소를 **적는 것**과 그곳에 돈이 나가는 호출을 **돌려도 된다고 말하는 것**은 다른 행동이고, 배포 설정을 복사하다 딸려 온 값과 사람이 의도해서 넣은 값을 가릴 방법이 그것밖에 없다. 위험 순서대로 판정하며 앞의 셋은 **확인을 받아도 허용하지 않는다**) + **진단 이력과 비교**(정책 4101-② — `diagnostic-history.ts`·`diagnostic_runs` 신규. TASK-4001의 진단은 부를 때마다 새로 냈고, 그래서 "실패 2건"이 **오늘 새로 생긴 것**인지 **지난주부터 계속 그랬던 것**인지 알 수 없었다. 그 둘은 완전히 다른 소식이다 — 새로 생긴 것은 **어제 무언가를 바꿨다**는 뜻이고 지금 되돌릴 수 있다. 이 파일이 특히 조심하는 것은 **사라진 항목**이다: 활성화 항목은 해당되지 않으면 아예 안 만들어지고 기동 진단의 관측 이력 항목은 일일 진단에 없으므로, 순진하게 비교하면 그것들이 "복구됨"으로 집계된다 — **없어진 검사가 성과로 보고되는 것**이고, 우리가 여러 번 경계해 온 "없어진 검사는 실패하지 않는다"의 정확히 반대편 실수다. `recovered`와 `disappeared`를 **다른 칸**에 담는다. 경보는 **새로 나빠진 것만** 낸다 — 계속 나쁜 것을 매일 다시 부르면 그 경보부터 무시된다) + **배포 단계별 진단**(정책 4101-③ — `tier-policy.ts` 신규, `DEPLOY_TIER`. TASK-4001의 진단은 세상을 둘로 봤다: 운영이거나 아니거나. 개발자 노트북에 대해서는 옳았지만 **스테이징을 개발과 같은 칸**에 넣는 결과를 낳았고, 스테이징의 빨간불이 아무 데도 안 갔다 — 그러면 스테이징은 검증 환경이 아니라 **또 하나의 개발 환경**이다. 그런데 우리가 실 Provider 검증을 하려는 곳이 바로 거기다. 이제 스테이징도 경보한다. 다만 **등급은 warning까지**다: 검증 기간에는 여기서 실패를 일부러 만들기까지 하고, 그것이 `critical`로 울리면 **진짜 운영 장애가 그 속에 묻힌다.** 그리고 `DEPLOY_TIER=staging`인데 `NODE_ENV`가 production이 아니면 실패로 잡는다 — 개발 모드로 도는 환경에서 잰 값은 운영의 값이 아니고, 그러면 검증 결과가 제품의 것인지 설정의 것인지 나중에 가릴 수 없다) + **만료 초안 되살림**(정책 4101-④ — `draft-revival.ts` 신규, `incidents.revived*`. 되살리는 데 실제로 필요한 경우가 둘 있다: 석 달 뒤 비슷한 사고가 나서 "그때 그 초안이 이거였네"를 알게 되는 경우와, 휴가·이관으로 아무도 못 봤을 뿐인 경우. 여기서 가장 쉬운 실수는 **만료 표시를 지우는 것**이다 — 그러면 목록은 깨끗해지고 이력은 "확인된 장애 1건"이 되며, 나중에 이력을 읽는 사람은 **"우리 팀은 초안을 잘 처리한다"** 고 읽는다. 실제로는 3주 늦게 본 것인데도. `expiredAt`은 그대로 두고 **누가·언제·왜·얼마나 늦게** 손댔는지를 따로 적는다. 요약도 "만료된 것 중 N건이 실제 장애였다"를 **성과가 아니라 그만큼 늦게 알았다는 기록**이라고 직접 말한다) + **검증 실행 순서와 잠금**(정책 4101-⑤⑥ — `validation-run.ts` 신규. 정책 ⑥을 문서로만 적어 두면 규칙이 되고, **사람의 기억에 기대는 규칙은 반드시 다시 어긋난다** — 실제로 일어나는 모습은 "검증 날짜가 잡히고, 자격 증명은 아직 안 왔는데, 누군가 일단 돌려 보자고 누르는 것"이고, 그때 남은 **스텁 상대 성공 기록이 나중에 실연결의 증거로 읽힌다.** 그래서 코드가 막고 **막힌 이유를 그대로 말한다**(막기만 하고 이유를 안 말하면 사람은 우회로를 찾는다). `--force` 같은 것은 두지 않는다 — 두면 그것이 기본 사용법이 된다. 순서는 **되돌릴 수 없는 것을 뒤에** 둔다: 앞 단계는 전부 읽기·시험이고 실제로 돈이 나가는 것은 스모크부터다) + 라이브 결함 2건 수정(**검증 대상이 운영을 가리키는데도 "검증용 환경 확보 완료"로 셌다** — 주소가 비어 있지 않다는 것만 봤고, 같은 값을 두 곳에서 서로 다르게 판정하고 있었다. 그 결과 뒤 단계까지 줄줄이 열렸다 · 목록 문구가 "만료 23일 뒤 만료 뒤 확인"으로 겹쳤다) + 정책 4101-⑥의 실제 검증은 **여전히 시작할 수 없다** — 준비 10단계 중 사람이 줘야 하는 3건이 남아 있고, 이제는 그 사실이 **코드로 막힌다** (마이그레이션 49 · `20260817000000_diagnostic_history`)

## 완료 — Sprint 40

- [x] **TASK-4001 — Enterprise Operations Intelligence Platform**: CTO 정책 4001-①~⑥ 반영. 주제는 **시간축에서도 스스로를 속이지 않는다**. 지금까지의 판정은 전부 "지금 어떤가"였다 — 이번에 더한 것은 "**나아지는 중인가**", "**아무도 안 본 채 얼마나 지났는가**", "**아무도 안 부르면 언제 도는가**"이다. **장애 초안 수명**(정책 4001-① — `draft-lifecycle.ts` 신규, `incidents.expiredAt`. TASK-3901이 만든 초안은 질문이고, 질문을 만들어 놓기만 하면 **대답 없는 질문이 쌓인다**. 서른 개 쌓이면 목록은 읽히지 않고 초안은 있으나 마나가 된다 — 우리가 3901에서 "warning으로 초안을 만들면 목록이 뒤덮인다"고 경계한 것과 **같은 실패가 시간축에서** 일어난다. 그렇다고 **자동으로 기각하지 않는다**: 기각은 "아무것도 아니었다"는 판단이고 아무도 보지 않은 것을 시스템이 그렇게 판단하면 진짜 장애가 조용히 사라진다. 대신 3일이면 경보(초안이 몇 건이든 **경보는 1건으로 묶는다** — 초안마다 내면 막으려던 뒤덮임이 경보에서 다시 일어난다), 30일이면 **만료**로 표시한다. **만료는 기각이 아니다** — 기각은 "아무것도 아니었다", 만료는 **"아무도 판단하지 않았다"** 이고, 그래서 `status`는 `DRAFT`로 두고 `expiredAt`만 적는다. 같은 칸에 넣으면 나중에 이력을 읽는 사람이 **방치를 기각 실적으로** 읽는다. 만료가 경보보다 빠른 설정 조합은 경보가 한 번도 안 나므로 이유와 함께 되돌린다) + **KPI 추세**(정책 4001-② — `kpi-trend.ts`·`kpi_snapshots` 신규, `GET /ops/kpi/trend`. "MTTR 180분"은 그 자체로 아무 행동도 만들지 않고, 사람이 알아야 하는 것은 "나아지는 중인가"인데 그건 두 번 재야 안다. 추세가 거짓말하는 세 방식을 막는다: **한 점으로 선을 긋는 것**("0% 변화"는 안정을 뜻하는데 실제로는 아무것도 모르는 상태다 → `unknown`), **기준이 바뀐 구간을 비교하는 것**(지난주에 임계값을 느슨하게 바꿨다면 `watch → good`은 상태가 좋아진 게 아니라 기준이 내려간 것이다 → 그때 쓰인 임계값을 스냅샷에 함께 적어 `thresholdChanged`로 말한다), **방향을 모르는 것**(MTTR이 느는 건 나쁜 소식이고 통과율이 느는 건 좋은 소식이다). 스냅샷은 **하루 한 번** — 화면을 열 때마다 찍으면 사람이 많이 본 날의 표본이 많아져 평균이 그날로 기운다. 값을 낼 수 없었던 지표도 **줄을 만든다**(빼면 나중에 "그날은 좋았다"가 되는데 실제로는 "그날은 몰랐다"였다)) + **임계값 설정 UI·변경 이력**(정책 4001-③ — `/admin/kpi`에 설정 폼·추세·이력. 기준이 움직인 사실이 화면 밖에 있으면 다음에 이 화면을 보는 사람은 **상태가 좋아진 줄 안다**. 각 변경 줄에 느슨해졌는지를 붙이되, 판정할 수 없으면 `null`로 두고 `false`로 바꾸지 않는다) + **긴급 채널 미구성을 진단 대상으로**(정책 4001-④ — 3901에서 미구성이면 일반 채널로 되돌리게 했는데, 되돌리는 것은 옳아도 **되돌린 채로 잊히는 것**이 문제다. 폴백은 임시 조치이고 임시 조치는 아무도 보지 않으면 영구가 된다. 개발에서는 경고하지 않는다 — 개발자 로컬에 긴급 웹훅을 요구하면 그 경고가 배경 소음이 되고 그러면 정작 운영의 경고도 안 읽힌다) + **기동·일일 진단**(정책 4001-⑤ — `diagnostics.ts` 신규, `daily-diagnostics` 예약(07:00, 정리·스모크가 끝난 **뒤**), `GET /ops/diagnostics`. 판정은 많은데 전부 **누군가 부를 때만** 돌았고, 그래서 배포 직후와 매일 아침이라는 두 순간이 비어 있었다. **기동을 막지 않는다** — 알림 채널이 미설정이라고 API가 안 뜨면 그게 더 큰 사고다. 다만 조용하지도 않다. 그리고 **기동 직후의 "정상"은 아직 아무것도 안 해 본 정상**이라 그렇게 적는다) + **검증 스프린트 준비**(정책 4001-⑥ — `validation-plan.ts` 신규, `GET /ops/validation-plan`. **"준비 완료"를 스스로 선언하지 않는다** — 남은 것은 자격 증명·나가는 길·검증용 환경이고 셋 다 사람이 주는 것이라, 자동으로 초록을 칠하면 그 초록은 우리가 한 일이 아니라 **우리가 못 하는 일을 가린 것**이 된다. 대신 10단계마다 **누가 하는가·무엇이 증거인가·못 하는가 안 하는가**를 밝힌다) + 라이브 결함 5건 수정(진단 요약이 긴급 경로 항목을 안 세어 **배지와 목록이 어긋남** · 검증 대상 환경이 없는데 **지금 환경의 증거로 단계를 통과 처리** · 앞 단계에 막힌 것을 **"우리 쪽에 남은 일"로 셈** · 오버라이드가 없던 상태의 **첫 완화를 "판정 불가"로 표시**(이 기능이 잡으려는 바로 그 경우) · 초안 수명 요약과 목록의 수가 어긋남) + **마크다운 누출 가드**(같은 결함을 네 번 고쳤다 — 주석은 마크다운으로 쓰고 그 습관이 바로 아래 문자열로 흘러든다. `no-markdown.spec.ts`가 `src/ops` 전체의 **문자열 리터럴 안** `**`를 기계로 잡는다) + 정책 4001-⑥의 실제 검증은 **시작할 수 없다** — 사람이 줘야 끝나는 단계 3건이 남아 있다 (마이그레이션 48 · `20260816000000_kpi_snapshot`)

## 완료 — Sprint 39

- [x] **TASK-3901 — Enterprise Operations Control Platform**: CTO 정책 3901-①~⑥ 반영. 주제는 **통제 장치가 스스로를 속이지 못하게 한다**. **`activation-lost` 공식 채택**(정책 3901-① — TASK-3801의 확장 해석이 정식 계약이 됐다. 그 과정에서 **키 결함**을 찾아 고쳤다: 키에 충족 조건 집합만 넣으면 `완료 → 풀림 → 다시 완료`의 **두 번째 완료가 첫 번째와 같은 키가 되어 조용히 버려진다** — 되살아난 것은 처음 된 것만큼 중요한 소식인데 그것만 못 듣는 구조였다. 순번을 넣어 각 전이가 다른 키를 갖게 하고, 같은 순번의 동시 판정은 표의 유니크 제약이 그대로 막는다) + **KPI 임계값 운영 설정**(정책 3901-② — `kpi-thresholds.ts` 신규. 이 기능의 위험은 명백하다: **MTTR 임계값을 240분에서 600분으로 올리면 아무것도 나아지지 않았는데 대시보드가 초록이 된다.** 그래서 셋을 함께 넣었다 — 기본값이 아닌 임계값은 카드가 **"운영자 조정값"** 이라고 말하고, **느슨하게 바꾼 것과 엄격하게 바꾼 것을 가르며**(느슨한 것은 "초록을 산 것"이다), **범위 밖 값은 막는다**(MTTR 임계를 30일로 두는 것은 임계값이 아니라 임계값을 없앤 것이고 그건 설정이 아니라 우회다). 요약이 느슨해진 지표 수를 **먼저** 말한다) + **보존 정책**(정책 3901-③ — `retention.ts` 신규. **감사 기록은 다른 것보다 오래 남아야 한다** — 경보는 90일이면 충분하지만 감사는 사고가 난 뒤 거슬러 올라가는 용도이고 사고는 몇 달 뒤에 드러난다. 그래서 **바닥**을 뒀다: 감사 180일·이벤트 90일 미만은 보존 정책이 아니라 **기능을 끄는 것**이라 설정으로 막는다(기본 365/180일). 정리는 `alert-archive`에 편입했고, **아무것도 안 지웠어도 결과를 남긴다** — 없어진 기록은 스스로 말하지 못한다) + **긴급/일반 채널 분리**(정책 3901-④ — `urgent-routing.ts` 신규. 분리의 진짜 위험은 **침묵**이다: 긴급 채널을 설정하지 않으면 급한 알림만 아무 데도 안 가고 그 침묵은 "조용하다"로 읽힌다. 그래서 **미구성이면 일반 채널로 되돌리고 그 사실을 말한다**) + **경보 → 장애 초안 자동 승격**(정책 3901-⑤ — `incident-promotion.ts` 신규, `incidents.status`. TASK-3701의 "경보와 장애는 다르다"는 지금도 맞지만 **반대쪽 실패**도 있었다: 새벽 3시에 CRITICAL 경보가 40분 살다 저절로 풀리면 그건 장애였는데 기록이 없다. 그래서 자동으로 만드는 것은 **초안**이지 장애가 아니다 — 초안은 질문이고, 사람이 확인하면 장애가 되고 **기각하면 사유와 함께** 남는다("아무것도 아니었다"는 판단도 다음에 쓰인다). 초안은 **MTTR에도 진행 중 장애 수에도 들어가지 않는다.** 기본은 꺼짐이고, CRITICAL만·30분 이상·같은 경보로 한 번만) + 라이브 결함 3건 수정(**긴급 경로가 채널만 갈리고 주소는 그대로였다** · `all()`이 갱신되지 않아 **운영자가 바꾼 설정이 재시작 전까지 반영되지 않았다** · 새 경로 넷이 감사 목록에 원시 경로로 남았다 — 이번엔 **CI가 잡도록 가드 테스트**를 넣었다) + 정책 3901-⑥(실 Provider 활성화 검증)은 **검증 경로만 확인**했다 — 활성화 0/3 조건이고 `api.openai.com`이 프록시에 막혀 있다 (마이그레이션 47 · `20260815000000_incident_draft`)

## 완료 — Sprint 38

- [x] **TASK-3801 — Enterprise Operations Governance Platform**: CTO 정책 3801-①~⑤ 반영. 주제는 **아무도 보고 있지 않을 때에도 남는다**. **활성화 완료 자동 이벤트**(정책 3801-① — `activation-event.ts`·`ops_events` 신규, `GET /ops/events`. 이력은 **보러 가야 보이는데**, 마지막 조건이 채워지는 순간은 대개 아무도 화면을 보고 있지 않을 때 온다(방화벽이 열렸거나 결제가 풀렸거나 CI가 마침내 초록이 됐을 때) — 그 순간을 아무도 모르면 운영은 **이미 켤 수 있게 된 상태로 며칠을 더 논다**. **경계를 넘을 때만** 나고(진행 상황은 이력이 담당한다), 같은 전이는 **표의 유니크 제약**이 한 번으로 막는다(애플리케이션 로직만으로는 두 인스턴스가 동시에 통과한다). **처음 관측에서는 내지 않는다** — 우리가 못 보는 동안 이미 그 상태였을 수 있고 "방금 완료됐다"는 거짓말이 된다. 그리고 **완료만 알리지 않는다**: 풀린 것은 훨씬 급한 소식이고, 좋은 소식만 전하는 알림은 아무도 신뢰하지 않는다) + **장애 사후 분석**(정책 3801-② — `incidents`에 `fixKind`·`rootCause`·`temporaryFix`·`permanentFix`·`prevention`. **재시작으로 살린 것과 원인을 없앤 것이 목록에서 똑같이 "복구됨"으로 보이면 팀은 자기가 몇 개의 시한폭탄을 안고 있는지 모른다.** 닫을 때 임시/영구를 **반드시 고르게** 한다 — 기본값을 두면 한쪽으로 목록이 거짓말한다. 임시로 닫힌 장애는 `needsFollowUp`으로 남고 영구 조치가 적히면 풀린다. **근본 원인 없이 재발 방지를 적을 수 없다** — 원인을 모르는 채 적은 재발 방지는 다음 장애 때 조사를 건너뛰게 만든다. 빈 값이 이미 적힌 조사 결과를 덮어 쓰지 않는다) + **운영 KPI**(정책 3801-③ — `kpi.ts` 신규, `GET /ops/kpi`, `/admin/kpi` 화면. 판정은 이미 일곱 군데에 있었고 문제는 **사람이 일곱 군데를 돌지 않는다**는 것이었다. KPI가 거짓말하는 두 방식을 막는다: **표본이 없는데 숫자가 나오는 것**(복구된 장애가 없을 때 MTTR 0분은 "우리는 매우 빠르다"로 읽힌다 → `null`·"낼 수 없음")과 **좋아 보이는 0**("장애 0건"은 아무도 적지 않았다는 뜻일 수도, "경보 0건"은 감시가 멈춰 있다는 뜻일 수도 → 지표마다 `caveat`). **`unknown`을 좋음으로 세지 않는다** — 요약은 모르는 지표가 몇 개인지 먼저 말한다) + **운영 감사 기록**(정책 3801-④ — `audit.ts`·`ops_audit_log` 신규, 가로채기로 `/ops/*` **변경 요청 전부**. 서비스마다 부르게 하면 다음 엔드포인트에서 누군가 빠뜨리고 **빠진 감사 기록은 실패하지 않는다**. **실패한 시도도 남긴다** — 장애 조사에서 가장 자주 필요한 문장이 "그 시각에 누가 무엇을 눌렀는가"이고 거절된 시도는 그 자체가 신호다. 조회는 남기지 않고(소음이 진짜 변경을 묻는다), 본문은 **값이 아니라 이름만** 남긴다(감사 기록이 자격 증명이 새는 곳이 되면 안 된다 — 다만 등급·구성 요소 같은 짧은 열거값은 남긴다)) + 라이브 결함 2건 수정(경보 지표 문구가 값과 어긋남 · `POST /ops/incidents`가 `incident.run`으로 이름 붙음) + 정책 3801-⑤(실 Provider 활성화)는 **수행하지 못했다** — 활성화 0/3 조건이고 `api.openai.com`이 프록시에 막혀 있다 (마이그레이션 46 · `20260814000000_ops_governance`)

## 완료 — Sprint 37

- [x] **TASK-3701 — Enterprise Production Activation Platform**: CTO 정책 3701-①~⑤ 반영. 주제는 **본 것만 세고, 안 본 것은 안 본 것으로 둔다**. **활성화 이력**(정책 3701-① — `activation-history.ts` 신규, `activation_events` 표, `GET /ops/activation/history`. `judgeActivation`은 **지금**에 답하지만 전환은 몇 주에 걸쳐 조건이 하나씩 채워지는 과정이고, 사람이 알고 싶은 것은 "언제부터 이 상태인가 · 되던 것이 되돌아간 적이 있나"다. `/ops/activation`은 화면을 열 때마다 불리므로 매번 적으면 같은 문장 수천 줄이 되고 그 안에서 **변화가 보이지 않는다** — 그래서 **상태 지문이 바뀔 때만** 새 줄을 만들고 같은 상태면 `lastSeenAt`·`observations`만 갱신한다. 그 `lastSeenAt`이 **"3주째 그대로"와 "3주 동안 아무도 안 봤다"를 가른다**: 마지막 관측 이후 구간(`unobservedMs`)은 "유지됐다"가 아니라 "모른다"이고 화면이 그렇게 말한다. 기록이 없으면 `activated`는 `false`가 아니라 **`null`**이다) + **운영 스모크**(정책 3701-② — `smoke.ts` 신규, `smoke_runs` 표, `POST /ops/smoke`·`GET /ops/smoke`. 지금까지의 판정은 전부 간접 증거였다(키 형식·주소·TCP 도달·DB 성공 기록). 그 넷을 다 통과하고도 **결제 정지·모델 접근 거부·버킷 쓰기 권한 없음**은 보이지 않고, 그런 것은 **불러야만** 보인다. LLM·OCR·S3를 가장 작은 호출로 한 번씩 부른다(토큰 8개 완성 · 1x1 PNG · 수십 바이트 오브젝트 쓰기·읽기·삭제). **성공이 곧 통과가 아니다** — 공식 주소를 상대로 한 성공만 `passed`이고 스텁 상대 성공은 `stubbed`(통과 아님)다. S3는 **읽은 것이 쓴 것과 같아야** 성공이다(200과 정합은 다르다). 실 호출은 돈이 나가므로 사람이 누를 때만 돌고 누가 눌렀는지 남는다(결정 1301-①). 예약 `provider-smoke`도 **같은 검사**를 돈다 — 이름이 같은 검사가 둘이면 더 느슨한 쪽이 근거로 쓰인다) + **활성화 대시보드**(정책 3701-③ — `/admin/activation` 신규. 지금·과정·증거·장애를 한 화면에. 이 화면이 하지 않는 일은 **좋아 보이게 만드는 것**이다: 스텁 성공은 초록이 아니라 호박색이고, 되돌아간 조건은 붉게 적히며, 아무도 안 본 구간은 그 사실이 적힌다) + **장애 이력**(정책 3701-④ — `incident.ts` 신규, `incidents` 표, `GET/POST /ops/incidents`·`POST /ops/incidents/:id/resolve`. **경보와 장애는 다르다** — 경보는 자동으로 뜨는 신호이고 장애는 사람이 선언한 사건이다. 경보에서 장애를 자동으로 뽑으려는 시도는 늘 둘 중 하나로 끝난다(아무것도 아닌 것이 장애가 되거나, 조용히 지나간 진짜 장애가 빠지거나). **진행 중인 장애의 지속 시간은 최종값이 아니라 "지금까지"** 이므로 MTTR은 복구된 것만으로 내고, 복구된 것이 하나도 없으면 **0분이 아니라 null**이다. 감지 시간과 복구 시간을 나눠 센다 — 늦게 안 것과 늦게 고친 것은 고칠 곳이 다르다. **복구 방법 없이는 닫히지 않는다**) + 정책 3701-⑤(실 Provider 활성화)는 **수행하지 못했다** — 자격 증명·네트워크·전환 판정 세 조건이 모두 미충족이고, 라이브에서 `api.openai.com`이 `HTTP 403 Host not in allowlist`로 막혀 있다. 보고서에 "준비됨"이 아니라 그 사실을 그대로 적는다 (마이그레이션 45 · `20260813000000_activation_history`, 커밋 대기)

## 완료 — Sprint 36

- [x] **TASK-3601 — Enterprise Production Activation Platform**: CTO 정책 3601-①~④ 반영. 주제는 **완료는 셋이 모두 될 때만 완료다**. **운영 활성화 세 조건**(정책 3601-① — `activation.ts` 신규, `GET /ops/activation`. 전환 판정 하나만으로는 **무엇이 남았는지** 말하지 못한다: 키가 없는 것과 길이 막힌 것과 판정이 빨간 것은 사람이 할 일이 전혀 다르고, 하나로 뭉친 초록불은 둘이 충족된 상태를 "거의 다"로 보이게 만든다. `자격 증명`(형식만 본다 — 유효성은 실제 호출이 알고 그것은 `cutover` 조건이 본다. mock·키 없음·플레이스홀더·`minioadmin`을 잡는다) · `네트워크`(막힘은 물론 **403(가릴 수 없음)도 충족이 아니고**, **점검하지 않았으면 충족이 아니다** — 빈 목록은 "닿는다"가 아니라 "모른다"다) · `전환 판정`(`/ops/cutover`가 전부 verified인가) 셋이며 **하나라도 아니면 완료가 아니다**. `pnpm cutover`가 두 겹으로 출력한다) + **요청 추적**(정책 3601-② — `RequestContextService`(AsyncLocalStorage)+미들웨어 신규, `executions`·`ocr_results`에 `requestId`·`traceId`. 한 번의 사용자 요청이 LLM·OCR을 여러 번 부르는데 그 호출들을 묶는 끈이 없으면 "이 요청이 얼마를 썼는가"·"이 실패가 그 요청의 것인가"에 답할 수 없고 장애 때 로그와 기록을 손으로 맞춰 보게 된다. **인자로 넘기지 않는 이유**: 호출 지점이 몇 겹 아래라 매개변수를 하나라도 빠뜨리면 그 경로만 조용히 추적이 끊긴다. 앞단이 준 `x-request-id`는 **그대로 쓰고**(우리가 새로 만들면 앞단 로그와 이어지지 않는다), `traceparent`가 있으면 그 추적에 붙으며, 응답에도 실어 보낸다(신고받은 값 하나로 기록을 찾을 수 있어야 한다). 모르면 null — 지어내지 않는다) + **공지 어댑터 구조**(정책 3601-③ — 형식별 파서를 `PRICE_SOURCE_PARSERS` 한 곳에 모아 **형식이 늘어도 판정은 늘지 않게** 했다. 파서가 하는 일은 "줄의 목록으로 펴는 것"뿐이고 유효성·실패 처리는 바깥 한 곳에서만 정한다. `html`·`rss`·`jsonfeed`는 `PLANNED_PRICE_SOURCE_FORMATS`로 두어 **"아직 어댑터가 없습니다"** 로 거부한다 — **"알 수 없는 형식"과 다른 문구다**: 오타를 의심할 일과 어댑터를 요청할 일은 다르다) + **CI Job 분리**(정책 3601-④ — `quality-gates`(정적 게이트+Core/API)와 `web-e2e`(브라우저 필요) 두 Job. 한 Job이 전부를 돌면 e2e가 늘 때마다 모든 게이트의 답이 늦어지고 **게이트가 느려지면 사람은 게이트를 안 기다린다**. `pnpm test:unit`·`pnpm test:e2e` 신설, 게이트 자체 검증도 새 구성을 반영) + 화면(`/admin/production`에 활성화 세 조건 카드) — core(`activation.ts` 신규, `price-source.ts` 파서 레지스트리·예정 형식, `execution.ts` 추적 필드·해석기, `ci-workflow.ts` 게이트 재정의)+api(`RequestContextService`·`CommonModule` 신규, `GET /ops/activation`, OCR 저장소·LLM 서비스 추적 주입) 분리, **마이그레이션 1건**(`20260812000000_execution_trace` — nullable 컬럼 4개와 인덱스 2개, Major Migration 아님), 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Vision 스텁 + 공지 스텁 4곳** — 응답 헤더에 `x-request-id`가 실리고 **앞단이 준 값(`gateway-live-3601`)을 그대로** 쓰는 것 확인 · OCR 1회가 `requestId`·`traceId`를 실제로 DB에 남김 · 활성화 3조건이 각각 "아직"으로 갈리고 무엇이 없는지 이름으로 말함 · `html` 형식이 **"아직 어댑터가 없습니다"** 로 거부됨 · `pnpm cutover` 종료 코드 1과 두 겹 출력)

## 완료 — Sprint 35

- [x] **TASK-3501 — Enterprise Production Cutover Platform**: CTO 지시 1~6 반영. 주제는 **키가 틀린 것과 길이 막힌 것은 다르다**. **CTO_REQUEST #67 승인 사항**(지시 1 — 지시 2·3이 "전환하라"는 답이지만 **자격 증명 자체는 도착하지 않았다**. #67 ②(공지 발효 시각 자동 예약)·③(최종 승인자 자격)은 여전히 미결이라 구현하지 않고 #68에 다시 올린다. #67 ④(CI에서 Live Verification 범위)는 지시 4가 "품질 게이트 자동 검증"으로 답했다고 읽고 **게이트 자체 검증**을 넣되 Live Verification은 CI에 넣지 않았다 — 실 자격 증명이 필요하고 매 푸시마다 과금된다) + **도달 점검**(지시 2·3 — `EgressService`+`judgeProductionCutover`의 `unreachable`. 자격 증명 이전의 조건이다: 키를 넣어도 방화벽·프록시가 공식 주소를 막고 있으면 전환은 되지 않고, 그때 나오는 오류는 **키 문제처럼 보인다**. 판정은 셋이다 — `reachable`(401·404 등 응답이 왔다: 우리가 보는 것은 길이지 권한이 아니다) · `blocked`(연결 자체가 안 됐다 → `unreachable`) · **`ambiguous`**(403 — Provider의 거절인지 중간 프록시인지 **가릴 수 없다**). 자격 증명은 보내지 않는다(도달만 보면 되고 키를 보내면 과금될 수 있다). 점검하지 않았으면 "닿는다"고도 "막혔다"고도 말하지 않는다) + **`pnpm cutover`**(전환 판정을 명령 하나로 — 0 전환 완료 / 1 미완 / **2 판정 불가**. 전환을 수행하지 않고 확인만 하며 통과시키는 우회로는 두지 않았다) + **게이트가 게이트를 검증한다**(지시 4 — `scripts/check-ci-gates.mjs`+`REQUIRED_CI_GATES`에 `ci-gates` 추가. 워크플로에서 게이트 한 줄이 사라져도 CI는 초록으로 끝난다: **없어진 검사는 실패하지 않기 때문이다.** TASK-3401에서 본 실패의 다른 얼굴이다 — 그때는 적혀 있는데 돌지 않았고, 이번에 막는 것은 적혀 있지도 않게 되는 것이다. 안정화: 같은 브랜치의 앞선 실행 취소·작업 시간 상한 20분·권한 `contents: read`) + **공지 어댑터 확장**(지시 5 — `csv` 형식 추가(첫 줄이 머리글인 표. **빈 칸을 0으로 읽지 않는다** — 0은 "무료"라는 뜻이 되어 버린다) + **소스가 책임지는 단가 선언**(`PRICE_SOURCE_KEYS_<PROVIDER>`). 소스를 나눈 대가로 생긴 빈 곳을 메운다: 공지 하나가 죽으면 그 공지가 알려 주던 단가는 그냥 사라지고, 어느 공지가 죽었는지는 알아도 "그래서 어떤 단가를 확인하지 못했는가"에 답할 수 없었다. 선언하지 않으면 빈 목록이다 — 모르는 것을 아는 척하지 않는다) + **운영 UX**(지시 6 — 전환 화면이 **지금 할 일 하나**를 크게 보여 준다(넷을 나란히 두면 사람은 "그래서 뭐부터?"에서 멈춘다) · 도달 점검을 **세 상태로 갈라** 보여 준다 · 공지 표에 책임 단가와 확인하지 못한 단가) — core(`production-cutover.ts`: `EgressProbe`·`EgressStatus`·`unreachable`, `price-source.ts`: csv 어댑터·책임 키·`unverifiedKeys`, `ci-workflow.ts`: `ci-gates` 게이트)+api(`EgressService` 신규, `ProductionCutoverService`에 도달 점검 편입, `PriceSourceService`가 책임 키 전달)+scripts(`check-ci-gates.mjs`·`cutover.mjs` 신규) 분리, **마이그레이션 없음**, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Vision 스텁 + 공지 스텁 3곳(acos·flat·**csv**) + GitHub Actions 스텁** — 세 형식을 각각 읽고 소스별 책임 단가 표시 · openai 공지가 죽자 **확인하지 못한 단가 `gpt-4o, gpt-4o-mini`를 이름으로** 보고 · csv 공지의 단가 변경과 발효 시각 감지 · `pnpm cutover` 종료 코드 1과 남은 항목·다음 할 일 출력 · **실제로 `api.openai.com`이 프록시에 막혀 있음을 발견**), **자체 발견 결함 3건 수정**(① **403을 "닿음"으로 세어 LLM을 `verified`로 판정했다** — 그 403은 Provider가 아니라 **프록시**가 막은 것이었고, 판정은 성공 기록 7건(과거 라이브 검증이 심은 행)과 합쳐 "전환 완료"라고 말할 뻔했다. 세 번째 상태 `ambiguous`를 만들어 **모르는 것을 닿는다고 말하지 않게** 고쳤다 ② 그 판정 문구가 점검 문구의 "가릴 수 없습니다"를 **두 번** 적었다(TASK-3401에서 고친 겹침의 재발) ③ 화면이 403을 "닿지 못함"으로 표시했다 — 모르는 것을 막혔다고 말하면 사람은 방화벽을 뒤지다 시간을 버린다. "가릴 수 없음"으로 갈랐다), 해석 확인 CTO_REQUEST #68
- [x] **TASK-3501 이어서 — 승인된 정책 3501-①~⑤ 반영**: **① 전환은 운영·Staging에서**(`judgeProductionCutover`가 환경을 받아 `applicable`을 판정한다. 개발에서 상시 빨간색을 띄우면 사람은 그 빨간색을 무시하게 되고 **정작 운영에서 떴을 때도 무시한다**. 판정 자체는 감추지 않고 참고용이라는 사실만 덧붙이며, `pnpm cutover`도 개발에서는 종료 코드 0으로 끝낸다) + **② 공지 발효 시각이 예약 기본값**(적용 시 `effectiveFrom`을 비우면 공지가 밝힌 시각으로 예약한다. 사람이 공지를 다시 읽어 옮겨 적는 일은 옮겨 적기 실수를 부르고 그 실수는 "언제부터 이 단가인가"를 틀리게 만든다. **운영자가 넣은 값이 이기고**, **과거 시각은 기본값이어도 거부**한다(정책 3201-③에 예외를 두지 않는다). 화면은 그 시각을 미리 알려 준다) + **③ 최종 승인자는 다른 ADMIN 유지**(변경 없음 — 결정을 문서와 테스트로 고정) + **④ 호출 대상 기록**(`executions`·`ocr_results`에 `endpoint`·`baseUrl`·`calledAt` 추가. **호출하는 그 자리에서** 남긴다 — 나중에 환경변수를 다시 읽어 추정하면 그 사이에 설정이 바뀐 경우 과거를 잘못 설명하게 되고, 그 설명이 전환 판정의 근거가 된다. `resolveCallTarget`은 core 순수 함수이고 어댑터가 주입한다) + **⑤ verified는 실제 호출 대상을 근거로**(`baseUrl`이 공식 주소인 성공만 센다. 옛 기록의 `null`은 "공식이었다"가 아니라 **"모른다"** 이며, 성공이 있는데 공식이 없으면 그 사실을 그대로 말한다 — 이것이 TASK-3501 자체 발견 결함 ①이 드러낸 구멍을 닫는 조각이다) — **마이그레이션 1건**(`20260811000000_execution_call_target` — nullable 컬럼 6개와 인덱스 2개, Major Migration 아님), 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Vision 스텁 + 공지 스텁 3곳** — OCR 1회 실행이 `endpoint`·`baseUrl`·`calledAt`을 실제로 남기는 것을 DB에서 확인 · Vision 주소를 공식으로 되돌리자 판정이 **"최근 성공한 OCR 23건이 있지만 공식 주소로 만들어진 것은 없습니다"** 로 바뀜(23건 전부 스텁 상대이거나 옛 기록) · `NODE_ENV`에 따라 `applicable`이 갈림 · 공지 발효 시각이 예약 기본값으로 들어가고 운영자 값이 우선)

## 완료 — Sprint 34

- [x] **TASK-3401 — Enterprise Production Readiness Platform**: CTO 지시 1~7 반영. 주제는 **스텁을 진짜로 세지 않는다**. **CTO_REQUEST #66 승인 사항**(지시 1 — 결정 3301-⑤ Provider별 공지 어댑터, 결정 3301-⑥ 실패 장기화 시 등급 승격. 질문 ②(공지 발효 시각 자동 예약)·③(최종 승인자 자격)은 결정이 내려지지 않아 **구현하지 않고** CTO_REQUEST #67에 다시 올린다 — 답이 없는 것을 승인으로 읽지 않는다) + **Provider별 공지 어댑터**(지시 2, 결정 3301-⑤ — `PRICE_SOURCE_URL_<PROVIDER>`·`PRICE_SOURCE_FORMAT_<PROVIDER>`(`acos`|`flat`)·`PRICE_SOURCE_TOKEN_<PROVIDER>`. 한 주소가 모든 단가를 담으면 그 주소 하나가 죽을 때 **아무 단가도 대조하지 못한다** — OpenAI 공지가 안 열린다고 Google 단가까지 확인을 못 하는 것은 사실과 맞지 않다. 소스마다 따로 읽고 따로 판정하되 **전체 상태는 가장 나쁜 것을 따른다**: 셋 중 둘을 읽었다고 "정상"이라 하면 못 읽은 하나가 다시 사라진다(`partial`을 따로 둔 것과 같은 이유, 단위가 줄에서 소스로 바뀌었을 뿐이다). **모르는 형식은 짐작해서 읽지 않고** 그 소스를 읽지 않았다고 말한다 — 잘못 읽은 단가는 못 읽은 단가보다 위험하다. 프로젝트별 공지도 정책 3301-④와 같은 이유로 거부한다. 제안 근거에 **어느 공지가 알렸는지**(`sourceId`)가 남고, 실행 이력도 소스별로 남는다) + **공지 실패 장기화 승격**(지시 3, 결정 3301-⑥ — 기본 24시간(`PRICE_SOURCE_ESCALATE_AFTER_MS`) 초과 시 `warning → critical`, 문구에 몇 시간째인지가 붙는다. 같은 warning이 반복되면 사람은 "원래 그런 경보"로 읽고 그 순간 경보는 있는데 아무도 보지 않는 상태가 된다. **미구성은 승격하지 않는다** — 아직 안 붙인 것은 시간이 지나도 실패가 되지 않고, 승격하면 "붙이지 않기로 한" 선택이 며칠 뒤 장애로 둔갑한다. 시작 시각은 경보의 `firstRaisedAt`(처음 본 때)이 아니라 **실행 이력에서 이어진 실패의 시작**으로 센다 — 지난달에 한 번 실패하고 나았던 공지가 오늘 다시 실패했다고 곧바로 critical이 되면 안 된다. 경보 키도 소스별이라 한 곳이 나아도 다른 곳이 뒤에 숨지 않는다) + **실 Vision·LLM 운영 검증**(지시 4 — `production-cutover.ts` 신규. `provider-rollout`(TASK-2901)은 "성공 기록이 있으면 연결됨"으로 판정하는데, 그 기록이 **계약 스텁을 상대로** 만들어졌으면 화면은 붙지 않은 시스템을 붙었다고 보고한다. 그래서 **상대가 누구였는지**를 본다: 엔드포인트가 공식 호스트인지(`OPENAI_BASE_URL`·`ANTHROPIC_BASE_URL`은 우리 코드가 안 읽어도 **공식 SDK가 읽는** 창구다), `mock`·`tesseract`인지. 판정은 `verified`/`not-production`(돌고는 있지만 운영의 그것이 아님)/`unverified`/`not-configured`/`invalid` 다섯이고 **하나라도 verified가 아니면 ready는 false** — 운영 전환에 부분 점수는 없다. 연결 순서 판정도 같은 기준을 따르게 고쳤다(두 화면이 서로 다른 말을 하면 안 된다)) + **Amazon S3 운영 전환**(지시 5 — 어중간한 전환을 잡는다: 주소는 Amazon인데 자격 증명이 비었거나 `minioadmin`이면 `invalid`(그 상태의 인증 실패는 "S3 장애"처럼 보인다), 이미지 버킷과 백업 버킷이 같으면 `invalid`(결정 1701-②), 접근 점검 결과가 없으면 `unverified`. `S3_ENDPOINT` 미설정은 **미구성이며 실패가 아니다**) + **GitHub Actions 운영 검증**(지시 6 — `ci-workflow.ts` 신규. 워크플로 **파일**과 최근 **실행 결과**를 따로 판정한다: 적혀 있는 것과 초록으로 끝나는 것은 다른 사실이다. **이 저장소의 CI는 13회 실행이 전부 실패하고 있었다** — `pnpm test`에 web e2e가 들어 있는데 러너에 Playwright 브라우저가 없어 테스트 게이트가 통째로 죽었고, 우리는 그동안 로컬 결과로 "전부 통과"를 보고해 왔다. 브라우저 설치 단계와 **없던 TypeScript 게이트**를 추가하고 Node 22로 올렸다. 실행 이력이 없으면 `unknown`이다 — "실패한 적이 없다"와 "한 번도 안 돌았다"를 같은 초록으로 보여 주면 아무도 CI를 켜지 않은 저장소가 가장 건강해 보인다) + **문서·체크리스트**(지시 7 — `docs/operations/production-cutover.md` 신설, 배포 체크리스트에 CI 게이트 순서와 전환 검증 절차, `s3-migration.md` §5.1 기계 판정, `real-provider-smoke.md`에 "스모크가 성공해도 연결됨이 아닐 수 있다", `ai-cost-governance.md` Provider별 공지·승격) + 화면(`/admin/production` **운영 전환 검증** 섹션 — 항목별 상태·판정 근거·다음 할 일, `/admin/costs` **가격 공지 소스** 표와 거부된 설정. Playwright 7종 → 웹 e2e 166) — core 순수 로직(`production-cutover.ts`·`ci-workflow.ts` 신규, `price-source.ts`: 소스 해석·`flat` 어댑터·소스 합산, `alerts.ts`: 소스별 키·승격 판정, `provider-rollout.ts`: 엔드포인트 출처)+api 어댑터(`ProductionCutoverService`·`CiStatusService` 신규, `PriceSourceService.fetchAll`, `PricingService.priceSourceFailingSince`, `GET /ops/cutover`) 분리, **마이그레이션 없음**, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Vision 계약 스텁 + 가격 공지 계약 스텁 2곳(형식 다름) + GitHub Actions 계약 스텁** — 두 공지를 각각 읽고 `flat` 형식도 같은 결과로 판정 · `csv` 형식과 프로젝트별 공지를 **사유와 함께 거부** · openai 공지를 죽이자 전체가 `unreachable`이 되고 **google에서 읽은 단가는 그대로 대조** · 소스별 실행 이력 기록 · 3시간째 못 읽는 상태에서 **WARNING → CRITICAL 승격**("3시간째") · 중간에 한 번 읽힌 이력이 있으면 승격하지 않음 · `/ops/cutover`가 네 항목 모두를 `not-production`/`invalid`로 판정하고 `ready: false` · **`/ops/providers`가 "OCR 연결됨 22건"이라 말하던 것이 전부 우리 스텁 상대였음이 드러남** · CI 이력을 초록/빈 이력으로 바꾸면 `verified`/`unverified`로 갈림), **자체 발견 결함 2건 수정**(① 공지 실패 경보 문구에 "사람이 직접 확인해 주세요"가 **두 번** 들어갔다 — 판정 문구가 이미 요구하고 있는데 경보가 또 붙였다. 같은 문장이 겹치면 경보가 기계가 쓴 글로 읽히고 그때부터 사람은 제목만 본다 ② 공지 소스 표의 열이 붙어 읽히지 않았다 — 라이브 스크린샷에서 드러나 열 간격을 주었다), 해석 확인 CTO_REQUEST #67

## 완료 — Sprint 33

- [x] **TASK-3301 — Enterprise Provider Intelligence Platform**: CTO 정책 3301-①~④ 반영. 주제는 **못 읽은 것을 괜찮다고 말하지 않는다**. **외부 가격 공지**(정책 3301-① — `PRICE_SOURCE_URL`의 JSON 가격표를 읽어 우리 표와 대조한다. 이것이 "Provider가 단가를 올렸다"를 실제로 알 수 있는 유일한 경로다(TASK-3201의 기록 대조는 우리 계산의 불일치까지만 본다). 공지 기반 제안은 출처가 `published`이고 근거에 **공지 주소와 공지가 밝힌 발효 시각**이 남는다. **같은 항목에 두 근거가 잡히면 공지가 남는다** — 공지는 Provider가 밝힌 값이고 기록 불일치는 그 결과일 뿐이라, 순서를 반대로 두면 강한 근거가 약한 근거에 밀린다) + **파싱 실패는 "변경 없음"이 아니다**(조용히 지나가면 화면은 "변경 없음"으로 보이고 사람은 확인했다고 믿고 단가는 낡은 채로 돈이 나간다. `unreachable`·`unparsable`·`partial`을 가르고 **일부만 읽어도 경고**하며 못 읽은 항목을 목록으로 남긴다 — 아홉을 읽었다고 성공이라 하면 못 읽은 하나가 사라진다. `unconfigured`는 실패가 아니므로 제목이 다르다. 어댑터는 **재시도하지 않고**(예약 점검이 다시 부른다) 실패를 빈 목록으로 바꾸지 않으며 토큰을 기록에 남기지 않는다) + **2단계 승인**(정책 3301-② — `시스템 생성 → ADMIN 승인 → 다른 ADMIN 최종 승인 → 적용`. `CONFIRMED` 단계 신설, **운영에서만** 강제한다: 사람이 낸 제안은 제안자·승인자가 이미 둘이지만 시스템 제안은 승인자 한 명이 곧 전부다. `APPROVED → APPLIED`는 400이고 **최종 승인자는 1차 승인자와 달라야** 한다 — 같은 사람이 두 번 누르는 절차는 단계만 늘 뿐 확인을 늘리지 않고 오히려 "두 사람이 봤다"는 잘못된 안심을 만든다) + **예약 취소는 삭제가 아니다**(정책 3301-③ — `CANCELLED` 상태 신설. 적용은 실제로 있었던 일이고 그 결정을 지우면 "왜 그때 그 단가가 예약됐다가 사라졌는가"에 아무도 답할 수 없다. **발효 전 예약만** 취소할 수 있고(이미 발효된 단가는 새 제안으로 되돌린다) 사유가 필요하며, 취소해도 목록에 남고 실효 가격표 계산에서만 빠진다. 취소 시 캐시도 즉시 무효화한다 — 다음 발효 경계가 바뀐다) + **감지 주기는 Provider별만**(정책 3301-④ — 기본 6시간, `PRICE_DETECT_INTERVAL_<PROVIDER>`. **프로젝트별 설정은 거부하고 사유를 남긴다**: 단가는 Provider와의 계약이지 프로젝트의 속성이 아니고, 프로젝트마다 주기를 다르게 두면 "언제 마지막으로 봤는가"에 답이 여러 개가 된다. 조용히 무시하면 설정한 사람은 적용된 줄 안다. **수동 실행은 주기를 무시한다** — 사람이 지금 보라고 누른 것이다. `price_detection_runs` 신설로 **0건도 건너뛴 것도** 기록한다: 그래야 "감지 0건"과 "감지를 안 돌렸다"를 구분할 수 있다) + 화면(공지 실패 경고 배너·못 읽은 항목 목록 · 공지 출처 배지와 공지 근거 · 최종 승인 버튼과 2단계 안내 · 예약 취소 버튼 · Provider별 감지 현황과 거부된 설정. Playwright 7종 → 웹 e2e 158) — core 순수 로직(`price-source.ts` 신규, `pricing-governance.ts`: CONFIRMED·CANCELLED·published 출처·`judgeSecondApproval`·`judgeScheduleCancel`·전이 맥락, `price-detection.ts`: Provider별 주기 해석, `alerts.ts`: `price-source` 경보)+api 어댑터(`PriceSourceService` 신규, `PricingService.detect` 공지 대조·주기·실행 이력, `/ops/pricing/:id/{confirm,cancel}`) 분리, **마이그레이션 1건**(`20260810000000_provider_intelligence` — 컬럼 5개 + `price_detection_runs` 테이블, Major Migration 아님), `docs/operations/ai-cost-governance.md` §1-2~§1-6 확장, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Vision 계약 스텁 + 가격 공지 계약 스텁** — 공지 정상/형식 붕괴/HTTP 503/타임아웃/일부 해석을 각각 `ok`·`unparsable`·`unreachable`·`unreachable`·`partial`로 판정하고 전부 사람 확인을 요구 · 공지가 새 단가를 알리자 `published` 제안 생성(근거에 공지 주소·발효 시각) · 운영에서 1차 승인 후 적용 **400**, 같은 사람의 최종 승인 **400**, 다른 ADMIN의 최종 승인 후 적용 성공 · 예약 취소 사유 없음 400·취소 후 `CANCELLED`로 남고 예약만 사라짐·되살리기 400·이미 발효된 단가 취소 400 · Provider별 주기(google-vision 12h·나머지 6h)와 프로젝트별 설정 거부·예약 점검 2회차 주기 미도래·건너뛴 실행도 기록), **자체 발견 결함 3건 수정**(① 같은 항목에 두 근거가 잡힐 때 **약한 근거(기록)가 강한 근거(공지)를 밀어냈다** — 처리 순서를 바꿔 공지를 남긴다 ② 공지 기반 제안의 근거에는 `sampleIds`가 없는데 화면이 표본 기반 모양으로 읽어 **페이지 전체가 깨졌다** — DTO를 출처별 선택 필드 합집합으로 바로잡고 화면을 갈랐다(라이브에서 발견) ③ 공지 제안의 제안자가 다시 "제안 알 수 없음"으로 보였다 — 출처가 늘어도 같은 규칙이 적용되도록 `manual`만 예외로 두었다), 해석 확인 CTO_REQUEST #66

## 완료 — Sprint 32

- [x] **TASK-3201 — Enterprise Pricing Automation Platform**: CTO 정책 3201-①~⑤ 반영. 주제는 **자동화는 찾아내고, 사람이 정한다**. **감지 → 승인 → 적용**(정책 3201-① — `DETECTED` 단계·`origin`·`evidence` 신설. 예약 점검 `pricing-detect`(6시간)와 `POST /ops/pricing/detect`가 기록과 가격표를 대조해 제안을 만들고 **근거**(표본 수·기간·표본 id·차이)를 붙인다. 감지가 검토를 대신하지만 `DETECTED → APPLIED`가 **없어서** 승인·적용은 언제나 사람이 한다 — 자동 적용을 허용하면 일시적 이상이나 계산 오류가 곧바로 돈의 기준을 바꾸고 그 뒤의 모든 숫자가 설명 불가능해진다. 같은 항목에 진행 중인 제안이 있으면 **또 만들지 않는다**(6시간마다 같은 제안이 쌓이면 승인할 것이 무엇인지 흐려진다). 감지는 `pricing-drift` 경보로 나가되 **차단하지 않는다**) + **감지할 수 없는 것을 감지한다고 말하지 않는다**(Provider 가격 공지는 읽을 수 없다 — 비용은 우리가 우리 가격표로 계산하므로 기록만으로는 "Provider가 올렸다"를 알 방법이 원리적으로 없다. 감지하는 것은 **기록과 지금 가격표의 불일치**이고 원인은 낡은 인스턴스·절차 우회·가격표 오류 셋이다. **발효 시각 이후 기록만** 보고(적용 직전 기록이 옛 단가인 것은 당연하다 — 그것을 세면 적용할 때마다 되돌리는 제안이 생긴다), **최근 표본이 한 값으로 모일 때만** 판단하고(섞여 있으면 평균은 존재하지 않는 값을 가리킨다), **LLM은 입력·출력 단가를 가를 수 없어 숫자를 만들지 않고** 어긋남만 알린다) + **운영 자기 승인 차단**(정책 3201-② — `judgeSelfApproval({environment})`가 운영에서는 400으로 막고 "다른 ADMIN 계정으로 승인해 주세요"를 말하며, 개발에서는 허용하고 "운영에서는 차단됩니다"를 남긴다. 환경은 **인자로 받는다** — core가 `process.env`를 읽으면 같은 판정이 테스트에서 재현되지 않는다. 감지된 제안은 제안자가 시스템(`proposedBy: null`)이라 통과하되 **사람의 확인이 1회**라는 사실을 밝힌다. 운영에는 ADMIN 계정이 최소 둘 필요하다 — 의도된 결과다) + **Effective From**(정책 3201-③ — **적용은 결정이고 발효는 시각이다.** `effectiveFrom` 신설, `resolvePricingAt`이 `appliedAt` 대신 발효 시각으로 판정하고 호출부는 반드시 지금 시각을 넘긴다. 예약분은 그 시각까지 어떤 계산에도 쓰이지 않고 화면·응답이 **따로** 보여 준다(섞으면 이미 쓰이는 것처럼 읽힌다). **과거 시점은 거부**(이미 기록된 비용을 소급해 다시 해석하게 되고 맞았던 기록이 불일치로 바뀐다 — 정책 3101-②가 무의미해진다) · 최대 365일 · 시계 오차 60초 허용 · 해석 불가한 값은 **즉시 적용으로 바꾸지 않는다**) + **Forecast는 Alert만**(정책 3201-④ — 예약 점검 `cost-forecast`(하루 1회 06:00 로컬)가 `cost-forecast:monthly` 경보를 내고 문구가 "이 값으로 호출을 차단하지 않습니다"를 직접 말한다. 표본 부족이면 경보하지 않는다. 예측 경보 경로에 `assertWithinBudget`이 **없다는 사실을 소스 검사로 고정**) + **적용 즉시 캐시 무효화**(정책 3201-⑤ — `PricingCacheBus`(Redis pub/sub `acos:pricing:invalidate`). 적용한 인스턴스는 그 자리에서, 나머지는 신호로 버린다. 캐시 만료는 **수명(30초)과 다음 발효 시각 중 이른 쪽** — 예약을 지나쳐 캐시하면 발효 순간이 조용히 늦어지고 그 사이 기록된 비용을 아무도 설명할 수 없다. `REDIS_URL`이 없으면 단일 인스턴스 모드이며 프로세스 내 무효화가 곧 전체 무효화다 — **미구성은 실패가 아니다**) + 화면(감지됨·자동 감지 배지 · 근거 표시 · 지금 감지 버튼 · 발효 시각 입력 · 예약된 변경 섹션 · 예측 "경보만" 문구. Playwright 6종 → 웹 e2e 152) — core 순수 로직(`price-detection.ts` 신규, `pricing-governance.ts`: DETECTED·origin·judgeSelfApproval·validateEffectiveFrom·nextPricingChangeAt, `alerts.ts`: pricing-drift·cost-forecast 경보, `schedule.ts`: 점검 2종)+api 어댑터(`PricingCacheBus` 신규, `PricingService.detect`, `/ops/pricing/detect`, 예약 점검 2종) 분리, **마이그레이션 1건**(`20260809000000_pricing_automation` — 컬럼 3개 추가 + 인덱스, 기존 적용 기록은 `appliedAt`을 발효 시각으로 백필, Major Migration 아님), `docs/operations/ai-cost-governance.md` §1-2~§1-5 신설, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Vision 계약 스텁 + API 2개 인스턴스** — 운영에서 자기 승인 **400** 차단 후 다른 ADMIN이 승인 · 과거 시점 400 · 365일 초과 400 · 20초 뒤 발효 예약이 **두 인스턴스 모두에서** 그때까지 옛 단가($0.002)를 쓰고 발효 후 $0.0025로 바뀜 · **인스턴스 B의 캐시를 채운 뒤 A에서 적용하니 B가 즉시 새 단가($0.003)를 반환**하고 B 로그에 "가격표 캐시를 버렸습니다" 기록 · 낡은 인스턴스 상태를 재현한 기록 5건을 감지해 `DETECTED` 제안 생성(근거 표본 5건·차이 33.3%) · 감지된 제안은 검토 400·적용 400이고 승인 후에만 적용 · 두 번째 감지는 **건너뜀** · 적용 후 다시 감지하니 어긋남 0건으로 **경보 해소** · 예측 경보가 떠 있는 상태에서 LLM 200·OCR 201), **자체 발견 결함 2건 수정**(① 감지가 발효 시각을 무시하면 **적용할 때마다 "옛 단가로 되돌리자"는 제안**이 생긴다는 것을 설계 검토에서 발견해 `inForceFrom` 창을 도입 ② 화면이 감지된 제안의 제안자를 "제안 알 수 없음"으로 표시했다 — 모르는 것과 사람이 아닌 것은 다르다. 라이브 스크린샷에서 드러나 "시스템 (자동 감지)"로 갈랐다), 해석 확인 CTO_REQUEST #65

## 완료 — Sprint 31

- [x] **TASK-3101 — Enterprise AI Cost Intelligence Platform**: CTO 정책 3101-①~④ 반영. 주제는 **단가는 절차를 거쳐 바뀌고, 기록은 손대지 않는다**. **가격표 거버넌스**(정책 3101-① — `pricing_proposals` 신설. 단계 전이는 core 순수 판정(`judgePricingTransition`)이 결정하고 어댑터는 **누가 언제 했는지**만 기록한다. 검토 없이 승인·승인 없이 적용 **불가**, **적용·반려는 끝**(되돌리려면 새 제안 — 끝난 것을 되살리면 "무엇이 언제 적용됐는가"의 이력이 흐려진다), 사유는 제안·반려 모두 **필수**(사유가 없으면 이력이 있어도 판단을 복원할 수 없다), **0은 허용·음수는 거부**(무료 모델은 실제로 있고, 음수 단가는 지출을 줄여 예산 상한을 무력화한다), **적용만 계산을 바꾼다**(승인은 "적용해도 된다"는 뜻이고 적용 시각이 있어야 "언제부터 이 단가로 계산됐나"에 답할 수 있다), 제안자=승인자는 **경고만**(막으면 사람은 코드를 고쳐 우회하고 이력이 아예 없어진다 — 결정 2601-① 교훈)) + **단가의 단일 출처**(`ExecutionTracker`가 가격표를 **함수로** 받고(`LlmPricingSource`) `PrismaOcrRunStore`도 같은 출처를 쓴다 — 경로마다 따로 읽으면 한쪽이 조용히 낡고 같은 호출의 비용이 화면마다 달라진다. **조회 실패 시 기준 가격표로 계산**한다: 비용을 `null`로 남기면 예산 계산에서 빠져 상한이 무력해지고, 그것이 조회 실패보다 위험하다) + **Append Only**(정책 3101-② — 단가가 바뀌어도 과거 `cost`는 그대로이고 검증은 **그 시점에 유효했던 단가**로 대조한다(`resolvePricingAt`). 새 단가로 과거를 대조하면 단가를 한 번 바꿀 때마다 과거 전체가 "불일치"로 보고되고, **무시되는 경보는 없는 경보보다 나쁘다**. 비용 컬럼을 갱신하는 코드가 없다는 사실을 소스 검사로 못질(`pricing/append-only.spec.ts`), 적용 이력은 **한 번만 읽어** 시점별 해석 함수를 만든다) + **Forecast는 참고자료**(정책 3101-③ — 하루 평균 × 이번 달 일수(UTC). 관측 **3일 미만이면 숫자를 만들지 않고** 최소 일수는 **고정값**(환경변수로 열면 "일단 1일로 낮추는" 우회가 생긴다). **예산 관문이 이 모듈을 모른다**는 사실을 소스 검사로 고정 — 예측으로 막으면 아직 쓰지 않은 돈 때문에 서비스가 멈추고 "왜 막혔나"에 답할 수도 없다. 미산정이 있으면 **추정도 실제보다 작을 수 있다**고 함께 말한다) + **Billing Report는 운영 지표**(정책 3101-④ — 응답·화면에 **항상** 면책 문구와 **차이의 이유 넷**(미산정·실패 호출·무료 구간·환율)을 적는다: 이유가 없으면 "우리 숫자가 틀렸다"로 읽히고, 숫자만 있으면 누군가 청구서로 다룬다. **비용 큰 것부터** 정렬(리포트를 여는 이유가 그것이다), 총액 0이면 비중은 `null`(0%로 적으면 "안 썼다"로 읽힌다)) + 화면(관리자 **AI 비용 관리** `/admin/costs` 신설 — 실효 가격표의 **출처** · 제안 등록·단계 진행 버튼 · 예측 **참고자료** 배지 · 리포트 **운영 지표** 배지와 면책 문구. Playwright 7종 → 웹 e2e 146) — core 순수 로직(`pricing-governance.ts` 신규, `cost-forecast.ts` 신규, `billing-report.ts` 신규, `cost-verification.ts` 시점별 해석, `execution.ts` 가격표 함수 주입)+api 어댑터(`PricingModule`/`PricingService` 신규, `CostIntelligenceService` 신규, `/ops/pricing`·`/ops/billing`·`/ops/cost-forecast`) 분리, **마이그레이션 1건**(`20260808000000_pricing_proposals` — 신규 테이블만, Major Migration 아님), `docs/operations/ai-cost-governance.md` 신설, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Google Cloud Vision 계약 스텁** — 검토 없이 승인·승인 없이 적용이 각각 400으로 막히고 사유가 순서를 밝힘 · 승인만으로는 실효 단가가 안 바뀌고 **적용 직후 OCR 호출이 $0.002**로 기록 · **적용 이전 기록 6건은 $0.0015 그대로**이고 비용 검증에 **불일치 0건**(시점별 대조 동작) · LLM도 같은 경로로 확인(적용 후 Execution $0.015, 어제 기록은 0 그대로) · 적용 후 반려 400·반려 후 검토 400 · 예측이 **월 예산의 391.6%**를 예상하는 상태에서도 LLM 호출 **200**(실제 지출 50.5%) — 정책 3101-③이 실제로 지켜짐 · 미인증 401·EDITOR 403), **자체 발견 결함 1건 수정**(실효 가격표에서 **LLM 단가의 출처가 비어 있었다**(`—`) — 승인된 단가와 코드 기본값이 구분되지 않으면 "이 금액은 누가 정했나"에 답할 수 없고, 그 질문이 이 TASK의 존재 이유다. 라이브 스크린샷에서 드러나 OCR과 같은 모양으로 맞췄다), 해석 확인 CTO_REQUEST #64

## 완료 — Sprint 30

- [x] **TASK-3001 — Enterprise AI Production Operations Platform**: CTO 결정 2901-①~④ 반영. 주제는 **AI 지출을 하나로 보고 OCR도 같은 눈으로 관측하는 것**. **판정 정의 3건을 테스트로 고정**(결정 2901-① 완료 판정은 `connected` 하나 — `done === (status === "connected")`를 모든 단계에서 확인하고, 설정만으로 완료가 되는 길(`assumeConnected`·`forceConnected`)이 **없음**을 소스 검사로 못질. 결정 2901-② OCR 운영 표준은 `google-vision`이고 개발용 엔진은 성공 50건이어도 `done: false`. 결정 2901-③ 근거 기한 30일 고정 + 문구가 기한을 밝힘 — 판정의 정의가 느슨해지면 대시보드가 조용히 거짓이 된다) + **OCR 비용 편입**(결정 2901-④ — `ocr_results.cost`·`units` 신설, `DEFAULT_OCR_PRICING`(google-vision 1,000단위당 $1.50) 중앙 정의. **무료 구간은 반영하지 않는다**: 빼면 예산이 낙관적으로 보이고 여러 환경이 한 계정을 공유하면 남은 무료 구간을 우리가 알 수 없다 — 모르는 것을 유리하게 가정하지 않는다. 표에 없는 엔진은 `null`(미산정)이고 0("과금 없음")과 다르다 — 0으로 채우면 예산 상한이 조용히 무력해진다. **실패한 호출에는 비용을 적지 않는다**: 과금 여부를 알 수 없고 모르는 것을 숫자로 적으면 예산이 거짓이 된다) + **예산은 AI 지출 총액**(`combineAiSpend`로 두 원장(LLM Execution·OCR 실행 이력)을 합쳐 일·월 예산과 비교. **원장을 합치지 않고 둘로 유지한 이유**: OCR은 LLM 호출이 아니어서 같은 표에 넣으면 토큰·모델·Failover·장애 판정이 뒤섞인다. 지출은 하나로 보고 원장은 성격대로 나눈다. 초과 시 OCR도 **호출 전에** 429로 막고(하지 않은 일을 기록하지 않는다) **무엇이 막혔는지** 문구가 말한다 — "LLM 예산"이라고만 하면 OCR을 눌렀다 429를 받은 사람이 엉뚱한 곳을 본다) + **비용 검증·관측 편입**(`verifyOcrCosts`가 LLM과 **같은 결과 모양**을 돌려주고 `/llm/cost-verification`이 둘을 합산하되 `bySource`로 원장별을 밝힌다. `monitorOcr`은 LLM과 **같은 판정 함수**(`monitorProduction`)를 쓰고 `/llm/monitoring/ocr`로 노출. 장애는 **같은 종류**(`provider-failure`)로 경보하며 health-check가 LLM·OCR을 **한 번에 동기화**한다 — 같은 종류를 두 번 부르면 뒤 호출이 앞의 경보를 해소한다(TASK-2801에서 배운 형태)) + 환경·화면(관리자 Provider 운영 점검에 **OCR 관측** 섹션·비용 원장별 표시, OCR 실행 응답에 `cost`·`units` 노출 — 비용을 감추지 않는다. Playwright 3종 → 웹 e2e 139) — core 순수 로직(`ocr-pricing.ts` 신규, `ai-spend.ts` 신규, `cost-verification.ts`: verifyOcrCosts·`unrecorded` 종류, `ocr-provider.ts` nullable 비용)+api 어댑터(`PrismaOcrRunStore` 비용 기록, `LlmBudgetService` 두 원장 합산, `ProviderProductionService.monitorOcr`) 분리, **마이그레이션 1건**(`20260807000000_ocr_cost` — nullable 컬럼 + 기간 인덱스, Major Migration 아님), `docs/operations/provider-rollout.md` §4 신설, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Google Cloud Vision 계약 스텁** — OCR 실행마다 $0.0015 기록·응답 노출 · 일 예산 $0.004에서 **OCR이 429로 차단**되고 문구가 "예산은 LLM과 OCR 지출을 합해서 봅니다"를 밝힘 · 예산 현황 `bySource.daily.ocr` 반영 · 비용 검증이 9건을 합산하고 `unpriced`(clova)·`unrecorded`(비용 편입 이전 2건)를 **가려서** 보고 · 미산정 엔진이 `unpriced-model:clova:text-detection` 경보로 나감 · OCR 관측이 degraded(성공률 66.7%·p50 49ms)로 판정), **자체 발견 결함 1건 수정**(`null` 비용을 0으로 보고 **"기록 $0 vs 기대 $0.003" 불일치**로 보고했다 — 기록이 없는 것과 0이 기록된 것은 다르고, 그 문구는 "기록된 값이 다르다"는 뜻이 되어 사실과 어긋난다. 라이브 검증에서 드러나 `unrecorded` 종류를 신설해 LLM·OCR 양쪽에서 갈랐다), 해석 확인 CTO_REQUEST #63

## 완료 — Sprint 29

- [x] **TASK-2901 — Enterprise AI Provider Production Platform**: CTO 결정 2801-①~⑤ 반영. 주제는 **실 Provider를 순서대로 붙이고 붙었는지를 사실로 확인하는 것**. **유지 결정 3건을 테스트로 고정**(결정 2801-①② 전체·프로젝트별 Alert 둘 다 유지 — 하나를 없애면 ⓐ 어디가 나빠졌는지 모르거나 ⓑ 전체 규모를 알 수 없다. 결정 2801-③ 신규 위반 표본 10건 고정 — `NEW_VIOLATION_SAMPLE_LIMIT`이 10이고 core에 `process.env`가 없음을 소스 검사로 못질. 유지 결정은 코드에 흔적이 없어서 나중에 누가 "정리"하며 없애도 아무도 모른다) + **작성자 직접 알림 미구현**(결정 2801-④ — 경보에 수신자 개념이 없고 `author`·`recipient` 같은 흔적이 없음을 고정) + **연결 순서를 값으로**(결정 2801-⑤ — `PROVIDER_ROLLOUT_ORDER = [openai, anthropic, gemini, vision, ocr]`, `judgeProviderRollout`이 단계별 상태와 **다음에 붙일 단계**를 판정. `GET /ops/providers`(ADMIN) + 관리자 화면. **`unverified`를 `connected`로 세지 않는다** — 키 형식은 오타를 잡을 뿐이고 그것을 연결 완료로 세면 붙지 않은 시스템이 붙은 것처럼 보고된다. `connected`의 근거는 **성공한 실행 기록**이고 기한은 최근 30일 고정 — 2년 전 성공으로 "지금도 붙어 있다"고 말할 수 없다. 순서 이탈은 `outOfOrder`로 **말하되 막지 않는다**) + **OCR 운영 표준 엔진 신규**(`GoogleVisionOcrProvider` — `images:annotate` TEXT_DETECTION. **신뢰도를 지어내지 않는다**: Google이 주지 않으면 `null`(1.0은 거짓, 0은 실패처럼 읽힌다) — `OcrRecognition.confidence`를 `number | null`로 넓혔고 평균 계산이 이미 null을 제외한다. **오류를 뭉치지 않는다**: 400·401/403·429·5xx·JSON 아님이 각각 사람이 할 일을 말한다. **글자 없는 이미지는 실패가 아니다**(빈 텍스트로 성공) 그러나 형식이 다른 응답은 실패다(빈 텍스트로 넘기면 상품이 빈 재료로 조립된다). 키를 URL·로그·기록에 남기지 않고, 재시도는 실행 계층에만 둔다) + **조용한 mock 대체 제거(자체 발견 결함)**(`OCR_PROVIDER=google`(오타·미구현)이 경고 한 줄만 남기고 **가짜 OCR로 운영을 돌렸다** — 이미지에서 읽지도 않은 텍스트로 조립된 상품이 검수를 통과하는 가장 위험한 조용한 실패. 운영에서는 기동 차단(Fail Fast), 개발에서는 경고 후 mock 유지. `google-vision`인데 키가 없으면 **기동 시점에** 실패한다 — 첫 호출까지 기다리면 그때는 이미 이미지가 올라간 뒤다) + **환경 검증에 OCR·Vision 엔진 편입**(`OCR_PROVIDER`·`GOOGLE_VISION_API_KEY`·`GOOGLE_VISION_ENDPOINT`·`OCR_LANGUAGE_HINTS` 등록. 그전에는 OCR 엔진 선택이 **어느 화면에도 없어** 운영에서 가짜가 돌아도 알 수 없었다. 운영 mock은 경고(차단하지 않는다), 엔드포인트 변경은 **키가 그 주소로 전송된다는 사실**을 경고, `VISION_PROVIDER`는 "더 이상 읽지 않음"으로 등록해 조용히 무시하지 않는다) — core 순수 로직(`provider-rollout.ts` 신규, `api-key.ts` google-vision 규칙, `ocr-provider.ts` nullable 신뢰도, `env-spec.ts`)+api 어댑터(`GoogleVisionOcrProvider`, `createOcrProvider` Fail Fast, `ProviderProductionService.rollout`) 분리, **마이그레이션 없음**, `docs/operations/provider-rollout.md` 신규(단계별 연결 방법 + **확인한 것과 확인하지 못한 것**), 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + Google Cloud Vision 계약 스텁** — 알 수 없는 엔진·키 없음에서 **기동 차단 2종** · 실 HTTP 경로로 OCR 성공(provider `google-vision`·신뢰도 0.94·요청에 base64 70바이트+TEXT_DETECTION+언어 힌트) · 오류 5종(403·429·503·JSON 아님·빈 결과)이 각각 다른 문구와 판정 · 키가 `rawJson`·로그에 **0건** · `/ops/providers`가 네 상태를 실제로 구분(연결됨+근거 / 확인 안 됨(형식 OK·실패 이력만) / 미구성 / 가짜) · 순서 이탈을 막지 않고 표시), **미확인 사항을 문서와 보고서에 명시**(실 Google·실 LLM 키 호출은 이 저장소에서 확인 불가 — 그래서 `unverified`로 남는다), 해석 확인 CTO_REQUEST #62

## 완료 — Sprint 28

- [x] **TASK-2801 — Enterprise Governance Intelligence Platform**: CTO 결정 2701-①~⑤ 반영. 주제는 **경보가 어디를 봐야 할지 말해 주는 것** — TASK-2701이 "늘었으면 부른다"를 만들었고, 이번에는 그 부름이 **어느 프로젝트에서, 무엇이 새로 위반됐는지**까지 담게 만든다. **되살리기 권한은 EDITOR 유지**(결정 2701-① — ADMIN으로 올리지 않았다. 콘텐츠를 고치는 사람이 내린 것을 되살릴 수 없으면 사람들은 새 콘텐츠를 만들어 우회하고, 그러면 왜 내렸는지가 끊긴다. 되살리기를 **별도 API로 분리하지도 않았다** — 분리하면 권한·감사 이력이 두 갈래가 된다. `RequireRole("EDITOR")`이고 ADMIN·`revive`·`unarchive`가 **없음**을 소스 검사로 고정) + **마지막 발행 시각**(결정 2701-② — `publishedAt`은 **최초** 발행 시각으로 그대로 두고 `lastPublishedAt`을 신설. 재발행이 공식 절차가 된 뒤(결정 2601-①) 최초 시각 하나로는 **"지금 언제부터 나가 있는가"**에 답할 수 없다. 판정은 core 순수 함수 `resolvePublishTimestamps` 하나에서만 한다 — 두 곳에서 정하면 한쪽이 최초 시각을 덮어쓰는 순간을 아무도 모른다. **`null`은 모르는 것**이다(이 값을 두기 전에 발행된 콘텐츠) — 화면이 "재발행 없음"이라 하지 않고 "마지막 발행 시각 미기록"이라 말한다. 백필은 `publishedAt` 복사가 아니라 **감사 이력에서 실제 마지막 PUBLISHED 전이를 되살린다** — 복사하면 이미 재발행된 콘텐츠가 "한 번만 발행"으로 보인다) + **프로젝트별 예약 스캔**(결정 2701-③ — 예약 실행이 프로젝트를 하나씩 훑고 **전체 범위는 그 합으로** 만든다. 따로 한 번 더 훑으면 콘텐츠를 두 번 읽고 **읽은 시점이 달라 전체와 프로젝트별 숫자가 어긋날 수 있다**. 합으로 만들면 어긋날 수가 없다. 프로젝트가 없어도 전체 실행은 남긴다 — 기록이 없으면 "돌지 않았다"와 구분되지 않는다) + **프로젝트별 Alert와 독립 Cooldown**(결정 2701-④ — 범위마다 기준선·경보 키가 따로다. 뭉치면 ⓐ 한 프로젝트가 나아지고 다른 프로젝트가 나빠질 때 총량이 그대로여서 **아무 경보도 오지 않고** ⓑ 프로젝트 A의 스캔이 **B의 경보를 해소해 버린다.** 재알림 간격은 이미 키 단위(`notifiedAt`)이고, 여기에 `reconcileAlerts(resolvableKeys)`를 더해 **해소 범위도 이번에 판정한 범위로** 좁혔다) + **경보에 증가 수와 새로 위반된 콘텐츠**(결정 2701-⑤ — `diffViolations`로 지난 실행의 위반 목록과 대조해 최대 10건을 제목·id와 함께 담는다. **증가 수와 새로 위반된 수는 다를 수 있다**(2건이 생기고 1건이 해소되면 총량은 1건 증가) — 한 숫자로 뭉치면 문구가 거짓이 되므로 둘을 따로 말한다. 10건이 넘으면 생략 건수를 밝힌다. `newlyCount`의 **`null`은 "가릴 수 없었다"**이고 0("새로 생긴 것 없음")과 다르다 — 섞으면 목록을 두기 전의 위반이 전부 "새로 생겼다"로 보고된다) + 웹 콘텐츠 목록에 재발행 표기·스캔 이력에 신규 위반 수(`신규 판별 불가`를 별도 문구로 — Playwright 2종 → 웹 e2e 133) — core 순수 로직(`publish-timestamps.ts` 신규, `scan-scope.ts` 신규, `scan-alert.ts`: diffViolations/describeNewViolations, `alerts.ts`: resolvableKeys)+api 어댑터(`GovernanceScanService.runScheduled` 프로젝트별+합, `GovernancePreflightService.items`, `AlertService.sync(options)`) 분리, **마이그레이션 1건**(`20260806000000_governance_intelligence` — nullable 컬럼 추가 + 감사 이력 기반 백필. 순수 추가라 Major Migration 아님), `docs/operations/content-governance.md` §4.2 범위·경보 내용·§5.1 권한·시각 2종·§6 경계 2행 추가, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 예약 1회로 **프로젝트 6개 + 전체** 기록되고 합이 정확히 일치(2+9=11) · 같은 결과 재실행은 경보 0·`newlyCount` 0 · **총량 11 그대로인데 한 프로젝트만 악화 → 그 프로젝트만 경보**(TASK-2701 설계로는 아무 경보도 없었을 상황)이고 문구에 증가 수 + 새로 위반된 콘텐츠 이름이 담김 · 그 위반을 고치자 **해당 키만 RESOLVED, 전체 범위의 기존 CRITICAL은 ACTIVE 유지**(거짓 해소 없음) · 12건 동시 위반에서 표본 10건 + "나머지 2건" 명시 · 재발행에서 `publishedAt` 유지·`lastPublishedAt` 갱신 · **EDITOR 계정으로 보관→되살림→검토→재발행 전 과정 성공** · 백필이 감사 이력에서 실제 재발행 시각(02:43 발행 → 03:34 재발행)을 되살림), 해석 확인 CTO_REQUEST #61

## 완료 — Sprint 27

- [x] **TASK-2701 — Enterprise Governance Automation Platform**: CTO 결정 2601-①~⑤ 반영. 주제는 **사람이 누르지 않아도 도는 거버넌스** — TASK-2601이 스캔 도구를 만들었고, 이번에는 그것을 **예약으로 돌리고 늘었을 때만 부르고 큰 프로젝트에서도 정확하게** 만든다. **이미 나간 위반의 공식 절차**(결정 2601-① — `PUBLISHED → ARCHIVED → 수정 → 재발행`으로 확정. 그런데 `ARCHIVED`가 종결 상태여서 **그 절차를 코드가 막고 있었다** — `ARCHIVED → DRAFT` 되살리기를 열었다. 절차를 정해 두고 코드가 막으면 사람은 새 콘텐츠를 만들어 우회하고, 그러면 **왜 내렸는지가 새 콘텐츠에 남지 않는다.** `ARCHIVED → PUBLISHED` 직행은 열지 않았다 — 되살린 것은 `REVIEW`를 거쳐야 하고 그래야 **게이트가 다시 돈다**. 400 문구가 그 경로를 안내한다. 별도 상태(`SUSPENDED`)는 만들지 않았고, 상태 목록 자체를 테스트로 고정했다(주석이 아니라 값을 본다)) + **예약 스캔**(결정 2601-② — `governance-scan` 작업, 매일 03:50 로컬. 보관 정리(04:00)보다 **앞에** 둔다: 스캔이 남긴 기록을 보관이 곧바로 치우면 방금 만든 것을 못 본다. 위반이 있는 것은 시스템 장애가 아니므로 점검 자체는 `ok: true`다 — `ok: false`로 두면 예약 점검 실패로 읽혀 엉뚱한 곳을 보게 된다) + **늘었을 때만 부른다**(결정 2601-③ — 첫 스캔은 **기준선**(규칙을 처음 켠 직후 전량을 경보로 만들면 아무 뜻이 없다) · 늘면 경보(증가분과 무엇이 늘었는지) · 같으면 조용 · 줄면 조용(기존 경보 유지) · 0이 되면 해소. 이미 나간 위반이 섞이면 **심각**. 비교 기준은 **범위별**(`all`/`project:<id>`)이고, **경보를 만들지 않은 실행도 기록**한다 — "돌았지만 조용했다"와 "돌지 않았다"는 다르다. `governance_scan_runs` 신규 테이블) + **페이지**(결정 2601-④ — `?offset`·`?limit`은 **목록만** 자르고 **`summary`는 항상 전체 기준**이다. 판정은 전량을 하되 내부에서 **커서**로 200건씩 나눠 읽는다(`skip`은 읽는 도중 새 콘텐츠가 생기면 한 건을 두 번 세거나 건너뛴다). 잘못된 `offset`은 400 — 조용히 0으로 되돌리면 **다른 페이지를 보고 있는 줄도 모른다**) + **규칙 캐시는 Request Scope만**(결정 2601-⑤ — `GovernanceRulesService.snapshot()`으로 스캔 한 번에 한 번만 읽는다(500건 스캔이면 조회 500회가 1회로 줄고, 한 스캔 안에서 규칙은 바뀌지 않으므로 단일 원천을 흐리지 않는다). **전역 캐시는 두지 않는다** — 규칙을 고친 운영자가 언제 반영되는지 알 수 없게 되면 규칙을 신뢰할 수 없다. core 판정 계층에 `cache`·`ttl`이 없고 스캔 계층에 모듈 수준 `Map`·`globalThis`가 없음을 소스 검사로 고정) + 웹 프로젝트 상세에 페이지 이동·예약 스캔 이력(경보 여부를 **글자로** — 조용한 것과 안 돈 것은 다르다. Playwright 6종 → 웹 e2e 131) — core 순수 로직(`preflight.ts`: 페이지, `scan-alert.ts`: compareScan/detectGovernanceScanAlert/shouldSyncScanAlerts/describeScanRun, `content-status.ts`: ARCHIVED → DRAFT, `alerts.ts`·`schedule.ts`: 새 종류·작업)+api 어댑터(`GovernanceScanService`, `GovernancePreflightService` 전량 판정, `GovernanceRulesService.snapshot`) 분리, **마이그레이션 1건**(`20260805000000_governance_scan_runs` — 순수 추가라 Major Migration 아님), `docs/operations/content-governance.md` §4.2·§5.1 추가, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 기준선(알림 0·경보 테이블 0건) → 같음(알림 0·**경보 여전히 0건**) → 늘림(알림 1·CRITICAL 생성) → 줄임(알림 0·경보 ACTIVE 유지, 거짓 해소 없음) · 페이지 4구간에서 `summary`가 모두 동일(blocked 10·published 1·scanned 32)하고 6페이지를 이어 붙여 **11건 수집·중복 0건** · 잘못된 offset 3종 400 · 공식 절차 6단계(내림→직행 시도 400+경로 안내→되살림→수정→검토→재발행 200) 전부 확인), **자체 발견 결함 1건 수정**(경보 저장소 동기화를 매 실행마다 불러 **늘지 않은 실행에서 경보가 새로 생겼다** — 결정 ③ 정면 위반이고 라이브 2단계에서 드러났다. `shouldSyncScanAlerts`를 도입해 **늘었을 때와 0이 됐을 때만** 동기화하도록 고쳤다. 그 사이에는 저장소를 건드리지 않으므로 거짓 해소도 나지 않는다. 회귀 테스트 3건 + 라이브 재검증), 해석 확인 CTO_REQUEST #60

## 완료 — Sprint 26

- [x] **TASK-2601 — Enterprise Governance Operations Platform**: CTO 결정 2501-①~⑤ 반영. 주제는 **켜 놓은 규칙을 운영하는 것** — TASK-2501이 발행 게이트를 켰고, 이번에는 그것을 **켜기 전에 세어 보고 켠 뒤에 정리하는** 도구를 붙인다. **Governance Preflight Scan**(결정 2501-① — 규칙을 처음 켜면 이미 `REVIEW`에 있던 콘텐츠들이 위반을 담고 있을 수 있고 그러면 **발행이 줄줄이 막힌다**. 하나씩 눌러 보며 발견하는 방식으로는 언제 끝날지 알 수 없으므로 켜기 전에 센다. **상태를 바꾸지 않고, 자동으로 고치지 않고, 판정 기록도 남기지 않는다** — 훑어본 것을 발행 시도로 기록하면 이력이 사실과 어긋난다. 화면에도 "한 번에 고치기" 버튼을 두지 않았다. 자동 수정을 만들지 않는 이유는 원격 사본 자동 복구를 만들지 않은 것(결정 2201-②)과 같다: 금지어를 기계가 지우면 문장이 무슨 뜻이 되는지 아무도 확인하지 않은 채 발행되고, 고지 문구를 기계가 덧붙이면 맥락과 무관한 자리에 붙는다. **쓰기 함수를 아예 부르지 않음을 테스트로 고정했다**) + **이미 나간 위반을 따로 센다**(`blocked`(발행 시 막힐 것)와 `publishedViolations`(**이미 발행됨**)를 나눈다 — 발행 게이트는 나가는 것을 막지만 **이미 나간 것에는 아무 힘이 없다**. 섞어 세면 "위반 5건"이 무슨 뜻인지 알 수 없다. 문구도 목록도 **이미 나간 것을 먼저** 말한다 — 막을 수 없는 것이 더 급하다. 기본 스캔 범위는 아직 나가지 않은 것(`DRAFT`·`REVIEW`)이고 `?status=PUBLISHED`로 이미 나간 것까지 본다) + **조용히 자르지 않는다**(`?limit`은 **목록만** 자르고 요약의 숫자는 자르기 전 전체다 — 2건만 보여 주며 "위반 2건"이라고 하면 실제 건수를 아무도 모른다. 잘랐다는 사실과 생략 건수를 문구에 적고, 판정 대상 상한(500건)을 넘으면 "나머지는 검사하지 않았습니다"까지 밝힌다. 잘못된 `status`·`limit`은 **400** — 조용히 기본값으로 되돌리면 운영자는 자기가 준 값이 적용됐다고 믿는다) + **판정 기록 90일 보관**(결정 2501-⑤ — 경보(1302-④)·Dead Letter(1501-③)와 같은 정책. **삭제하지 않는다.** 기준은 **작성 시각**이다 — 판정 기록에는 해소라는 개념이 없는 시점 기록이므로 경보와 기준이 다르고, 그래서 문구도 "해소 후"와 "작성 후"를 나누어 적는다. 예약 정리 작업(`alert-archive`)에 편입했다 — **새 작업을 만들지 않았다**: 같은 성격의 정리가 서로 다른 시각에 돌면 무엇이 남았는지 흐려진다. 이미 보관된 것은 다시 찍지 않는다(찍으면 언제 보관했는지가 바뀐다). 보관된 것도 `?includeArchived=1`로 볼 수 있다 — **볼 길이 없으면 보관이 사실상 삭제다**) + **하지 않기로 한 것을 테스트로 고정**(결정 2501-②③④ — `content-governance-boundary.spec.ts`: 금지어는 부분 일치이고 `최고`가 `최고급`에도 걸린다(단어 경계·예외 목록·정규식이 코드에 없음을 소스 검사로 고정 — 넓게 걸고 사람이 판단한다. 좁히면 놓치는 쪽이 위험하다) · READY 판정 모듈에 `throw`도 `publishable`도 `blocking`도 없다(**강제 차단은 `PUBLISHED`뿐**) · 판정 입력에 `scopeId`·`PROJECT` 같은 규칙 스코프가 없다 · Preflight에 `autoFix`류 출력이 없고 **왜 안 하는지가 주석으로 남아 있다**) + 웹 프로젝트 상세에 스캔 패널(이미 나간 위반은 붉은 안내로 맨 위에 · 색만이 아니라 **글자로** "이미 발행됨" · Playwright 5종 → 웹 e2e 125) — core 순수 로직(`preflight.ts`: summarizePreflight/describePreflight/isReleased + resolveGovernanceArchiveAfterDays/planGovernanceArchive/describeGovernanceArchive)+api 어댑터(`GovernancePreflightService`, `ContentGovernanceService.archive`, `GovernanceController`) 분리, **마이그레이션 1건**(`20260804000000_governance_archive` — `archivedAt` 컬럼 추가, 순수 추가라 Major Migration 아님), `docs/operations/content-governance.md` §3.1·§4.1·§6 추가, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 실제 3건을 스캔해 `blocked 1` · `publishedViolations 1` · `clean 1`을 확인하고 **스캔 후 상태·판정 기록이 모두 그대로** · `?limit=1`에서 요약은 전체(3건)이고 목록만 1건 · 잘못된 `status`·`limit` 400 · 전체 범위는 무인증 401 후 ADMIN으로 32건 스캔 · 100일 전 기록 1건이 예약 작업으로 **보관되고 행은 5건 그대로**(삭제 아님) · 재실행 시 0건 · 현황 이력 4건 / `includeArchived=1` 5건), 해석 확인 CTO_REQUEST #59

## 완료 — Sprint 25

- [x] **TASK-2501 — Enterprise Content Governance Platform**: 주제는 **발행되는 것을 검사하는 것** — Sprint 16~24는 인프라·운영 거버넌스였고, 이번에는 **제품이 세상에 내보내는 것**으로 같은 원칙을 가져온다. **여기서 큰 공백을 발견했다**: 금지어·규칙 검사는 Sprint 4(TASK-0404)부터 있었지만 **Product Object의 READY 전환에만** 걸려 있었고, 세상에 나가는 것은 Product Object가 아니라 **Content의 제목과 본문**인데 그 경로의 발행 조건은 `isPublishable`(REVIEW 상태 + 제목·본문 비어 있지 않음)뿐이었다 — **금지어가 든 본문도, 필수 고지가 빠진 본문도 아무 판정 없이 발행됐다.** 게다가 READY 판정 자체가 "검증 결과 미강제"였다. 상품을 검사하고 콘텐츠를 검사하지 않는 것은 **재료를 검사하고 완성품은 안 보는 것**과 같다. **발행 게이트**(`PUBLISHED` 전이에만 적용 — 검토를 요청하는 것과 세상에 내보내는 것은 다르므로 `REVIEW`·`ARCHIVED`는 막지 않는다. 판정은 **읽어 둔 레코드로** 한다 — 다시 읽으면 판정한 내용과 발행되는 내용이 다를 수 있다. 발행 형식 검증(`isPublishable`)이 거버넌스보다 먼저라, 빈 본문은 판정 기록조차 만들지 않는다) + **금지어는 제목과 본문 모두**(READY 판정은 상품의 제목·브랜드·OCR만 봤다. 스캔 규칙은 `scanBannedWords`를 **재사용**한다 — 상품에서는 걸리는 말이 콘텐츠에서는 안 걸리면 어느 쪽 판정도 믿을 수 없다. 메시지에 **어디에 몇 건**인지 적는다 — "금지어가 있습니다"만으로는 고칠 수 없다) + **필수 고지**(CTO_REQUEST #7의 "필수 고지" — `whenCategory`로 분류별 적용. 모든 상품에 모든 고지를 요구하면 본문이 고지문으로 덮이고 아무도 읽지 않는다. 공백·줄바꿈 차이는 무시한다(붙여 넣은 문구는 원문과 줄바꿈이 다르다). 분류를 모르면 전체 적용 규칙만 검사하고 **그 사실을 말한다** — 적용 0건과 통과는 다르다. `reason`을 함께 남긴다 — 문구만 있으면 나중에 아무도 못 지우고 왜 있는지도 설명할 수 없다) + **경보와 차단의 구분**(근거 상품 연결 끊김·관련 규칙 존재는 `주의`로 드러내되 막지 않는다. 막는 항목은 `blocking` 필드로 밝힌다 — 화면이 "주의인데 왜 막혔지"를 추측하지 않게. 색만으로 구분하지 않고 화면에 **글자로** "발행 차단"을 적는다) + **판정 기록**(`content_governance_checks` 신규 테이블, 삭제하지 않는다. **그때의 기준**(금지어 수·적용 고지 id)을 함께 남긴다 — 규칙은 나중에 바뀌므로 "왜 이게 발행됐지"에 답하려면 판정 당시 기준이 있어야 한다. **막힌 기록도** 남긴다 — 없으면 "왜 이렇게 늦게 발행됐지"에 답할 수 없다. `published`(실제 발행 여부)와 `status`(판정)를 따로 둔다 — 하나로 쓰면 기록이 사실과 어긋난다) + **규칙을 읽는 곳도 하나**(`GovernanceRulesService` — READY 판정과 발행 게이트가 같은 곳에서 읽는다. 규칙이 두 곳에 있으면 키가 달라지거나 형식 해석이 갈라진다. CTO 결정 2301-①과 같은 이유) + **미리보기와 게이트가 같은 판정 함수**(`GET …/governance` — 화면이 "발행 가능"이라 했는데 누르면 막히는 상태를 구조적으로 없앴다. 미리보기는 전이도 기록도 하지 않는다 — 보는 것과 시도하는 것은 다르다) + 웹 프로젝트 상세 확장(발행 버튼 **옆**에 판정 패널 — 눌러 보고 막히는 것과 무엇이 막는지 먼저 보는 것은 다르다. Playwright 5종 → 웹 e2e 120) — core 순수 로직(`content-governance.ts`: evaluateContentGovernance/containsDisclosure/applicableDisclosures/parseBannedWords/parseDisclosures)+api 어댑터(`ContentGovernanceService`·`GovernanceRulesService`, `ContentsService.updateStatus` 게이트) 분리, **마이그레이션 1건**(`20260803000000_content_governance` — 순수 추가라 Major Migration 아님), `docs/operations/content-governance.md` 신설, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 실제 Memory에 금지어·고지 규칙을 등록하고 금지어 든 본문의 발행이 **400으로 차단**되며 상태가 `REVIEW` 그대로 유지됨 · 금지어만 지우면 고지 누락으로 **여전히 차단** · 고지까지 넣으면 발행 성공 · **3건의 판정 기록**(막힘 2 + 통과 1)이 시간순으로 남아 지연을 설명 · 같은 금지어를 READY 판정과 발행 판정이 **함께 잡음** · 규칙 미설정에서는 `주의`로만 남고 발행됨 · 기준이 `0`이 아니라 `null`로 기록됨), **자체 발견 결함 1건 수정**(형식이 틀린 규칙에 "설정되지 않았습니다"라고 안내했다 — 등록은 되어 있는데 그렇게 말하면 운영자는 등록하러 갔다가 이미 있는 것을 발견한다. **상태와 설명이 모순되는** 그 결함 계열이다. `RuleSource`(`configured`/`missing`/`invalid`)를 도입해 안내를 분리, 회귀 테스트 6건 + 라이브 재검증), 해석 확인 CTO_REQUEST #58

## 완료 — Sprint 24

- [x] **TASK-2401 — Enterprise Operational Compliance Platform**: CTO 결정 2301-①~④ 반영. 주제는 **같은 사실을 두 곳이 따로 판정하지 않게 하는 것** — 지금까지 판정을 늘려 왔고(백업·복원·보호·사슬·원격·스키마), 판정이 늘면 **같은 것을 두 곳에서 재는 일**이 생긴다. 화면마다 답이 다르면 어느 쪽을 믿어야 할지 아무도 모른다. **복구 판정의 단일 원천**(결정 2301-① — `RecoveryEvaluationService`를 신설해 재해 복구 체크리스트 조립을 그 계층으로 내렸다. `/ops/readiness`와 `/health/ready`가 **같은 함수를 부른다**. 이전에는 배포 체크리스트가 순환 의존을 피하려고 **백업·복원 이력 개수**로 대신 판정했고, 그래서 이력이 있으면 "복구 가능"으로 읽혀 **한 화면은 복구 가능, 다른 화면은 복구 불가**가 될 수 있었다. 순환은 모듈 방향으로 풀었다 — Health가 Ops를 가져오고 그 반대는 없다. 확인하지 못하면 `null`이고 `직접 확인`으로 남는다. 스키마 상태도 같은 이유로 `MigrationGovernanceService` 한 곳에서만 읽는다) + **스키마가 코드보다 앞선 상태는 경보로**(결정 2301-② — `detectUnknownMigrationAlert`: 최초 `주의`, **7일 초과 `심각`**. 되돌린 배포는 흔하고 직후에 사람을 부를 일은 아니지만 **일주일이 지나도 그대로라면 되돌린 것이 아니라 잊은 것**이다. **배포는 막지 않는다** — 막으면 되돌린 배포를 다시 되돌릴 수 없다. 경보 문구에 그 사실을 적었다. 경과는 경보의 `firstRaisedAt`으로 센다 — 경보는 삭제하지 않으므로(결정 1302-④) 그 시각이 곧 "처음 본 때"이고, 관측 기록용 테이블을 새로 두지 않았다. 목록이 바뀌어도 키가 같다 — 키가 바뀌면 경과가 초기화된다) + **S3 전환 후 Versioning 조회 실패는 Fail**(결정 2301-③ — 배포 체크리스트 `operations` 입력에 `storageStandard`를 넣어 `unknownIsFailure = production && storageStandard`. 전환 전에는 `직접 확인`(개발 저장소는 조회를 지원하지 않는다), 전환 후에는 **실패·배포 차단**이다. S3는 조회를 지원하므로 못 읽는 것은 저장소의 한계가 아니라 **`s3:GetBucketVersioning` 권한 누락**이고, 문구가 그렇게 말한다. 같은 상태에 같은 판정을 주면 전환의 의미 절반이 사라진다) + **`pendingMigrations`는 실제 미적용 개수**(결정 2301-④ — 공식 의미를 문서와 코드에 못 박았다. 확인 불가는 `0`이 아니라 `null`이고 화면에는 `확인 불가`다. 여기서 **결함 하나를 더 발견해 고쳤다**: `judgeMigrations`가 실패 행이 있으면 `pending: []`로 **비워서** 돌려줬다 — 반쯤 적용된 스키마에서 `pendingMigrations`가 0으로 보고돼 다시 거짓 통과가 됐다. 무엇을 먼저 알릴지(실패 우선)와 무엇이 사실인지는 다르므로, 대조를 먼저 하고 실패를 먼저 알린다) + 웹 `/admin/health` e2e 3종 추가(확인 불가를 0으로 적지 않는지 · 전환 후 조회 실패가 막는지 · 복구 판정 확인 불가가 `직접 확인`인지 → 웹 e2e 115) — core 순수 로직(`migration-governance.ts`: detectUnknownMigrationAlert/UNKNOWN_MIGRATION_ESCALATE_MS + pending 대조 순서, `deployment-checklist.ts`: storageStandard 게이팅, `alerts.ts`: `migration-governance` 종류)+api 어댑터(`RecoveryEvaluationService`, `MigrationGovernanceService`, `AlertService.firstSeenAt`) 분리, **마이그레이션 없음**, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + 실 pg_dump/pg_restore** — 실제 복원 성공(32개 테이블)으로 **옛 대리 판정이라면 "복구 가능"이 될 상태**를 만든 뒤 두 화면이 **함께 복구 불가**임을 확인 · 코드에 없는 마이그레이션을 넣자 감시가 `WARNING` 생성 → `firstRaisedAt`을 8일 전으로 되돌리자 **`CRITICAL`·"8일째"**로 승격하되 **`migrations` 항목은 `주의`로 남아 배포를 막지 않음** · `s3:GetBucketVersioning`을 403으로 막는 프록시를 `*.amazonaws.com` 호스트로 붙이자 `versioning`이 **실패·배포 차단**, 같은 거부를 비-S3 호스트로 붙이면 **`직접 확인`** · 미적용 1건 + 실패 행 1건 상태에서 `pendingMigrations`가 **1로 유지**됨), **자체 발견 결함 1건 수정**(실패 행이 있으면 미적용 목록을 비우던 문제 — 위 ④. 회귀 테스트 1건 + 신규 테스트 25건), 해석 확인 CTO_REQUEST #57

## 완료 — Sprint 23

- [x] **TASK-2301 — Enterprise Deployment Governance**: CTO 결정 2201-①~④ 반영. 주제는 **경계를 문서가 아니라 판정으로 만드는 것** — "운영 담당자가 한다"고 적어 두는 것과, 하지 않았을 때 **배포가 막히는** 것은 다르다. TASK-2201이 경계를 그었고, 이번에는 그 경계를 **지키지 않으면 드러나게** 만든다. **스키마도 운영 담당자가**(결정 2201-① — 저장소·IAM에 이어 DB 스키마까지 같은 경계. `resolveProvisioningPolicy.mayApplyMigrations`는 **어느 환경에서도 false**다(개발에서도 기동이 스키마를 바꾸면 브랜치를 옮길 때마다 데이터가 조용히 달라진다). 검증만 한다는 것은 **검증이 실제로 작동해야 한다**는 뜻이라, 여기서 **기존 결함을 발견해 고쳤다**: `pendingMigrations`가 `_prisma_migrations`의 **실패 행만** 세고 있었다 — 적용하지 않은 마이그레이션은 실패 행조차 남기지 않으므로, 운영 담당자가 `migrate deploy`를 잊으면 **"미적용 마이그레이션 없음"이라는 거짓 통과**가 나왔다. 마이그레이션 디렉터리와 적용 기록을 **대조**하도록 바꿨고(`judgeMigrations`), 적용됐는데 코드에 없는 것은 `주의`로 알린다(되돌린 배포·브랜치 불일치 신호 — 조용히 지나가면 "왜 이 컬럼이 있지"를 나중에 아무도 설명할 수 없다). 운영에서는 제목에 **"운영 담당자 수행"**이 붙고 문구가 주체를 밝힌다) + **자동 복구를 만들지 않는다**(결정 2201-② — 원격 대조 실패는 Critical Alert만. 자동으로 다시 올리면 ⓐ 로컬 덤프가 온전한지 확인하지 않은 채 원격을 덮어써 **틀린 사본을 더 확실하게** 만들 수 있고 ⓑ 사라진 이유(수명 주기 정책·권한·삭제)를 모르는 채 올리면 같은 일이 반복된다. **경보 문구에 수동 복구임과 절차 위치를 적었다** — 무엇을 해야 하는지 적지 않은 경보는 읽어도 할 일이 없다. 대조 경로가 `putBackupObject`를 부르지 않음을 소스 검사로 고정, DR 문서에 4단계 복구 절차 추가(2번을 건너뛰고 3번만 하면 다음 주에 같은 경보가 온다)) + **우회 플래그를 두지 않는다**(결정 2201-③ — `STORAGE_PROVISIONING`을 추가하지 않고 정책은 `NODE_ENV`만 본다. **설정해도 무시됨**을 테스트로 고정했다 — 하지 않기로 한 일은 코드에 흔적이 없어서 나중에 누가 "편의를 위해" 되살려도 아무도 모른다) + **운영 표준 배포 체크리스트**(결정 2201-④ — `bucket`·`iam`·`versioning`·`backup-bucket`·`readiness` 다섯 항목을 배포 체크리스트로 승격. 운영에서 `bucket`·`backup-bucket`·`readiness`는 **배포 차단**이고 `versioning`은 꺼져 있을 때만 차단이다(조회 불가는 `직접 확인`). `iam`은 차단하지 않는다 — 보호 상태를 읽지 못해도 지금 서비스는 돈다. **복구 리허설은 여기에 들어오지 않는다**(결정 1801-② 회귀 테스트로 고정). `docs/operations/deployment-checklist.md`를 운영 표준 문서로 신설) + 웹 `/admin/health` 확장(스키마 적용 주체·미적용/미지 목록 패널, 승격 항목 자동 표시, Playwright 6종 → 웹 e2e 112) — core 순수 로직(`migration-governance.ts`: judgeMigrations, `deployment-checklist.ts`: 다섯 항목 + `operations` 입력, `storage-provisioning.ts`: mayApplyMigrations)+api 어댑터(`ReadinessService.migrationState`·`operationsState`·`recoverable`, `StorageService.bucketExists`) 분리, **마이그레이션 없음**, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 임시 마이그레이션을 만들어 적용하지 않자 **`pendingMigrations: 1` · 배포 차단** 확인 · `migrate deploy` 후 통과로 전환 · 디렉터리를 지우자 `주의`로 전환하며 이름 노출 · `STORAGE_PROVISIONING=managed`를 줘도 버킷이 생기지 않음 · s3rver의 Versioning 꺼짐이 운영에서 실제로 **배포를 차단**), **자체 발견 결함 1건 수정**(실패 행만 세는 마이그레이션 검증의 거짓 통과 — 위 ①. 회귀 테스트 9건 추가) + 문구 다듬기 2건(프로비저닝 정책 문구의 마크다운 제거 — 로그에 그대로 나가는 문자열이다 / 미적용 안내의 줄표 중복 제거), 해석 확인 CTO_REQUEST #56

## 완료 — Sprint 22

- [x] **TASK-2201 — Enterprise Production Readiness**: CTO 결정 2101-①~④ 반영. 주제는 **운영에 넘길 수 있는 상태** — 지금까지 시스템은 필요한 것을 스스로 준비해 왔다(버킷을 만들고, 정책을 걸고, 잘못된 설정을 조용히 고쳐서). 개발에서는 편하지만 **운영에서는 그것이 곧 책임의 실종**이다. 이번 TASK는 그 손을 놓고, 대신 **무엇이 필요한지 말하고 기다리는** 쪽으로 바꾼다. **수동 대조 최대 3건**(결정 2101-① — 자동(주 1회)은 1건 그대로다. 주기적으로 도는 것은 비용이 반복되므로 최소로 두고, 사람이 필요할 때 한 번 더 보는 쪽에만 폭을 준다. **상한을 두는 이유**는 "전부"를 허용하면 이력이 쌓인 뒤 한 번의 클릭이 예상치 못한 전송 비용이 되기 때문이다. `resolveRemoteVerifyCount`가 1~3으로 조이고 잘못 적은 값은 400으로 거절한다. **하나라도 실패하면 실패**다 — 3건 중 2건이 온전해도 잃은 1건은 그 시점으로 되돌아갈 수 없다는 뜻이라, 평균을 내면 그 사실이 사라진다. **건마다 결과를 남긴다** — 어느 백업이 온전한지가 복구 시점 선택의 근거다) + **관측 창 상향은 Warning**(결정 2101-② — 상향은 유지하되 체크리스트 항목 `chain-window`로 승격해 주의로 드러낸다. **기동은 막지 않는다**. 화면 한 줄로만 두면 아무도 고치지 않고, 기동을 막으면 판정 설정 하나 때문에 서비스가 뜨지 않는다 — 둘 사이에서 **드러내되 세우지는 않는** 자리를 골랐다. 여기서 **기존 결함 하나를 발견해 고쳤다**: `BACKUP_CHAIN_WINDOW_HOURS`에 `validate`가 걸려 있어 **해석 불가 값이면 Fail Fast로 기동이 막혔다** — 코드는 조용히 기본값으로 돌면서 환경 검증만 기동을 막는, 정반대로 어긋난 상태였다. `productionAdvice`(경고)로 낮추고, 무시한 사실을 `source: "invalid"`로 드러내 같은 Warning에 태웠다) + **CI 게이트 위치 고정**(결정 2101-③ — 현재 위치(Build 이후) 유지. 앞으로 옮기면 검증이 `@acos/core` 빌드 산출물을 못 찾아 **조용히 exit 2**가 되고 그것이 "판정 불가"로 취급된다. `ci-workflow.spec.ts`가 워크플로 파일을 읽어 순서를 고정한다 — 기록되지 않는 규칙은 지켜지지 않는다) + **버킷·IAM은 운영 담당자가**(결정 2101-④ — `resolveProvisioningPolicy`로 운영은 `external`, 그 외는 `managed`. **운영에서 애플리케이션은 버킷을 만들지도, 정책을 걸지도 않는다.** 이유 셋: ⓐ 오타 하나로 엉뚱한 이름의 버킷이 조용히 생기고 그 버킷은 백업 대상도 보호 정책 대상도 아니다 ⓑ 애플리케이션이 정책을 덮어쓰면 운영자가 콘솔에서 조인 권한이 다음 재기동에 풀린다 ⓒ 누가 무엇을 책임지는지가 흐려진다. 버킷이 없으면 `describeMissingBucket`이 **누가 무엇을 해야 하는지**까지 적는다 — "찾을 수 없습니다"로 끝내면 운영자는 앱이 만들어 주기를 기다린다. **운영에서 `putObject` 실패 문구도 고쳤다** — "MinIO가 실행 중인지 확인하세요"는 운영에서 거짓 안내이고 진짜 원인은 대개 버킷 미준비다. 백업 버킷에는 **어느 환경에서도** 공개 정책을 걸지 않는다) + 웹 `/admin/operations` 확장(1건·3건 검증 버튼 분리, `사슬 관측 창 설정` 체크리스트 항목, `버킷·권한 준비 주체` 칸, Playwright 7종 → 웹 e2e 106) — core 순수 로직(`backup-integrity.ts`: resolveRemoteVerifyCount/summarizeRemoteBatch/judgeChainWindow + `invalid` source, `storage-provisioning.ts`: resolveProvisioningPolicy/describeMissingBucket)+api 어댑터(`BackupService.verifyRemoteCopies`, `StorageService.provisioning`) 분리, **마이그레이션 없음**(환경·정책 판정만), `docs/operations/s3-migration.md` §2.1 추가, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 해석 불가 창 설정으로도 **기동됨**(권고만) · `chain-window` 항목이 `warn`이고 `recoverable`을 낮추지 않음 · 실 백업 3건을 내려받아 **서로 다른 SHA-256 3개 대조** 후 건마다 DB 기록 · `count=99` → 3건 · `count=all` → **400** · 운영에서 없는 버킷을 가리키자 **s3rver에 버킷이 생기지 않았고** 로그·체크리스트에 운영 담당자 안내가 그대로 노출 · 개발 모드에서는 같은 조건에서 버킷이 생성됨), **자체 발견 결함 2건 수정**(① 위의 `validate` Fail Fast 모순 ② **선언한 DTO가 실제 응답과 달랐다** — `RemoteVerifyResultDto.entries`를 `{fileName, verdict, detail}`로 적었는데 실제로는 `{fileName, result:{...}}`였다. 타입 수준의 "상태와 설명 모순"이라 DTO를 실제에 맞추고 컨트롤러 반환 타입으로 묶었다), 해석 확인 CTO_REQUEST #55

## 완료 — Sprint 21

- [x] **TASK-2101 — Enterprise Operational Automation**: CTO 결정 2001-①~⑤ 반영. 주제는 **사람이 기억하지 않아도 도는 것** — TASK-2001은 판정을 만들었고, 이번에는 그 판정이 **누가 누르지 않아도** 유지되게 한다. 규칙은 있는데 아무도 실행하지 않으면 규칙이 없는 것과 같다. **사슬 관측 창 설정**(결정 2001-① — `BACKUP_CHAIN_WINDOW_HOURS`, 기본 24시간. **최소는 백업 간격 × 4**이고 그보다 짧게 두면 자동으로 올린다. 창이 간격의 4배도 안 되면 창 안에 백업이 서너 개뿐이라 한 번만 걸러도 판정이 뒤집힌다 — 표본이 없는 판정은 판정이 아니라 잡음이다. **설정을 거절하고 판정을 멈추는 대신 판정할 수 있는 창으로 올리고, 올렸다는 사실을 화면에 적는다** — 조용히 다른 값으로 도는 것이 가장 나쁘다) + **원격 대조 주 1회 자동**(결정 2001-② — 예약 점검 `remote-verify` 신설. **마지막 백업 1건만** 내려받아 SHA-256을 대조한다. `PRODUCTION_ONLY_JOBS`로 **운영에서만** 기본 켜짐이며, 개발·테스트는 전송 비용을 낼 이유가 없고 개발 저장소에는 대조할 원격 사본도 없다. 다만 환경변수로 명시하면 스테이징에서도 켤 수 있다. **결과를 `backup_runs.remoteCheckedAt`/`remoteVerdict`에 남긴다** — 남기지 않으면 자동 대조를 돌려도 화면은 계속 "확인하지 않았습니다"라고 말한다. 화면·경보는 **기록을 읽는다**(조회마다 내려받으면 화면을 여는 것만으로 전송 비용이 생긴다). **주기의 2배를 넘긴 성공은 통과로 두지 않는다** — 예약이 두 번 걸렀다면 그때의 `ok`는 지금의 사실이 아니다. 반대로 **실패는 낡아도 실패다** — 시간이 지난다고 사본이 돌아오지 않는다) + **`_major_` 파일명 규칙 + CI 교차 검증**(결정 2001-③ — 매니페스트는 **잊기 쉽고**(파일을 만들고 목록에 넣는 것을 잊는다) 파일명은 **되돌리기 쉽다**(이름을 바꾸면 지정이 사라진다). 둘의 **합집합**을 major로 보고, `scripts/check-major-migrations.mjs`가 어긋남을 잡는다 — 한쪽에만 있으면 exit 1, 읽지 못하면 **exit 2**(판정 불가를 통과로 처리하지 않는다). 매니페스트에만 있고 규칙을 안 따르는 것은 오류가 아니다 — 이름을 소급해 바꾸면 `_prisma_migrations`와 어긋난다. `.github/workflows/ci.yml` 신설로 build 뒤에 돌린다) + **재기동 후 확인 절차**(결정 2001-④ — 기동 시 1회 정책을 유지하는 대신, `RecoveryDrillService.lastAutoRegistration()`이 결과를 남기고 readiness `enterprise.drill.autoRegistration`으로 노출한다. **`registered: null`은 "확인하지 못한 것"**이고 "등록할 것이 없었다"와 구분된다 — 실패를 성공처럼 적으면 Runbook의 확인 절차가 거짓말이 된다. Runbook에 3단계 확인 절차 추가) + **Amazon S3 운영 전환 계획**(결정 2001-⑤ — `docs/operations/s3-migration.md` 신설. 버킷 구성·IAM 최소 권한·이관·확인·되돌리기. **핵심은 저장소를 바꾸는 것이 아니라 `s3:GetBucketVersioning`·`s3:GetReplicationConfiguration` 조회 권한을 빠뜨리지 않는 것** — 권한이 없으면 전환해도 보호 상태가 `직접 확인`으로 남아 바꾼 의미의 절반이 사라진다) + 웹 `/admin/operations` 확장(관측 창 상향 고지·자동 대조 주기 표시·자동 등록 확인 칸, Playwright 8종 → 웹 e2e 99) — core 순수 로직(`backup-integrity.ts`: resolveChainWindowMs/judgeRecordedRemoteIntegrity, `major-migrations.ts`: isMajorMigrationName/crossCheckMajorMigrations, `schedule.ts`: PRODUCTION_ONLY_JOBS·`d` 단위)+api 어댑터(`BackupService.chainWindow`·`lastRemoteRecord`·`recordRemoteVerdict`, `ScheduledChecksService` remote-verify 실행) 분리, **마이그레이션 34**(backup_runs 원격 대조 기록), 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 창 2시간 설정이 4시간으로 상향되고 사유가 표시됨 · 운영에서 `remote-verify — 7일`로 예약 등록 · 실제 S3에서 내려받아 SHA-256 일치 후 DB에 `ok` 기록 · 조회가 내려받지 않고 기록을 읽음 · 15일 전 성공이 `직접 확인`으로 되돌아감 · `missing` 기록에 watchdog이 **CRITICAL 경보** 발생 · CI 교차 검증 3경로(일치 0 / 파일명만 1 / 매니페스트만 1) · 재기동 후 자동 등록 `registered: true` 확인), **자체 발견 결함 2건 수정**(둘 다 라이브에서만 드러났다 — ① **주 1회가 "604800초"로 찍혀 아무도 읽을 수 없었다**: 기동 로그의 간격 표기가 초 고정이었다. 일·시간·분 단위로 표기 ② **"2.0시간로 두면"** — 단위가 초·분·시간·일로 바뀌면 받침이 달라져 조사가 어긋난다(TASK-1901의 "백업 버킷가"와 같은 결함 계열). 조사를 떼고 문장을 다시 씀. 둘 다 회귀 테스트 추가), 해석 확인 CTO_REQUEST #54

## 완료 — Sprint 20

- [x] **TASK-2001 — Enterprise Backup Integrity Platform**: CTO 결정 1901-①~⑤ 반영. 주제는 **남지 않은 것을 보는 것** — 지금까지의 판정은 전부 "남은 기록"을 읽었다. 백업이 실패하면 이력에 남고, 복원이 실패하면 이력에 남는다. 그런데 **돌지 않은 백업은 아무 데도 기록되지 않는다.** 서버가 멈춰 있던 구간은 실패 기록조차 없어서, 이력만 보면 **개별 백업이 전부 성공**으로 보인다. 이번 TASK는 그 빈 구간을 판정 대상으로 끌어온다. **백업 사슬 연속성**(`judgeBackupChain` — 관측 창(기본 24시간) 안에 **있어야 할 개수와 실제 개수를 비교**하고, 창 시작~첫 백업 / 백업 사이 / 마지막 백업~지금을 모두 공백으로 센다. 공백이 **간격의 2배**를 넘으면 실패이고 `backup-chain`은 **복구 필수 항목**이다 — 그 구간은 되돌아갈 수 없기 때문이다. 심각 경보 `backup-integrity:chain`. **한 번 걸러도 2배 안이면 통과**한다 — 지연에 매번 경보하면 정작 불러야 할 때 오지 않는다. **이력이 전혀 없는 첫 도입은 판정하지 않는다**(`직접 확인`) — 처음 켠 서버를 "사슬이 끊겼다"고 할 수는 없다) + **원격 사본 무결성**(`judgeRemoteIntegrity` + `POST /ops/backup/verify-remote` — 올렸다는 **기록**과 사본이 실제로 **있다**는 것은 다르다. 원격 사본을 내려받아 SHA-256을 기록된 체크섬과 대조한다. `missing`(수명 주기 정책·삭제) / `mismatch`(업로드 절단·덮어쓰기 — 이 사본으로는 복구를 장담할 수 없다) / `ok`. **전송 비용이 들어 기본으로 돌지 않으며**(결정 1301-⑤의 연장), 확인하지 않은 상태는 통과가 아니라 `직접 확인`이다. 로컬 덤프로 복구는 되므로 **복구 필수 항목은 아니다**) + **운영 저장소 표준 = Amazon S3**(결정 1901-③ — 체크리스트 항목 `storage-standard`로 승격. 운영에서 `*.amazonaws.com`이 아니면 **실패**, 미설정도 실패. 다만 **개발에서는 개발 저장소가 정상**이다 — 개발자에게 S3를 요구하지 않는다. 해석할 수 없는 주소는 실패로 단정하지 않고 `직접 확인`) + **DB 규모 재평가 신호**(결정 1901-④ — `pg_database_size`로 실측해 10·50·100GB 도달 시 주의. **크기 자체가 문제가 아니라 백업 성능 기준(2초·10초·30초)을 실측으로 다시 재라는 신호**다. 기준마다 경보 키가 달라 10GB를 해소하고 50GB에서 다시 알린다. **기준을 자동으로 바꾸지 않는다** — 재평가는 사람이 한다) + **Major Migration만 자동 등록**(결정 1901-① — 마이그레이션이 전부 대규모 변경은 아니다. 컬럼 하나 추가할 때마다 리허설을 요구하면 **부를 일이 아닌데 부르는 것**이고, 그러면 정작 불러야 할 때 오지 않는다. `apps/api/prisma/major-migrations.json` 매니페스트에 지정된 것만 기동 시 `_prisma_migrations`와 대조해 등록한다. `dr-change`·`pitr-adoption`은 코드가 알 수 없는 사건이라 운영자가 직접 등록) + **중복 등록 금지**(결정 1901-⑤ — 같은 `trigger`가 미해소면 `409`. 리허설 1회로 함께 해소되므로 쌓을 이유가 없고, 쌓이면 목록만 길어져 정작 무엇이 남았는지 보이지 않는다) + **삭제 금지, 취소만**(결정 1901-② — `cancelledAt`/`cancelledBy`/`cancelReason` 추가. `cancelledBy`와 `reason`은 **필수**다 — 왜 취소했는지가 남지 않으면 기록이 아니다. 이미 해소된 요구는 취소할 수 없다(`409`). 취소된 요구는 목록에서 사라지지 않고 **사유와 함께 남는다** — 지운 요구는 왜 지웠는지 남지 않지만 취소는 남는다) + 웹 `/admin/operations` 확장(백업 사슬·원격 사본 무결성·운영 저장소 표준·DB 규모 4개 항목, 원격 사본 검증 버튼, 요구별 취소 버튼, 취소된 요구 목록, Playwright 8종 → 웹 e2e 91) — core 순수 로직(`backup-integrity.ts`: judgeBackupChain/judgeRemoteIntegrity/judgeDatabaseScale/judgeStorageStandard + 경보 3종)+api 어댑터(`BackupService.verifyRemoteCopy`·`databaseSizeBytes`·`backupHistorySince`, `RecoveryDrillService.cancelRequirement`·`onModuleInit`) 분리, **마이그레이션 33**(drill_requirements 취소 필드), 경보 종류에 `backup-integrity` 추가, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3** — 기동 시 Major Migration 자동 등록 로그 확인 · 같은 trigger 중복 등록 **409** · 사유 없는 취소 **400** · 취소 후 재등록 **201** · 해소된 요구 취소 **409** · 없는 요구 **404** · 원격 사본을 실제로 내려받아 SHA-256 일치 확인 · `pg_database_size` 실측 10.5MB · 운영 모드에서 localhost 엔드포인트가 `storage-standard` **실패** 판정 · **18.6시간 실제 사슬 공백을 탐지해 CRITICAL 경보 발생 후 사슬을 메우자 RESOLVED**), **자체 발견 결함 3건 수정**(모두 라이브 검증에서만 드러났다 — ① **재기동이 사슬 공백을 지웠다**: 관측 창을 기동 시각으로 자르는 바람에 꺼져 있던 동안의 진짜 공백이 사라졌다. 기동 시각 기준 절단은 **이력이 전혀 없는 첫 도입일 때만** 적용하도록 수정 ② **개수 제한이 없는 공백을 만들었다**: 사슬 판정이 최근 500건만 읽어 20초 간격 운영에서 창 안 4320건 중 500건만 보이자 **21시간짜리 가짜 공백**을 보고했다. 개수가 아니라 **기간으로** 조회하도록 수정(`backupHistorySince`) ③ **단위 표기가 사실을 가렸다**: 10.5MB DB가 "0.0GB", 20초 공백이 "0분"으로 표시돼 비어 있다/공백이 없다는 뜻으로 읽혔다. 1GB 미만은 MB로, 1분 미만은 초로 표기. 셋 다 회귀 테스트 추가), 해석 확인 CTO_REQUEST #53

## 완료 — Sprint 19

- [x] **TASK-1901 — Enterprise Recovery Assurance**: CTO 결정 1801-①~⑤ 반영. 주제는 **보증** — 관측만 하던 값에 기준을 붙이고, 판정이 미치지 않던 곳까지 같은 규칙을 넓히고, 달력에만 묶여 있던 리허설을 **변경에 반응하게** 만든다. **백업 소요 시간 기준**(결정 1801-① — 2초 미만 정상 / 2~10초 주의 / **10초 초과 연속 3회** 경보 / 30초 초과 심각. `backup_runs.durationMs`는 이미 기록되고 있었고, 여기서 한 일은 **그 값에 기준을 붙이는 것**뿐이다 — 관측만 하고 판정하지 않으면 아무도 보지 않는다. **연속 3회를 요구하는 이유는 한 번 느린 것이 흔하기 때문**이고(디스크 경합·작업 겹침), 30초는 한 번만 넘어도 심각으로 본다(일시적 경합으로 설명되지 않는다). **주의(2~10초)는 경보하지 않는다** — 화면에만 드러낸다. 부를 일이 아닌데 부르면 정작 불러야 할 때 오지 않는다. **실패한 백업은 성능 판정에서 제외**한다(실패는 백업 항목이 이미 잡아내고, 실패까지 걸린 시간을 성능으로 세면 판정이 흐려진다). **자동으로 간격을 바꾸지 않는다** — 시스템이 스스로 백업을 드물게 만들면 손실 구간이 조용히 늘어난다) + **백업 버킷 보호 판정**(결정 1801-③ — 이미지 버킷과 **같은 규칙**으로 판정하고, 문구에 **어느 버킷 이야기인지**를 넣는다. "버전 관리가 꺼져 있습니다"만 두 번 나오면 어느 쪽을 고쳐야 할지 알 수 없다) + **운영 저장소 표준 = Amazon S3**(결정 1801-④ — MinIO·s3rver는 **개발 전용**이다. 운영에서 `S3_ENDPOINT`가 `*.amazonaws.com`이 아니면 경고한다. 개발 전용 저장소는 버전 관리·복제 조회를 지원하지 않아, 그대로 운영에 쓰면 보호 상태를 영영 `직접 확인`으로만 보게 된다) + **변경 후 즉시 추가 리허설**(결정 1801-⑤ — **달력이 아니라 변경이 리허설을 부른다.** 90일 주기는 "아무것도 바뀌지 않았을 때"의 간격이다. `dr-change`·`db-major-change`·`pitr-adoption`이 등록되면 **기한이 남아 있어도 실패**다 — 마지막 리허설이 검증한 것은 변경 이전의 시스템이기 때문이다. **성공한 리허설만 해소한다**(실패한 리허설은 "확인했다"가 아니라 "안 되더라"는 뜻이다). 판정과 경보의 **우선순위를 같게** 뒀다: 마지막 리허설 실패 → 변경 사건 → 기한 초과. 순서가 다르면 제목과 본문이 어긋나므로 `lastFailed` 플래그로 구분한다) + **배포 게이트 미포함 확인**(결정 1801-② — 리허설이 밀렸다는 이유로 긴급 배포가 막히면 안 된다. 배포 체크리스트에 리허설·백업 성능 항목이 없음을 회귀 테스트로 고정) + 웹 `/admin/operations` 확장(백업 소요 시간·백업 버킷 보호·미해소 변경 사건 표시, Playwright 4종 → 웹 e2e 83) — core 순수 로직(`backup-performance.ts`: judgeBackupPerformance/detectBackupPerformanceAlert, `recovery-drill.ts`: DRILL_TRIGGERS·requirements 판정, `enterprise-recovery.ts`: judgeStorageProtection에 label)+api 어댑터(`RecoveryDrillService.requireDrill`·`StorageService.describeBackupProtection`) 분리, **마이그레이션 32**(drill_requirements), 경보 종류에 `backup-performance` 추가, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + 실 SMTP** — 실 백업 이력으로 성능 `normal`(225ms) 판정 · 35초 이력 주입 시 **CRITICAL 경보** 발생 · **변경 사건 등록 시 다음 예정일이 89일 남았는데도 리허설이 `fail`로 전환**되고 warning 경보 발생 · **실패한 리허설은 요구를 해소하지 못하고**(satisfiedAt null 유지) 성공한 리허설이 해소 · 백업 버킷 보호가 이미지 버킷과 별도로 판정됨), **자체 발견 결함 3건 수정**(경보 제목과 본문이 어긋날 수 있었다 — 판정은 실패를 먼저 보는데 경보는 변경 사건을 먼저 봤다 / 한국어 조사 오류 "백업 버킷**가**" — 라벨 뒤 조사 제거 / "30.0초**을** 넘었습니다" — "보다 깁니다"로 변경, 셋 다 회귀 테스트 추가), 해석 확인 CTO_REQUEST #52

## 완료 — Sprint 18 (Goal: Enterprise Operations Platform — CTO 공식 종료, 2026-07-29)

- [x] **TASK-1801 — Enterprise Operations Platform**: CTO 결정 1701-①~⑤ 반영. 큰 줄기는 **판정만 하던 것을 지키게 만드는** 일이다 — 손실 구간을 실제로 줄이고, 백업을 이미지와 분리하고, 리허설을 기록 대상으로 만든다. **백업 주기 1시간**(결정 1701-① — `backup`이 시각 기반에서 간격 기반(`OPS_CHECK_BACKUP_INTERVAL`, 기본 `1h`)으로 바뀌어 **RPO 상한이 24시간 → 2시간**이 됐다. 복구 절차는 전혀 바뀌지 않았다 — `backup-strategy.md`가 PITR보다 이쪽을 먼저 권고한 이유다. **손실 한도 목표가 간격을 따라간다**(`defaultRpoTargetMs = 간격 × 2`) — 간격만 바꾸고 목표를 24시간에 묶어 두면 백업이 20시간 멈춰도 "정상"으로 보인다. 구 `OPS_CHECK_BACKUP_AT`은 쓰이지 않으며, **값이 남아 있으면 경고한다** — 조용히 무시하면 운영자는 그 시각에 돈다고 믿는다) + **백업 전용 버킷**(결정 1701-② — 원격 복제 대상이 `BACKUP_BUCKET`(기본 `<S3_BUCKET>-backups`)으로 분리됐다. 같은 버킷에 두면 그 버킷이 사라질 때 **이미지와 백업이 함께** 사라져 원격 복제의 목적을 절반만 달성한다. 백업 버킷에는 **공개 읽기 정책을 걸지 않는다** — 덤프가 공개되면 데이터베이스 전체가 공개되는 것과 같다) + **Versioning 운영 필수 · Replication 권장**(결정 1701-③ — 운영에서 버전 관리가 꺼져 있으면 `fail`, 복제는 `warn`. 둘의 무게가 다른 이유는 버전 관리가 없으면 **실수 한 번으로 되돌릴 수 없게** 되지만 복제가 없는 것은 리전 전체가 사라지는 훨씬 드문 사건 대비가 없다는 뜻이기 때문이다. **조회 불가는 운영에서도 `manual` 유지** — 확인하지 못한 것을 "꺼짐"으로 단정하면 그것도 거짓이다) + **복구 리허설 분기 1회**(결정 1701-⑤ — TASK-1601부터 "복구 절차 숙지"는 `manual`로 남아 있었다. 정직한 표시였지만 **`manual`은 영원히 `manual`이라 아무도 하지 않아도 아무 일도 일어나지 않는다.** 기록하면 그 항목이 비로소 판정 대상이 된다. 기록 없음=`manual`·경보 없음(아직 시작하지 않은 것이지 규칙을 어긴 것이 아니다) / 기한 초과=주의 경보 / 유예(14일) 초과=실패 / **마지막 리허설이 실패=심각 경보**(절차가 깨진 것이 확인됐다). **실패한 리허설도 기록한다** — 오히려 그쪽이 값지다. 성공만 남기면 기록은 장식이 된다. **수행자는 필수**이고, 리허설이 밀린 것은 **복구 가능 판정을 막지 않는다**. 리허설 경보는 예약 점검 경보와 종류를 나눠 동기화한다 — 섞으면 한쪽이 다른 쪽을 해소한다) + **원격 사본 자동 검증은 Sprint 18 이후로 유보**(결정 1701-④) + 웹 `/admin/operations` 확장(복구 리허설 섹션·기록 폼·이력·백업 버킷 분리 표시, Playwright 5종 → 웹 e2e 79) — core 순수 로직(`recovery-drill.ts`: judgeRecoveryDrill/detectDrillAlert, `schedule.ts`: defaultRpoTargetMs, `enterprise-recovery.ts`: judgeStorageProtection에 production 반영)+api 어댑터(`RecoveryDrillService`·`StorageService.putBackupObject`) 분리, **마이그레이션 31**(recovery_drills), 경보 종류에 `recovery-drill` 추가, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + 실 SMTP + 실 pg_dump/pg_restore** — 20초 간격으로 설정해 **예약 백업이 3회 반복 실행**되고 매회 무결성 판독(객체 162개)·체크섬·복제까지 수행 · **`acos-backups` 버킷이 이미지 버킷과 따로 생성**되어 덤프가 그쪽에만 저장됨 · RPO 목표가 간격의 2배(40초)로 자동 계산됨 · **리허설 기록 전 `manual`·무경보 → 실패 기록 시 CRITICAL 경보 발생 → 성공 기록 시 RESOLVED**, 다음 예정일 89일 계산 · 리허설 실패가 `recoverable`을 막지 않음 확인), 해석 확인 CTO_REQUEST #51

## 완료 — Sprint 17 (Goal: Enterprise Backup & Disaster Recovery — CTO 공식 종료, 2026-07-29)

- [x] **TASK-1701 — Enterprise Backup & Disaster Recovery**: TASK-1601이 "백업이 있고 복원된다"를 증명했다면, 이번은 그 다음 세 질문이다 — **덤프가 온전한가 · 같은 곳에만 있지 않은가 · 얼마를 잃고 얼마나 걸리는가**. **Backup Integrity**(백업 직후 `pg_restore --list`로 목차를 읽고 SHA-256을 기록한다 — **복원하지 않고도 파일이 온전한지 알 수 있는 가장 싼 검사**다. 읽히지 않는 덤프는 존재하지만 쓸 수 없는 파일이고, 그것을 "백업 있음"으로 세면 복구 계획이 거짓이 된다. 목차가 비어 있으면 읽힌 것이 아니다. 무결성 실패는 **복구 가능성을 좌우하는 항목**이다. 체크섬은 이력에 그대로 남기되(원격 사본과 대조할 근거) 체크리스트 설명에는 앞 12자만 싣는다) + **Offsite Replication**(`BACKUP_OFFSITE=on`이면 덤프를 오브젝트 저장소에도 올린다 — 백업이 데이터베이스와 **같은 곳에만** 있으면 그 곳이 사라질 때 백업도 사라진다. **복제 실패가 백업을 실패시키지는 않는다**(덤프는 이미 받았다) — 대신 로그로 남기고 화면에서 드러낸다. 미구성은 실패가 아니라 주의이고 복구 가능성을 막지도 않는다. 원격 키는 노출하지 않고 **있는지만** 알린다) + **RPO·RTO 판정**(RPO는 마지막 성공 백업 이후 경과, RTO는 **마지막 복원 검증에 실제로 걸린 시간** — 추정이 아니라 측정이다. 복원 검증이 이미 매일 돌고 소요 시간을 기록하고 있으므로 별도 계측 없이 근거 있는 값을 쓴다. 다만 **하한**이며, 장애를 알아차리고 결정하고 인프라를 준비하는 시간은 포함하지 않는다 — 화면에도 그렇게 적는다. 측정치가 없으면 `직접 확인`이고 **통과가 아니다**) + **복원 대상 강제 분리**(CTO 결정 1601-② — 복원은 대상 스키마를 **지우고 쓴다**. 호스트·포트·DB 이름만 비교해 **자격 증명이 달라도 같은 DB는 같은 DB**로 보고, 같으면 **기동하지 않는다**. 개발 환경에서도 오류다 — 운영 DB를 지우는 설정은 어디서도 허용할 수 없다. 기동 후 설정이 바뀔 수 있으므로 실행 직전에 한 번 더 본다) + **BACKUP_DIR 운영 필수**(CTO 결정 1601-① — 미설정이면 **기동하지 않는다**. 백업이 컨테이너와 함께 사라지는 구성으로 운영을 시작할 수는 없다) + **이미지 저장소 보호 상태**(CTO 결정 1601-④ — 버킷의 Versioning·Replication을 읽어 체크리스트에 표시한다. **애플리케이션은 이미지를 직접 백업하지 않는다.** 저장소가 알려 주지 않으면 `직접 확인`으로 남긴다 — 조회를 지원하지 않는 저장소를 "정상"이라고 말하면 그 화면은 거짓이 된다. 꺼져 있으면 주의이지 실패가 아니다 — 버전 관리는 삭제·덮어쓰기로부터 보호하는 장치이지 지금 이미지가 없다는 뜻이 아니다) + **WAL·PITR·Incremental 도입 검토**(CTO 결정 1601-③ — 구현하지 않고 `docs/operations/backup-strategy.md`로 판단 근거를 정리. 결론: 현재 데이터 규모(100KB 수준)에서 **백업 주기 단축이 PITR보다 비용 대비 효과가 크다**. PITR은 복구를 *더 어렵게* 만들므로 정기 리허설이 운영 절차로 자리 잡은 뒤에 도입해야 하고, 증분은 PostgreSQL 17이 필요하며 지금은 아낄 것이 없다. 재검토 조건 4가지를 전부 **이미 기록 중인 값**(`backup_runs`)으로 확인할 수 있게 두었다) + **신선도 기본값 유지**(CTO 결정 1601-⑤ — 백업 2일·복원 검증 8일, 환경변수로 조정 가능하되 기본값을 회귀 테스트로 고정) + 웹 `/admin/operations` 확장(복구 목표 카드·백업 보호 4항목, Playwright 5종 → 웹 e2e 74) — core 순수 로직(`enterprise-recovery.ts`: judgeIntegrity/judgeOffsite/judgeRecoveryObjectives/judgeStorageProtection/judgeRestoreTarget)+api 어댑터(BackupService 확장·StorageService.describeProtection) 분리, **마이그레이션 30**(backup_runs에 checksum·integrityOk·entries·offsiteKey), 새 문서 `docs/operations/backup-strategy.md`, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 S3 + 실 SMTP + 실 pg_dump/pg_restore** — 백업 1회에서 무결성 판독 **객체 158개**·SHA-256 기록·S3 복제 성공, **원격 사본의 SHA-256이 기록된 체크섬과 정확히 일치**(비트 단위 동일 확인) · **손상된 덤프를 최신으로 만들자 크기 검사를 통과했음에도 `복구 불가`로 판정**(무결성이 유일한 차단 항목) · BACKUP_DIR 미설정 운영 기동 **차단** · 복원 대상을 운영 DB로 지정한 운영 기동 **차단** · s3rver가 복제 상태를 알려 주지 않아 저장소 보호가 `직접 확인`으로 남음), **자체 발견 결함 1건 수정**(환경변수 설명의 마크다운 `**`가 기동 실패 로그에 별표로 그대로 출력 — 1302·1601에 이은 같은 계열 재발이라 ENV_SPECS 전체를 검사하는 회귀 테스트 추가), 해석 확인 CTO_REQUEST #50

## 완료 — Sprint 16 (Goal: Production Verification & Operational Readiness — CTO 공식 종료, 2026-07-29)

- [x] **TASK-1601 — Production Verification & Operational Readiness**: "잘 돌고 있는가"에서 **"지금 무너지면 되살릴 수 있는가"** 로 넘어간다. **Real SMTP Validation**(`POST /ops/notifications/verify-smtp` — 실제 SMTP에 연결·인증까지 하되 **메일은 보내지 않는다**(점검이 수신함을 채우면 사람이 점검 메일을 무시하게 된다). 미설정은 실패가 아니라 **미구성**으로 알리고, 결과에 `ALERT_EMAIL_TO`를 담지 않는다 — 수신자 주소는 비밀이다. **이 세션에서 처음으로 살아 있는 SMTP 서버와 실제 EHLO→AUTH→QUIT를 주고받아 검증했다** — TASK-1401부터 남아 있던 "메일 경로 미검증" 부채를 갚았다) + **Backup Automation**(`pg_dump --format=custom`, 매일 `OPS_CHECK_BACKUP_AT` 기본 03:00 로컬. **크기를 기록한다** — pg_dump는 부분 실패에도 0에 가까운 파일을 남길 수 있고 그걸 성공으로 세면 복구 계획이 통째로 거짓이 된다(1KB 미만은 `failed`). 보존 기간이 지나도 **최근 `BACKUP_KEEP_MINIMUM`개는 남긴다** — 정리가 마지막 백업을 지우면 안 된다. 실패해도 예외를 던지지 않고 이력에 남긴다. 화면·API에는 **파일명만** 노출한다) + **Restore Verification**(매일 `OPS_CHECK_RESTORE_AT` 기본 03:30 로컬, 최신 덤프를 **별도 DB**(`BACKUP_RESTORE_DB_URL`)에 복원하고 테이블 수를 센다. **운영 DB에는 절대 복원하지 않는다** — 검증하려다 데이터를 잃는 것이 최악이다. 대상이 없으면 `configured:false`, **미구성과 실패는 다르다**. `pg_restore`는 무해한 경고에도 비영점 종료하므로 판정은 종료 코드가 아니라 **복원 후 테이블 수**로 한다. **복원해 보지 않은 백업은 백업이 아니다** — 이력이 없으면 `missing`이고 그것은 통과가 아니다) + **Disaster Recovery Checklist**(배포 체크리스트(1202)와 목적이 다르다 — 그쪽은 "지금 배포해도 되는가", 이쪽은 "지금 무너지면 되살릴 수 있는가". `복구 가능`은 백업·복원·DB·저장소에 실패가 없을 때만 true이고, Redis·경보 채널은 복구 가능성을 좌우하지 않는다. 복구 절차 숙지·연락 체계는 자동 판정이 불가능하므로 **`manual`로 남긴다** — 모르는 것을 통과로 처리하지 않는다) + **Provider Smoke Automation**(예약 자리(`OPS_CHECK_SMOKE_AT`)는 있지만 **기본은 꺼져 있다** — 실제 과금되기 때문(CTO 결정 1301-①). "지금 점검"(`runAll`)도 **꺼진 작업은 건너뛴다**) + **Operations Dashboard / Redis Health Dashboard**(웹 `/admin/operations` ADMIN 전용 — 체크리스트·백업 이력·복원 이력·Redis·SMTP, "지금 백업"·"지금 복원 검증"·"메일 경로 확인" 버튼, Playwright 8종 → 웹 e2e 69) + **CTO 결정 1501-①~④ 반영**(① 일 1회 점검을 **운영 서버 로컬 시각**으로(`TZ`) ② Redis 장애 시 **LLM 호출은 계속 허용**, Scheduler만 중단, 30분(`OPS_LOCK_OUTAGE_THRESHOLD_MS`) 이상 지속되면 Critical 반복 ③ **Dead Letter도 지우지 않고** 90일 후 `ARCHIVED` ④ Grace Factor 기본 3배 유지, `OPS_SCHEDULER_GRACE_<JOB>`으로 Job별 조정) — core 순수 로직(`disaster-recovery.ts`: judgeBackup/judgeRestore/buildDisasterRecoveryChecklist/summarizeDisasterRecovery, `alerts.ts`: detectLockOutageAlert, `schedule.ts`: 로컬 시각 판정·resolveGraceFactor)+api 어댑터(`BackupService`) 분리, **마이그레이션 29**(backup_runs·restore_runs·notification_queue.archivedAt·NotificationQueueStatus.ARCHIVED), 새 문서 `docs/operations/disaster-recovery.md`, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 SMTP + 실 pg_dump/pg_restore** — 백업 4회 성공(수동 1·예약 3, 96~98KB)·복원 검증 4회 모두 **테이블 30개 확인**·초기 상태는 `recoverable:false`(백업·복원 이력 없음)에서 백업·복원 후 `true`로 전환·SMTP 실연결 검증·**Redis 중단 중 LLM 호출 HTTP 200**이고 예약만 중단되며 Critical 경보 발생, 복구 시 RESOLVED, 재중단 시 재발생), **자체 발견 결함 4건 수정**(runAll이 꺼진 과금 작업을 실행 / Prisma `?schema=public`을 pg_dump가 거부 / 기동 로그·화면이 "04:00 UTC"라고 표시(실제는 로컬) / 경보 문구의 마크다운 `**`가 그대로 노출), 해석 확인 CTO_REQUEST #49

## 완료 — Sprint 15 (Goal: High Availability & Operations Reliability — CTO 공식 종료, 2026-07-29)

- [x] **TASK-1501 — High Availability & Operations Reliability**: TASK-1401이 남긴 부채 두 건(알림 큐잉·점검 정지 감지)을 갚고, 보관을 사람 손에서 떼어 낸다. **Scheduler Stopped Alert**(CTO 결정 1401-① — Redis 장애 시 **단일 모드로 자동 폴백하지 않는다**(폴백하면 여러 인스턴스가 동시에 점검을 돌린다). 대신 멈춘 사실을 critical로 알린다. **감시자는 잠금 없이 돈다** — 멈춘 원인이 대개 잠금을 못 잡는 것이라 감시까지 잠금을 요구하면 정작 알려야 할 때 알리지 못한다(여러 인스턴스가 동시에 감지해도 경보 `key`가 같아 중복되지 않는다). 판정은 **관대하게** — 간격의 3배(`OPS_SCHEDULER_GRACE_FACTOR`)를 넘겨야 멈춘 것으로 본다(한 번 늦었다고 경보하면 사람이 무시하게 된다). 한 번도 안 돌았으면 **기동 시점부터** 센다(방금 뜬 서버를 장애라 하지 않는다). 원인이 잠금이면 그렇게 적는다) + **Persistent Notification Queue / Retry Worker / Dead Letter Queue**(CTO 결정 1401-② — TASK-1401의 재시도는 프로세스 안에서만 돌아 재시작하면 사라졌다. 이제 경보는 `notification_queue`에 담기고 워커가 꺼내 보낸다 — **재시작을 견딘다**. **워커는 리더만 돌린다**(여러 인스턴스가 같은 항목을 집으면 같은 알림이 여러 번 간다), 잠금을 못 잡으면 조용히 넘긴다(정상 동작이라 소음이 될 이유가 없다). **Dead Letter로 가는 경우는 둘** — 되돌릴 수 없는 실패(4xx)와 최대 시도 소진. **지우지 않고** `requeue`로 시도 횟수를 되돌려 다시 보낸다(설정을 고친 뒤 쓰는 절차). 큐 적재 실패는 알림이 **아예 사라지는** 것이라 로그로 크게 남긴다) + **Alert Archive 예약화**(CTO 결정 1401-③ — 네 번째 예약 점검 `alert-archive`로 편입, **매일 04:00 UTC**(`OPS_CHECK_ARCHIVE_AT`). 시각 기반 점검을 표현하려고 스케줄러를 **단일 티커**로 재구성 — 점검마다 타이머를 두면 "매일 몇 시"를 표현할 수 없고 재기동할 때마다 시점이 밀린다. **그 시각 전에는 한 번도 안 돌았어도 돌지 않는다**("새벽에 돌리라"는 지시를 기동 시점에 어기지 않기 위해). 보관은 경보를 만들지 않는다 — 정리 작업이다) + **운영 기본 채널 정책**(CTO 결정 1401-④ — Slack Warning 이상 / **Email Critical 이상** / Webhook Warning 이상 / 해소 포함. 메일만 Critical인 이유는 메일이 지우기 번거롭고 쌓이면 읽지 않게 되기 때문. `ALERT_*_MIN_LEVEL`·`ALERT_*_RESOLVED`로 전부 변경 가능하고 알 수 없는 값은 기본값으로 되돌린다) + 웹 `/admin/production` 알림 큐 섹션(대기·전송 완료·Dead Letter·워커 상태·"지금 보내기"·"실패분 다시 보내기", 잠금 불가 시 "예약 점검이 돌지 않습니다" 표시, Playwright 3종 → 웹 e2e 60) — core 순수 로직(schedule.ts: parseDailyAt/shouldRun 시각 기반/isSchedulerStopped, alerts.ts: detectSchedulerAlerts, notification.ts: resolveChannelPolicy/decideQueueOutcome/isDue/summarizeQueue)+api 어댑터(NotificationQueueService) 분리, **마이그레이션 28**(notification_queue + NotificationQueueStatus), 라이브 검증(**실 Redis + 실 DB** — 큐 적재 후 워커가 503 두 번을 넘겨 2회째에 SENT · **워커를 끈 채 경보 발생 → 프로세스 강제 종료 → 재시작하니 큐에서 꺼내 전송(재시작 생존)** · 404 주소는 1회 시도 후 DEAD이고 requeue로 attempts 0 복귀 · **Redis 중단 시 예약 실행 0회(자동 폴백 없음)이고 정지 경보 CRITICAL 발생, Redis 복구 시 점검 재개·경보 RESOLVED** · 보관 점검이 "삭제하지 않습니다"로 실행), 해석 확인 CTO_REQUEST #48

## 완료 — Sprint 14 (CTO 공식 종료, 2026-07-29)

- [x] **TASK-1401 — Production Operations Platform** (`859886a`, CTO 승인 — Redis 장애 시 **단일 모드 자동 폴백 금지**하고 대신 Scheduler Stopped Alert 구현(→TASK-1501에서 이행), **Persistent Notification Queue**(Retry Worker + Dead Letter Queue) 구현(→TASK-1501에서 이행), **Archive를 Scheduler에 편입**하고 기본 하루 1회 새벽 실행(→TASK-1501에서 이행), 운영 기본 채널 정책(Slack Warning 이상 / Email Critical 이상 / Webhook Warning 이상 / Resolve 포함)을 환경변수로 변경 가능하게 유지(→TASK-1501에서 이행), Sprint 14 공식 종료): TASK-1302가 남긴 두 부채를 갚는다 — **예약 점검의 인스턴스별 중복 실행**과 **알림이 한 번 실패하면 아무도 모르는 채로 끝나던** 문제. **Distributed Scheduler / Redis Leader Election / Distributed Lock**(CTO 결정 1302-② — 예약 실행은 `acos:lock:scheduler:<job>` 임차를 잡은 인스턴스만 수행하고 **수동 실행은 잠금을 요구하지 않는다**(사람이 지금 확인하려는 것). **임차는 반드시 만료된다**(`OPS_LOCK_TTL_MS` 기본 30초) — TTL 없는 락은 리더가 죽으면 영원히 잠기고 그것이 가장 나쁜 실패다. **만료 직전이 아니라 TTL의 1/3 지점에서 미리 갱신**(만료 직전 갱신은 네트워크 지연 한 번에 리더십을 잃는다), **소유자 확인 Lua 스크립트로만 갱신·해제**(비교 없이 지우면 남의 임차를 해제해 리더가 둘이 된다), **Redis가 끊기면 잠그지 못한 것으로 본다**(모르는 채 "잡았다"고 하면 중복 실행이 조용히 일어난다). `REDIS_URL` 미설정 시 단일 인스턴스 모드이고 **그 사실을 API·화면에 드러낸다**) + **Notification Center — Slack / Email / Webhook**(채널마다 최소 심각도와 해소 알림 여부를 따로 정한다 — 모든 warning을 밤중에 받으면 사람이 알림을 끈다. **해소는 심각도와 별도 스위치**(critical만 받는 사람도 해소는 받고 싶을 수 있다), **채널 하나가 죽어도 나머지는 보낸다**(알림 체계가 단일 장애점이 되면 안 된다), **주소는 어떤 응답에도 담지 않는다**) + **Webhook Retry**(지수 백오프 1s→2s→4s 상한 30초, 최대 `ALERT_RETRY_MAX_ATTEMPTS` 기본 4회(최초 포함). **4xx는 재시도하지 않는다**(429 제외 — 같은 요청은 같은 답을 받는다. 매달려 있어 봐야 로그만 늘어난다), 네트워크 오류·5xx·429만 재시도. **모든 시도 결과를 `notification_deliveries`에 기록**해 "왜 아무도 못 받았는가"를 추적, 전송 실패가 경보 감지를 실패시키지 않는다) + **Alert History**(보관 포함 전체 이력 + 종류·심각도·상태·기간 필터 + **평균 해소 시간** — 경보가 많은 것보다 **오래 방치되는 것**이 더 나쁜 신호인데 건수만 세면 안 보인다) + **Alert Archive**(CTO 결정 1302-④ — **삭제하지 않는다**. 해소 후 `ALERT_ARCHIVE_AFTER_DAYS`(기본 90일) 경과 시 `ARCHIVED`로 이동. **활성 경보는 절대 보관하지 않고**(문제가 있는데 화면에서 사라지면 그게 사고다), **해소 시각을 모르면 건드리지 않는다**(유예가 지났는지 알 수 없다)) + **CTO 결정 1302-① 이행**(종류별 재알림 간격 `ALERT_COOLDOWN_BUDGET/PROVIDER/UNPRICED/CONFIG_MS` — `종류별 ?? 전체 기본 ?? 30분`, 30분 공식 표준 유지) + **CTO 결정 1302-③ 이행**(운영에서 `GATE_STRICT`·`GATE_ALERTS` **기본 ON**, 끄려면 명시적 `=0` — 안전한 쪽이 기본이어야 사람이 잊었을 때 사고가 나지 않는다. 그 외 환경은 꺼짐 기본) + 웹 `/admin/production` 확장(채널 구성·예약 조율(분산/단일·점검별 리더)·전송 시도 이력·보관 정리 버튼, Playwright 3종 → 웹 e2e 57) — core 순수 로직(leader.ts: decideLease/ownsLease/renewAfter, notification.ts: shouldNotify/decideRetry/채널 본문, alert-archive.ts: planArchive/summarizeHistory)+api 어댑터(DistributedLockService·NotificationService) 분리, **마이그레이션 27**(AlertStatus.ARCHIVED·alerts.archivedAt·notification_deliveries), 라이브 검증(**실 Redis + 실제 두 인스턴스** — 15초 간격 ~68초에 예약 실행 5회(잠금 없으면 ~9회)·리더 1명·**리더 강제 종료 시 40초 내 다른 인스턴스가 인계**·503 두 번 반환하는 수신 서버에 **2회째 시도로 성공**하고 attempts=2 기록·채널 시험 실제 전송 성공·최근 해소는 보관 0건/100일 전 해소는 1건 보관(행은 유지)·게이트 운영 기본값 STRICT=on·ALERTS=on·비밀 주소 미노출), 해석 확인 CTO_REQUEST #47

## 완료 — Sprint 13 (CTO 공식 종료, 2026-07-29)

- [x] **TASK-1302 — Production Automation & Alerting** (`942ff5f`, CTO 승인 — Alert 정책(Cooldown 30분·Resolve 알림·Severity 상승 즉시 알림·unknown 경보 제외)을 공식 표준으로 유지하되 **종류별 Cooldown 환경변수 확장 허용**(→TASK-1401에서 이행), 다중 인스턴스는 현재 중복 실행 구조 유지하고 **Redis Distributed Lock은 다음 Sprint**(→TASK-1401에서 이행), 운영에서는 **GATE_STRICT=ON·GATE_ALERTS=ON을 기본값으로**(→TASK-1401에서 이행), **Alert는 삭제하지 않고 Resolve 후 90일 보관 후 Archive**(→TASK-1401에서 이행), Sprint 13 공식 종료): TASK-1301의 점검들은 **사람이 화면을 열어야** 결과를 볼 수 있었다 — 이 TASK는 그것을 주기적으로 돌리고 문제가 생기면 **사람을 찾아가게** 한다. **Scheduled Cost Verification / Provider Validation / Health Check**(간격 `OPS_CHECK_*_INTERVAL`, `15m`·`1h`·`30s`·ms 모두 해석하고 해석 불가 시 기본값으로 — 잘못 적은 값 때문에 점검이 멈추거나 폭주하는 것보다 안전하다. `off`로 개별·전체 중단. 기본 15분/15분/1시간 — **Health Check가 가장 드문 이유**는 실 Provider 호출이 섞이는 점검이기 때문. **점검마다 책임지는 경보 종류를 나눔** — 해소 판정은 "이번에 감지되지 않았다"로 하므로 비용 점검이 Provider 경보까지 해소하면 죽어 있는 Provider가 조용히 사라진다. **예약 점검은 Live Check를 하지 않음**(CTO 결정 1301-① — 자동 과금 방지, health-check 점검은 쌓인 Execution만 읽고 새 호출을 만들지 않는다). 겹쳐 돌지 않음, 타이머 `unref()`, **점검 자체의 실패도 이력에 기록**(조용히 안 도는 점검이 가장 위험)) + **Budget / Provider Failure / Unpriced Model / Configuration Alert**(4종 — 예산은 임계 warning·초과 critical(무제한이면 경보 없음), Provider는 degraded warning·down critical이되 **`unknown`은 경보하지 않음**(모르는 것을 문제라 하지 않는다), unpriced는 warning이고 **호출 차단은 하지 않음**(CTO 결정 1301-⑤ — 막으면 안전하지만 새 모델 도입이 불가능해진다), 설정 오류·Provider 키 문제는 critical(기동 시점 Fail Fast만으로는 뜬 뒤의 변화를 알 수 없다)) + **중복·해소 판정**(`key`가 같으면 같은 문제 — 쿨다운(`ALERT_COOLDOWN_MS` 기본 30분) 안에서는 재알림 억제. **같은 내용이 반복되면 사람이 경보를 무시하기 시작하고 그 순간 경보 체계는 없는 것과 같아진다**. 심각도 상승(warning→critical)은 쿨다운 무관 알림, **해소도 알림**(났다는 사실만 알리면 지금 문제가 있는지 알 수 없다), 해소된 경보는 지우지 않고 RESOLVED로 보존) + **전달 채널**(로그 항상 + 웹훅 `ALERT_WEBHOOK_URL`. **웹훅 실패가 점검을 실패시키지 않음** — 알림 채널이 죽었다고 감지까지 멈추면 상황이 더 나빠진다. 웹훅 주소는 API로 노출하지 않음) + **CI/CD Deployment Gate**(`scripts/deployment-gate.mjs` — CTO 결정 1202-④ 이행. `/health/ready` 자동 판정, exit `0` 가능 / `1` 불가 / **`2` 판정 불가**(확인하지 못한 것을 통과로 처리하지 않는다), `GATE_STRICT=1` 수동 항목 차단·`GATE_ALERTS=1` critical 경보 차단) + **CTO 결정 1301-③ 이행**(진단 호출을 운영 통계에서 분리 — **feature를 추가하지 않고**(0601 승인의 4종 유지) `executions.diagnostic` 메타데이터로 구분. Production Monitoring에서 제외하되 `diagnosticCalls`로 **숨기지 않고 표시**) + **CTO 결정 1301-② 이행**(모니터링 기준 환경변수화 `LLM_MONITOR_HEALTHY_RATE`/`DEGRADED_RATE`/`MIN_SAMPLES`/`P95_WARN_MS` — 확정 기본값 0.95/0.5/5/20000 유지, 해석 불가·**순서가 뒤집힌 기준**(degraded>healthy)은 기본값으로 되돌림) + 웹 `/admin/production` 경보 섹션(활성 경보·예약 점검 구성·"지금 점검" 버튼, Playwright 4종 → 웹 e2e 57) + **구현 중 발견해 고친 표시 결함 2건**(① 경보 배지가 주의 경보만 있을 때 "이상 없음"인데 바로 아래에 경보를 나열해 **배지와 목록이 모순** — 1202에서 고친 것과 같은 부류라 회귀 테스트를 남김 ② 비용 검증 메시지의 마크다운 강조(`**…**`)가 화면에 평문으로 그대로 찍힘 — core에서 제거하고 "마크다운을 넣지 않는다" 테스트 추가) — core 순수 로직(alerts.ts: detect*/reconcileAlerts/summarizeAlerts, schedule.ts: parseIntervalMs/resolveSchedules/shouldRun)+api 어댑터(AlertService·ScheduledChecksService) 분리, **마이그레이션 26**(executions.diagnostic·alerts·check_runs), 라이브 검증(미인증 401 2/2·예약 점검이 스스로 돌아 미산정 경보 자동 감지(trigger=schedule)·같은 경보 재알림 억제(occurrences 누적, notified 0)·예산 초과 critical raise → 복구 시 resolve·진단 호출 3건 제외하고 사용자 호출 8건만 집계·게이트 exit 0/1/2 3경로), 해석 확인 CTO_REQUEST #46

- [x] **TASK-1301 — Real AI Provider Production Integration** (`6d2bce6`, CTO 승인 — Live Check는 기본 실행하지 않고 운영자가 명시적으로 실행하도록 유지, 모니터링 기본값(Healthy 95% / Degraded 50% / 최소 표본 5 / p95 20초) 유지하되 향후 환경변수로 조정 가능하도록 설계(→TASK-1302에서 이행), Health Check와 Live Check는 운영 통계에서 분리하되 **Feature를 추가하지 말고 Execution 메타데이터 기반으로** 구현(→TASK-1302에서 이행), Cost Verification은 수동 실행 유지하고 다음 Sprint에서 Scheduler 추가(→TASK-1302에서 이행), Unpriced 모델은 Alert 대상으로 유지하고 호출 차단은 하지 않음): **API Key Validation**(Provider별 키 형식 규칙을 core 한 곳에 선언 `packages/core/src/llm/api-key.ts` — openai `sk-`/anthropic `sk-ant-`/gemini 길이만, 공통 20자 이상. **플레이스홀더를 별도 상태로 탐지**(`sk-xxxx…`·`your-api-key`·`changeme` — 형식 검사만으로는 통과해 버리는 배포 사고의 단골). **키 값은 어떤 경로로도 노출하지 않음** — 앞 6자 힌트와 길이만) + **CTO 결정 1202-② 이행**(설정에서 참조하는 Provider(`LLM_PROVIDER`/`LLM_ROUTE_*`/`LLM_FAILOVER_PRIORITY`/`LLM_EXPERIMENT_*`)의 키를 **운영 조건부 필수**로 env-spec에 선언(`requiredWhen`) + 형식 검사(`validate`) — 배포 준비 검증과 **Fail Fast 기동**에 그대로 반영되어 참조하는데 키가 없으면 운영에서 서버가 뜨지 않는다. 쓰지 않는 Provider의 키는 불필요. 부분 문자열 오탐 방지(`openai-legacy`가 있다고 `openai` 참조로 보지 않음)) + **Live Check**(`?live=1` — Provider마다 최소 완성 호출 1회. **실제 과금이 발생하므로 기본은 형식 검사만** 하고 실행 여부를 `liveChecked`로 명시. 예외가 나도 보고서로 회수해 blocker로 표시) + **Cost Verification**(기록된 Execution 비용을 가격표로 재계산해 대조 — `unpriced`(가격표 없음 → **비용이 null로 남아 예산 상한이 무력화**되므로 가장 크게 다룸)/`mismatch`(단가 변동)/`missing-usage`(usage 없음)를 **조치가 다르므로 구분**, 응답에 가격표 동봉해 바로 조치 가능) + **Production Monitoring**(Provider별 성공률·지연 분포·비용·경보 — **표본이 적으면 판정하지 않음**(기본 5회 미만 `unknown`, 1회 실패로 "장애"라고 말하지 않는다), **p50/p95/p99**(평균만 보면 꼬리 지연을 놓친다. **최근접 순위**라 관측되지 않은 값을 지어내지 않음), 상태 healthy≥95%/degraded≥50%/down, 전체는 **가장 나쁜 Provider**를 따름, 실패 호출의 `cost=null`은 미산정으로 세지 않음(실패는 usage가 없는 게 정상 — 그러지 않으면 경보가 늘 울린다)) + **Vision Production**(이미지 전달 형식이 Provider마다 다름 — OpenAI `image_url`(data URL)/Anthropic `image` block(base64+media_type)/Gemini `inlineData`. 하나만 맞으면 라우팅·Failover로 Provider가 바뀌는 순간 **이미지가 조용히 사라지고 "이미지 없이 추측한 결과"가 정상처럼 기록**되므로 세 Provider 전부 같은 시나리오 회귀 테스트 + 이미지 없을 때 헛된 첨부 미생성) + **Provider Smoke Test 확장**(API Key 검증 + **키가 설정된 모든 Provider** Health Check(기본 하나만 확인하면 전환되는 순간에야 장애를 처음 안다) + Vision 커버리지 필수화 + 비용 검증·운영 모니터링 판정(critical 경보 시 실패). ADMIN 권한이 없어 건너뛴 항목은 `…`로 표시하고 **실패로 세지 않음** — 확인하지 못한 것을 통과라고 하지 않기 위해) + 웹 **`/admin/production`**(키 검증 표·Live Check 버튼(과금 경고 명시)·모니터링·비용 검증, Playwright 4종 → 웹 e2e 50) + API 3종 **전부 ADMIN 전용**(조회도 보호 — 결정 1201-⑤와 같은 판단, 설정 지형과 비용이 드러나므로) — core 순수 로직(api-key.ts / cost-verification.ts / production-monitor.ts)+api 어댑터(ProviderProductionService) 분리, 스키마 변경 없음, 라이브 검증(미인증 401·플레이스홀더 키 탐지 및 anthropic 조건부 필수 blocker 2건·Live Check 실패 회수(샌드박스 egress 차단 403)·mock 호출 6건 모니터링 healthy·비용 검증 일치·스모크 13/13 PASS), 해석 확인 CTO_REQUEST #45

## 완료 — Sprint 12 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-1202 — Production Readiness & Deployment** (`95e0fb4`, CTO 승인 — 운영에서 필수 설정 오류가 있으면 **Fail Fast**(기동하지 않음) 확정, 운영 필수 항목은 WEB_URL/DATABASE_URL/Storage(S3)/AUTH_ADMIN_EMAIL/AUTH_ADMIN_PASSWORD 공식 표준 유지하되 **실제 AI Provider 연결 시 Provider API Key 추가**(→TASK-1301에서 이행), 실 Provider Smoke Test와 Backup 확인은 Manual Check 유지, 다음 Sprint에서 CI/CD가 `/health/ready`로 배포 가능 여부 자동 판정, Sprint 12 공식 종료): **Environment Validation**(환경변수 선언 `packages/core/src/ops/env-spec.ts`을 **단일 원천**으로 두고 검증·대시보드·런북이 같은 선언을 사용 — 이름·설명·운영 필수 여부·형식 검사·기본 동작·비밀 여부·운영 권고를 한 곳에 선언. 필수 누락은 운영에서만 error, **형식 오류는 환경 무관 error**(잘못된 값은 어디서도 동작하지 않음), 운영 권고 위반은 warning) + **Startup Validation**(기동 시 검증 후 **운영에서 오류가 있으면 기동 차단·exit 1** — 잘못된 설정으로 뜬 서버가 조용히 오작동하는 것이 더 위험. 개발에서는 경고만 남기고 기동) + **Deployment Checklist**(기계 판정 — 환경·DB·마이그레이션·저장소·관리자 계정·실제 Provider·예산·Failover 자동, 스모크·백업은 정직하게 manual. blocking 실패가 있으면 배포 불가로 판정) + **Configuration Verification**(설정 현황 표 — **비밀 값은 설정 여부만** 노출) + **Health Dashboard**(웹 **`/admin/health`** — 배포 가능 여부·차단 사유·체크리스트·구성 요소·환경 검증 오류/권고·설정 현황, Playwright 4종 → 웹 e2e 46) + **Production Runbook**(docs/operations/production-runbook.md — 배포 전 준비·배포·직후 확인·일상 운영·모니터링 지점·권한 요약) + **Recovery Guide**(docs/operations/recovery-guide.md — 증상별 진입점, 기동 실패/DB/저장소/Provider/예산/실험/설정 미반영/롤백/사고 후 정리) + API(`GET /health` 기존 `OK` 계약 유지(웹 상태 표시가 의존), `GET /health/live` 구조화 생존 확인(무인증·내부 구성 비노출), **`GET /health/ready` ADMIN 전용**(결정 1201-⑤)) + 구성 요소 점검은 **독립 실패**(저장소가 죽어도 DB 상태를 볼 수 있어야 장애 대응이 된다) + StorageService.check() 추가 + **구현 중 발견해 고친 표시 결함**(개발 환경에서 예산·Failover 미설정이 상태는 `통과`인데 설명은 "비용 폭주"·"서비스 중단" 경고 문구여서 상태와 설명이 모순 — 환경별 문구로 분리하고 회귀 테스트 추가) — core 순수 로직(env-spec.ts: validateEnvironment/describeEnvironment, deployment-checklist.ts: buildDeploymentChecklist/summarizeChecklist)+api 어댑터(ReadinessService) 분리, 스키마 변경 없음, 라이브 검증(운영 모드 필수 누락 → 기동 차단 exit 1·정상 기동 시 "환경 검증 통과 (22개 항목)"·s3rver 중단 상태를 저장소 실패로 정확히 탐지하고 배포 불가 판정·복구 후 통과 8/실패 0/직접확인 2로 배포 가능·미인증 401), 해석 확인 CTO_REQUEST #44
- [x] **TASK-1201 — Provider Administration Console** (`893151e`, CTO 승인 — 설정 우선순위 DB Override → Environment Variable → Default 공식 표준 확정(Code-first 유지), 다중 인스턴스 전파는 TTL 기반 유지(Redis Pub/Sub는 후속 Sprint), Routing 규칙(LLM_ROUTE_*)은 현재 Sprint 범위 제외, 배포 시 Override 자동 삭제하지 않고 운영자가 명시적으로 해제, /admin/* 모든 조회와 변경은 ADMIN 인증 요구): 설정 원칙은 **Code-first(환경변수) 유지**하고 콘솔은 그 위의 **오버라이드**로 설계(`유효값 = 오버라이드(DB) ?? 환경변수 ?? 기본값` — 오버라이드가 없으면 기존 동작과 완전히 동일, 해제하면 환경변수로 복귀, 각 값의 **출처**(override/env/default)와 복귀값을 화면에 표시) + **Provider Enable/Disable**(`provider.<name>.enabled` — 끄면 라우팅·실험·Failover 후보에서 제외, **마지막 하나는 끌 수 없음**(호출 전멸 방지, 400)) + **Model Management**(`model.<feature>` — LLM_MODEL_* 보다 우선) + **Budget Management**(`budget.daily`/`monthly`/`alertRatio`) + **Experiment Management**(`experiment.<feature>` — 정의 문자열, 해석 가능한 값만 허용) + **Audit Log**(admin_audit_log — 누가·무엇을·이전→이후, GET /admin/audit) + 검증은 **저장 시점**(알 수 없는 Provider·음수 예산·해석 불가 실험 정의·지원하지 않는 키 → 400. 잘못된 값이 저장되면 다음 호출부터 라우팅이 깨지므로) + 읽기 성능(설정은 LLM 호출마다 참조되므로 DB 왕복 없이 **인메모리 동기 조회**, 기동 시 적재·쓰기 시 즉시 갱신·TTL(`ADMIN_SETTINGS_TTL_MS` 기본 10초) 경과 시 백그라운드 재적재) + **전부 ADMIN 전용**(전역 WriteProtectionGuard는 쓰기만 막으므로 **조회에도 AuthGuard 부착** — 콘솔 GET이 예산·모델 구성과 감사 이력(수행자 이메일)을 노출하기 때문. 라이브 검증 중 미인증 GET이 200으로 열려 있는 것을 발견해 수정하고 회귀 테스트 추가) + 웹 **`/admin/console`**(Provider 토글·모델/예산/실험 입력·출처 배지·변경 이력, Playwright 6종 → 웹 e2e 42) + **CTO 결정 1102-① 이행**(최소 표본 기본 30 유지 + `LLM_EXPERIMENT_MIN_SAMPLES`로 조정 가능) + **CTO 결정 1102-③ 이행**(관측 기간은 **Definition Signature 변경 시에만 초기화**, START/STOP은 기간 유지 — experiment_states에 signature·signatureChangedAt 추가) + 결정 1102-②④는 현행 유지 확정 — core 순수 로직(admin/settings.ts: validateSetting/resolveSetting/enabledProviders)+api 어댑터(AdminSettingsService) 분리, **마이그레이션 24·25**(experiment_signature_window, admin_settings·admin_audit_log), 라이브 검증(모델 오버라이드→라우팅 즉시 반영·openai 비활성→후보에서 제외·예산 10→99 반영·실험 정의 콘솔 설정·가드레일 4종 400·EDITOR 403·미인증 401), 해석 확인 CTO_REQUEST #43

## 완료 — Sprint 11 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-1102 — Experiment Analytics & Recommendation** (`9615770`, CTO 승인 — 최소 표본 기본 30회 유지하되 환경변수 조정 가능하도록 설계(→TASK-1201에서 이행), 추천 우선순위 성공률 → 호출당 비용 → 평균 지연을 공식 표준 확정, 관측 기간은 START/STOP에서 유지하고 Definition Signature 변경 시에만 초기화(→TASK-1201에서 이행), 다중 비교 보정 미도입, Sprint 11 공식 종료): **Variant Performance Summary**(변형별 호출·성공/실패·성공률·평균 지연(호출 수 가중 평균)·비용·호출당 비용·토큰, 성공률에 **Wilson 95% 신뢰구간** 동반 — 적은 표본을 과신해 표시하지 않기 위해. 근거는 배정이 아니라 **실행(Execution)**, 관측 기간은 마지막 START 이후) + **Success Rate / Latency / Cost Comparison**(가중치 최대 변형을 기준(baseline)으로 성공률 %p·지연 ms·호출당 비용 USD 차이와 성공률 신뢰도) + **Winner Recommendation**(품질 우선 — ① 전 변형 최소 30회 미달이면 추천 보류 ② 성공률 1·2위 차이 신뢰도 95% 이상이면 1위 승자(확정) ③ 성공률 동률이면 호출당 비용 ④ 비용도 같으면 지연 ⑤ 어느 축도 차이 없으면 보류) + **Confidence Score**(양측 2-비율 z검정, core 순수 함수 normalCdf(erf 근사)/wilsonInterval/proportionConfidence) + **Analytics Dashboard**(GET /llm/experiments/:feature/analytics + 웹 /experiments 실험 카드 내 추천 배지·근거 문장·기준 대비 비교표, Playwright 2종 → 웹 e2e 36) + **CTO 결정 1101-④ 이행**(권한 분리 — Start/Stop은 EDITOR 이상, **Promote/Rollback은 ADMIN 전용** @RequireRole. Express 5의 path-to-regexp가 `:action(start|stop)` 문법을 지원하지 않아 4개 명시 라우트로 분리) + **CTO 결정 1101-⑤ 이행**(정의 변경 재배정 Audit — experiment_assignment_events에 from/to 변형·서명·사유(DEFINITION_CHANGED/VARIANT_UNAVAILABLE) 기록, Assignment Dashboard에 표시. 최초 배정은 재배정이 아니므로 미기록) + 결정 1101-①②③은 현행 유지 확정 — **마이그레이션 23**(experiment_assignment_events), 라이브 검증(표본 1·3회 시 추천 보류 + Wilson 구간 0.21~1.00로 과신 방지 → 34·102회 축적 후 신뢰도 100% 확정 추천, 정의 변경 재배정 Audit 2건, EDITOR promote/rollback 403·ADMIN 200), 해석 확인 CTO_REQUEST #42
- [x] **TASK-1101 — Sticky Assignment & Experiment Lifecycle** (`5514b33`, CTO 승인 — 상태 레코드 없으면 RUNNING 기본 유지, Sticky Assignment는 Project 기반 공식 표준 확정, Winner Promotion은 운영자 수동 절차 유지, 권한은 Start/Stop EDITOR 이상·Promote/Rollback ADMIN 전용으로 변경(→TASK-1102에서 이행), 정의 변경 재배정 시 Audit 이력(→TASK-1102에서 이행)): **Project 기반 Sticky Assignment**(같은 프로젝트는 항상 같은 변형 — 배정을 저장소가 아닌 **결정적 해시**(FNV-1a + fmix32 최종 혼합)로 정해 저장 실패·재기동·다중 인스턴스와 무관하게 일관. FNV만 쓰면 proj-1/proj-2 같은 순번 키가 뭉쳐 한쪽으로 쏠리는 문제를 혼합 단계로 해결하고 회귀 테스트로 고정. 실험 정의 **서명**(변형=가중치)이 바뀌면 재배정, 이름·종류 변경은 서명 불변(결정 1003-③). projectId는 Content/Analysis/Vision 호출 경로에서 전달, 없으면 기존 무상태 추첨) + **Experiment Start/Stop**(STOPPED는 실험 미적용하고 기존 라우팅으로 처리, 행이 없으면 RUNNING으로 보아 TASK-1003 동작 보존) + **Winner Promotion**(승자 변형으로 전 트래픽, 현재 정의에 없는 변형은 400 거부, 승자 Provider 사용 불가 시 라우팅으로 강등) + **Rollback**(직전 전이의 이전 상태로 복원, ROLLBACK 자체는 대상 제외해 무한 왕복 방지) + **Assignment Dashboard**(GET /llm/experiments/assignments + 웹 /experiments — 상태 배지·조작 버튼·전이 이력·프로젝트별 배정 표, Playwright 4종 → 웹 e2e 34) + 전이 감사(experiment_events — action/from·to 상태/변형/actor/note, 상태 갱신과 한 트랜잭션, 전역 WriteProtectionGuard로 EDITOR 이상·미인증 401) + **CTO 결정 1003-①~④ 확정 반영**(unknown 오류 Failover 제외 공식 표준 / 표기법 `이름\|종류\|변형=가중치` 유지 / kind는 운영 메타데이터로 알고리즘 무영향 / 배정(Assignment)과 실행(Execution) 분리 유지 — 대시보드에 둘 다 표시) — core 순수 로직(experiment-lifecycle.ts: hashToUnitInterval/assignSticky/applyLifecycleAction/resolveLifecycle)+api 어댑터(ExperimentLifecycleService) 분리, **마이그레이션 22**(experiment_states·experiment_events·experiment_assignments), 라이브 검증(같은 프로젝트 6회 연속 동일 변형·서로 다른 프로젝트는 다른 변형·STOP→라우팅(anthropic)·PROMOTE→승자(mock)·잘못된 변형 400·ROLLBACK PROMOTED→STOPPED·미인증 401), 해석 확인 CTO_REQUEST #41

## 완료 — Sprint 10 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-1003 — Routing Experiment & Traffic Control** (`8a6aa57`, CTO 승인 — unknown 오류는 Failover 대상 아님 공식 표준 확정, 실험 표기법 `이름|종류|변형=가중치` 단일 형식 유지, kind는 운영 메타데이터로 Routing 알고리즘에 영향 없음, 배정(Assignment)과 실행(Execution)은 분리하고 Dashboard에 둘 다 유지, Sprint 10 공식 종료): Percentage/A·B/Canary/Weighted Routing(LLM_EXPERIMENT_CONTENT/ANALYSIS/VISION = `이름|종류|변형=가중치,…` — 이름·종류 생략 가능, 가중치 생략 시 균등, 변형은 `provider` 또는 `provider:model`. **4종을 하나의 가중 추첨 원리로 구현**하고 종류는 운영자의 의도 선언으로 대시보드 표시·추정에 사용) + Dynamic 해석(라우팅과 동일하게 호출 시점마다, 재기동 불필요) + Graceful degradation(사용 불가 변형 제외 후 가중치 재정규화, 전부 불가 시 실험 미적용하고 기존 라우팅으로 처리 — 실험 설정이 호출을 실패시키지 않음) + 우선순위(호출자 model > 실험 변형 > 라우팅 규칙 > LLM_MODEL_* > Provider 기본) + Experiment Dashboard(GET /llm/experiments + 웹 **/experiments** — 종류 배지·적용 여부·설정 비율·배정 비율·실제 배정·변형별 실행 지표, Playwright 4종 → 웹 e2e 30) + Experiment Metrics(/executions/stats의 **byVariant** — 변형 `feature→provider:model`별 호출·성공률·지연·토큰·비용, A/B 비교 원천) + **배정 ≠ 실행 구분**(변형이 불건강해지면 Failover가 체인을 재정렬하므로 추첨 결과와 실제 실행이 다를 수 있음 — 대시보드가 두 값을 나란히 표시) + **CTO 결정 1002-① 이행**(Failover 오류 분류 확정 — timeout/server_error/rate_limit/network만 대상, budget/validation/auth(401·403·잘못된 키)/invalid_request/unknown은 제외. 상태코드→오류코드→메시지 순 판정, SDK별 status/statusCode/response.status 모두 인식, 분류명 로그) + **CTO 결정 1002-④ 이행**(GET /llm/health 진단 호출을 attempts/failovers/exhausted/skipped/byProvider에서 분리하고 metrics.healthChecks로 별도 집계 — 건강 상태에는 계속 반영) — core 순수 로직(experiment.ts: parseExperiment/pickVariant/describeExperiment, failover.ts: classifyFailoverError)+api 어댑터(experiment-config.ts) 분리, 스키마 변경 없음, 라이브 검증(canary 80/20 실 배정 12/6·변형별 실행 지표 비교·auth 오류 Failover 미발생·health 계측 분리), 해석 확인 CTO_REQUEST #40
- [x] **TASK-1002 — Provider Failover Engine** (`b1c39bc`, CTO 승인 — Failover 대상 오류 목록 공식 확정(Timeout/5xx/Rate Limit/네트워크만, Budget·Validation·인증(401/403)·잘못된 API Key·잘못된 요청 제외 → TASK-1003에서 이행), Failover 시 전환 Provider 기본 모델 사용 현행 유지, Health 기본값(연속 3회·Cooldown 60초·Half-open) 공식 표준 확정, GET /llm/health를 운영 계측에서 분리 지시 → TASK-1003에서 이행): Provider Priority(LLM_FAILOVER_PRIORITY — 라우팅 Provider가 항상 1순위, 그 뒤 우선순위 순서·중복 제거·사용 불가 제외, **미설정이면 Failover 비활성**으로 기존 동작 보존) + Retry Policy(Provider별 게이트웨이 지수 백오프 LLM_MAX_ATTEMPTS 소진 후 다음 Provider로 전환, 2순위부터는 호출자 model을 버리고 각 Provider 기본 모델 사용 — 모델명은 Provider 간 비호환) + Timeout Policy(LLM_TIMEOUT_MS 기본 120000·0=무제한 — 초과 시 LlmTimeoutError로 전환) + Health Check Integration(ProviderHealthTracker — 연속 실패 LLM_FAILOVER_HEALTH_THRESHOLD(기본 3)회 도달 시 LLM_FAILOVER_HEALTH_COOLDOWN_SEC(기본 60초) 동안 체인 뒤로 밀림(제외 아님), 쿨다운 경과 시 half-open 재시도·성공 시 즉시 회복, GET /llm/health?provider=는 Failover 미사용 진단 경로) + Failover Metrics(GET /llm/failover — 우선순위·타임아웃·Provider별 건강 상태·attempts/failovers/exhausted/skipped·Provider별 성공·실패, 웹 /routing 하단 섹션 + Playwright 2종 → 웹 e2e 26) + **Failover 제외 대상**(예산 초과 429는 markNoFailover로 명시·Execution 미기록, 요청 검증 오류 400 — 둘 다 skipped 집계, CTO 지시) + **이력 Provider 일치**(CTO 결정 1001-③ — AnalysisRecognition.providerName 신설, AnalysisRun.provider·VisionSummary.source가 실제 라우팅/Failover된 Provider `llm:<provider>`를 기록해 Execution과 동일 의미) — core 순수 로직(failover.ts: buildFailoverChain/withTimeout/isFailoverEligible/ProviderHealthTracker)+api 어댑터(failover-config.ts) 분리, 스키마 변경 없음, 라이브 검증(openai 무효키 → mock 전환 성공·AnalysisRun.provider=llm:mock·연속 실패 2회 → 불건강·다음 호출에서 체인 뒤로), 해석 확인 CTO_REQUEST #39
- [x] **TASK-1001 — Cross-Provider Routing Engine** (`03dca0f`, CTO 승인 — Routing 단위는 Feature 3종(Content/Analysis/Vision) 공식 표준 확정(프로젝트별·사용자별은 후속 Sprint), Fallback은 설정 해석 시 기본 Provider 자동 전환 유지·실행 중 오류는 Failover 대상(→TASK-1002에서 이행), 이력 Provider 필드는 실제 Routing된 Provider 기록으로 통일(→TASK-1002에서 이행)): Feature별 Provider Mapping(LLM_ROUTE_CONTENT/ANALYSIS/VISION — `provider` 또는 `provider:model`) + Dynamic Routing(호출 시점 해석 — 재기동 불필요, 사용 불가 Provider는 기본으로 폴백 + 경고 로그, Failover는 CTO 지시로 범위 제외) + Routing Dashboard(GET /llm/routing + 웹 /routing — 결정 근거 배지 feature/default/fallback·폴백 사유·환경변수명) + Routing Metrics(/executions/stats의 byRoute — 경로 feature→provider별 호출·성공률·지연·토큰·비용) + Provider 인스턴스 맵(createLlmProviderMap — 키 설정된 Provider 전부, LlmService 단일 관문에서 경로별 게이트웨이 선택) — 우선순위(호출자 model > 규칙 :model > LLM_MODEL_* 동일 Provider만 > Provider 기본), 라이브 검증(분석→anthropic 실제 호출·기록, vision→gemini 폴백), 해석 확인 CTO_REQUEST #38

## 완료 — Sprint 9 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0903 — Anthropic & Gemini Provider Integration** (`f9cbda5`, CTO 승인 — Anthropic JSON은 System Prompt + JSON Parsing + Retry 방식 공식 표준 확정(Structured Output API는 추후 별도 Sprint 검토), 가격표는 공식 운영 모델만 등록(Preview 모델 제외), Sprint 9 공식 종료): Anthropic 공식 연결(JSON 전용 system 지시 강화 — Claude 4.6+ prefill 400이므로 미사용, 멀티모달 image block, 잘림 방어 stop_reason=max_tokens, 클라이언트 주입) + Gemini 공식 연결(responseMimeType application/json, inlineData 멀티모달, modelVersion 스냅샷 기록 → 접두사 매칭 비용, 잘림 방어 finishReason=MAX_TOKENS) + Provider Factory(Registry 기반 테이블 드리븐 provider.factory.ts — 키 단일 정의·mock 폴백, 새 Provider는 Registry·어댑터·가격표 3곳만) + Unified Execution(claude-opus-5/sonnet-5/haiku-4.5·gemini-2.5-flash 단가 등록, Registry 전 모델 가격표 등록을 테스트가 보증) + Provider Comparison Dashboard(/providers 비교 표 — 호출·성공률·평균 지연·토큰·비용·비용/호출) — 라이브 배선 검증(Anthropic 실 API 401 도달·Gemini mock 폴백), 해석 확인 CTO_REQUEST #37
- [x] **TASK-0902 — Cost Governance & Multi-Provider Foundation** (`5b39867`, CTO 승인 — 예산 정책 공식 표준 확정(80% Alert · 100% 초과 429 차단), Model Routing은 Provider 내부 모델 선택만 지원(Cross-Provider는 Anthropic/Gemini 공식 연결 이후), Budget은 환경변수 Code-first 유지(ADMIN 화면은 후속 Sprint)): Daily/Monthly Budget(LLM_DAILY/MONTHLY_BUDGET_USD, UTC 경계, 미설정=무제한 — 초과 시 LlmService 단일 관문 429 차단·Execution 미기록) + Cost Alert(80% 임계 LLM_BUDGET_ALERT_RATIO — 상태 전이 로그·대시보드 배지, core evaluateBudgetWindow 순수 로직) + Provider Registry(Code-first LLM_PROVIDER_REGISTRY — official/adapter-ready/mock·키 설정 여부만 노출, GET /llm/providers) + Model Routing(LLM_MODEL_CONTENT/ANALYSIS/VISION — 호출자 명시 우선, 선택된 Provider 내 모델 선택) + Provider Dashboard(웹 /providers — Registry·라우팅·예산 카드·Provider별 통계, Playwright 3종 → web e2e 21) + GET /llm/budget — 스키마 변경 없음, 해석 확인 CTO_REQUEST #36
- [x] **TASK-0901 — Real Provider Integration: OpenAI Production** (`8d095e3`, CTO 승인 — 출력 상한 공식 표준(Content 4096/Analysis 2048/Vision 2048, 환경변수 조정 유지), JSON은 length 시 FAILED·Text는 부분 결과 허용 확정, 실 Provider Smoke 정책 확정(운영/스테이징 배포 직후 1회 필수·모델 변경 시·인증/쿠키 변경 시 재실행 — 현재 런북·스크립트가 공식 운영 절차)): feature별 운영 출력 상한(LlmService 단일 관문 — content 4096/analysis 2048/vision 2048, LLM_*_MAX_TOKENS 조정) + JSON 잘림 방어(finish_reason=length+json → 명확한 오류·Execution FAILED) + OpenAI Production 통합 검증(주입 클라이언트로 실 응답 형태 재현 — Content 텍스트/Analysis json_object 엄격 파싱/Vision image_url 첨부/스냅샷 모델 접두사 비용/잘림 FAILED, openai-production.spec.ts 4종) + Production Smoke 10단계 확장(쿠키 전용 운영 모드 로그인 지원·운영 판정: byProvider/byFeature 커버리지·실키 cost>0) + 운영 런북(환경 세트) — 실키 네트워크 검증은 운영/스테이징 스모크 전용(0603 승인 ④), 해석 확인 CTO_REQUEST #35

## 완료 — Sprint 8 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0804 — Login Protection & Security Hardening** (`5ce8a20`, CTO 승인 — 기본 보안 정책 공식 표준 확정(Rate Limit 30회/60초·Lockout 5회/15분·Password 8자+영문+숫자, 환경변수 조정 유지), Rate Limit 인메모리 유지(다중 인스턴스 시 Redis 확장), Unlock 버튼 미구현(ADMIN Reset이 해제 절차), Idle Timeout 미구현(절대 TTL만), Sprint 8 공식 종료): Rate Limit(이메일 키 슬라이딩 윈도우 → 429, AUTH_LOGIN_MAX_ATTEMPTS/WINDOW_SEC 기본 30/60초) + Account Lockout(연속 5회 실패 → 15분 DB 잠금 users.lockedUntil, 성공 시 초기화·ADMIN 재설정 시 즉시 해제 — 마이그레이션 21) + Password Complexity(8자+영문+숫자, core 단일 정의 — 생성/변경/재설정 공통) + Failed Login Audit(LOGIN_FAILED·ACCOUNT_LOCKED, 액션 8종 + /admin/users 잠김 배지) + Cookie 전용 운영 모드(AUTH_COOKIE_ONLY — 운영/스테이징 기본, 본문 토큰 제외·쿠키만, 웹 credentials include) + Session Timeout(AUTH_SESSION_TTL_HOURS 기본 168h — 세션·쿠키 수명 통일) + Playwright 3종(웹 e2e 18종) — 해석 확인 CTO_REQUEST #34
- [x] **TASK-0803 — Password Management & Operational Security** (`40bd4d7`, CTO 승인 — 개발은 본문 토큰+httpOnly 쿠키 병행 유지·운영은 쿠키 전용 전환(→0804에서 이행), Reset은 ADMIN 직접 지정 유지(메일 기반은 메일 인프라 도입 후) 확정): 비밀번호 변경(PATCH /auth/password — 본인 확인·현재 세션 외 폐기·감사 PASSWORD_CHANGED) + 비밀번호 재설정(POST /auth/users/:id/password-reset, ADMIN — 대상 전 세션 폐기·자기 자신 불가·감사 PASSWORD_RESET) + httpOnly/Secure/SameSite 세션 쿠키(로그인 발급·Bearer→쿠키 순 인식·로그아웃 만료, AUTH_COOKIE_SECURE/AUTH_COOKIE_SAMESITE, core 순수 로직) + /llm/health 운영/스테이징 EDITOR+ 보호(AUTH_PROTECT_HEALTH, CTO 결정 0802-③) + /account UI·/admin/users 재설정 인라인(Playwright 3종, 웹 e2e 15종) — 스키마 변경 없음, 해석 확인 CTO_REQUEST #33
- [x] **TASK-0802 — User Management UI & Full Write Protection** (`3383e12`, CTO 승인 — @Public 예외 3종(Login·Company Brain Query·READY Validation)만 유지·새 쓰기 API 기본 보호, 권한 정책(Write EDITOR+/User Mgmt ADMIN/Logout VIEWER+/GET 유지) 확정, /llm/health는 운영/스테이징 EDITOR+ 인증(개발 비보호 가능)→0803에서 이행): 전면 쓰기 보호(WriteProtectionGuard APP_GUARD — 모든 POST/PATCH/PUT/DELETE 인증, 기본 EDITOR+, 읽기 성격 POST는 @Public, 조회 GET 비보호 유지) + 사용자 관리(목록/생성/역할 변경/비활성화 — 자기 자신 불가, 비활성화 시 세션 즉시 폐기) + Audit 확장(user_audit_log 4종 액션) + /admin/users UI(Playwright 3종, 웹 e2e 12종) + 웹 쓰기 호출 토큰 첨부·스모크 로그인 단계 — 해석 확인 CTO_REQUEST #32
- [x] **TASK-0801 — Authentication & Authorization Foundation** (`400b4ae`, CTO 승인 — Foundation 적용 범위(발행 전이·사용자 관리) 확정→0802에서 전면 확대 지시, 역할 3종 공식 표준, 토큰은 개발 localStorage·운영 httpOnly/Secure/SameSite 쿠키 전환 확정): User Entity(역할 3종)+DB 세션(Bearer 256비트, 7일)+scrypt 해시(@acos/core)+RBAC(AuthGuard/@RequireRole 계층 비교 — 발행 전이 EDITOR+, 사용자 생성 ADMIN)+Actor Audit(content_status_history.actor에 수행자 이메일)+Login UI(/login, localStorage 토큰, Playwright 3종) — 관리자 부트스트랩(AUTH_ADMIN_*), 적용 범위는 발행 전이·사용자 관리(전면 강제는 CTO 결정 대기), 해석 확인 CTO_REQUEST #31

## 완료 — Sprint 7 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0704 — Publishing Web UI & Audit History** (`dcc4817`, CTO 승인 — Audit는 from/to/timestamp 공식 표준(Actor는 인증 도입 후 추가→0801에서 이행), Publishing UI는 프로젝트 상세 유지 확정): 감사 이력(content_status_history — 전이 1건당 1레코드, 전이와 한 트랜잭션, GET …/contents/:id/history 최신순) + publishedAt 최초 발행 시점 보존 반영(0703 승인 ②) + 발행 Web UI(상태별 색상 Status Badge·allowedTransitions 기반 상태 변경 버튼·publishedAt UTC 표기·감사 이력 목록) + Playwright e2e 2종(전 구간 전이·되돌리기, 스텁 CORS) — 해석 확인 CTO_REQUEST #30
- [x] **TASK-0703 — Real Provider Smoke & Publishing Pipeline** (`1c28a52`, CTO 승인 — 전이 규칙 공식 표준 확정, publishedAt 최초 발행 시점 보존, 실키 스모크는 운영/스테이징 전용(개발은 mock 리허설만) 확정): 발행 파이프라인(PATCH /projects/:id/contents/:contentId/status — DRAFT→REVIEW→PUBLISHED→ARCHIVED, 전이 규칙 @acos/core canTransition 공식 사용, PUBLISHED는 isPublishable 검증+publishedAt 기록, 데이터 보존 마이그레이션) + 운영 스모크 테스트(scripts/real-provider-smoke.mjs 8단계 자동 판정 + docs/operations 절차 문서, mock 리허설 8/8 PASS — 실키 수행은 운영/스테이징) — 해석 확인 CTO_REQUEST #29
- [x] **TASK-0702 — Dashboard Filter & Web Testing** (`1f9ab8b`, CTO 승인 — stats 필터 공식 API·hour 31일 정책·자유 입력 유지(자동완성 후속)·Playwright 공식 게이트(모든 웹 기능 통과 필수) 확정): 대시보드 필터(Feature/Provider/Model/From/To UTC, GET 폼, stats+timeline 적용, interval 전환 시 유지) + Stats API 필터 확장(feature/provider/model) + hour 조회 최대 31일 제한(from 미지정 시 최근 31일 창, 초과 400) + Playwright CI 품질 게이트(web pnpm test — Dashboard/Filter/Empty/Error 스모크 4종, 모드 전환형 스텁 API, 사전 설치 chromium) — 해석 확인 CTO_REQUEST #28
- [x] **TASK-0701 — Execution Dashboard Web UI** (`eb202c6`, CTO 승인 — CSS 차트 유지(외부 라이브러리 미도입)·필터 추가·hour 31일 제한·Playwright CI 게이트 지시 → TASK-0702로 이행): /executions 운영 대시보드 — Stats/Timeline API 소비, KPI 카드 4종(성공률은 UI에서 % 표시), Timeline Chart(hour/day/week 전환·성공/실패 스택 막대·UTC 축·빈 버킷 UI 보간), Feature/Provider/Model 통계 테이블, 서버 컴포넌트+CSS 차트(외부 라이브러리 없음), API 미연결 안내 — 브라우저(Playwright) 검증, 해석 확인 CTO_REQUEST #27

## 완료 — Sprint 6 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0605 — Execution Timeline** (`7dbb42c`, CTO 승인 — UTC·ISO Week·hour/day/week 공식 표준, 빈 버킷은 UI 보간, hour 기간 제한은 다음 Sprint 확정): GET /executions/timeline?interval=hour|day|week — 호출 수·성공/실패율·토큰·비용·지연을 시간 버킷(UTC date_trunc, 오름차순, 데이터 있는 버킷만)으로 제공, feature/provider/model 정확 일치 + from/to 필터, DB (버킷,status) $queryRaw 집계(전 값 바인딩·interval 화이트리스트) → core buildExecutionTimeline 병합, 잘못된 interval/날짜 400 — 해석 확인 CTO_REQUEST #26
- [x] **TASK-0604 — Image Guard & Preprocessing** (`6c00dfb`, CTO 승인 — 기본 정책(20MB/1024px/5MB/JPEG q82)·위반 스킵 후 계속·업로드 원본 무변경(호출 시점 전처리) 확정): Vision Provider 호출 전 이미지 검증(MIME 허용 목록·원본 20MB·빈 파일)·리사이즈(최대 변 1024px, 비율 유지·확대 없음)·최적화(JPEG q82, 투명 PNG 유지)·EXIF 제거(Orientation은 픽셀 반영 후 삭제)·출력 5MB 제한 — core Port(ImageGuardPolicy/ImagePreprocessor) + sharp 어댑터(apps/api), 위반 이미지는 스킵(분석 계속, raw.skippedImages 기록)·인프라 오류는 null 폴백 유지, VISION_IMAGE_* 환경변수 조정 — 해석 확인 CTO_REQUEST #25
- [x] **TASK-0603 — Provider Integration (OpenAI)** (`34d1426`, CTO 승인 — 가격표 Code-first·접두사 매칭·cost null 유지, json_object 공식 구현, Health(실 ping+Execution 기록) 유지, 실키 검증은 운영/스테이징 확정): OpenAI Provider 공식 연결 — API Key(OPENAI_API_KEY, 없으면 mock 폴백), Health Check(GET /llm/health, 실호출 기반·Execution dev 기록), responseFormat→response_format json_object 매핑, Multimodal(image_url, 클라이언트 주입 단위 검증), Execution Cost(gpt-4o/$2.5·$10, gpt-4o-mini 단가 등록 + 스냅샷 모델 최장 접두사 매칭) — 실키 호출은 샌드박스 egress 제한으로 미검증(무효 키 배선 검증 완료), 해석 확인 CTO_REQUEST #24
- [x] **TASK-0602 — Execution Dashboard** (`162f165`, CTO 승인 — 지표 구성 유지·비율 0~1 API 반환(%는 UI)·from/to 공식 채택·화면은 다음 Sprint·일별 시계열은 Sprint 6 후반 확정): GET /executions/stats?from=&to= — 호출 수·성공률·실패율·토큰·비용(USD)·지연(가중 평균·최대)을 전체(totals) + feature/provider/model별로 집계, DB (차원,status) groupBy → @acos/core 순수 병합 로직(buildExecutionStats, DB 없이 단위 테스트), 기간 필터·날짜 검증 400, DB 변경 없음 — 해석 확인 CTO_REQUEST #23
- [x] **TASK-0601 — Execution Domain** (`fcbf6ce`, CTO 승인 — feature 4종·400 비기록·본문 비저장·가격표 Code-first·미등록 모델 cost null·FK 없는 독립 도메인 확정): 모든 LLM 호출(Content·Analysis·Vision·개발용)을 호출 1건당 Execution 1건으로 기록 — Execution 모델(feature/provider/model/token/cost USD/latencyMs/status/error) + 마이그레이션, ExecutionTracker(@acos/core, 기록 실패는 호출 미실패), 기록 지점은 LlmService.complete 단일화(feature 태깅), 비용은 코드 선언 가격표(mock 0, 미등록 모델 null), GET /executions 조회 API — 해석 확인 CTO_REQUEST #22

## 완료 — Sprint 5 (Goal: AI Execution — CTO 공식 종료, 2026-07-28)

- [x] **TASK-0506 — Legacy Generator Integration** (`574523a`, CTO 승인 — Wrapper 구조 유지·CONTENT_GENERATOR 제거 유지·Deprecated Generator는 Sprint 6 이후 제거 검토 확정): Deprecated 구 Generator 경로(POST /projects/:id/contents)를 제거하지 않고 EngineContentGenerator(Wrapper)를 통해 공식 Content Generation Engine 호출로 통합 — API 계약 유지, generateMarkdown() 생성 코어 공용화(두 경로 본문 동일 검증), CONTENT_GENERATOR 환경 변수 제거(MockContentGenerator는 @deprecated 보존), 0505 승인 결정 반영(VISION_MAX_IMAGES 환경 변수화) — 해석 확인 CTO_REQUEST #21
- [x] **TASK-0505 — Vision Multimodal Integration** (`d63a975`, CTO 승인 — 이미지 상한 유지+환경변수화·Mock Echo 공식 적용·VISION_PROVIDER 제거 유지·용량 제한은 실연결 전 구현 확정): 구 MockVisionProvider를 LLM 기반 멀티모달 공식 엔진(LlmVisionProvider)으로 교체 — Image Bytes(base64, 최대 5장) + Prompt Engine("vision-analysis" 템플릿) + LLM Gateway(images 멀티모달 확장 + 어댑터 3종 이미지 매핑) + Company Brain 사용, 응답 엄격 파싱(실패 시 재시도→null 폴백 유지), VisionSummary 모델·저장 위치 변경 없음, VISION_PROVIDER 환경 변수 제거(LLM_PROVIDER로 일원화) — 해석 확인 CTO_REQUEST #20
- [x] **TASK-0504 — Analysis Engine Integration** (`74e9fbb`, CTO 승인 — Mock JSON Echo 공식 전략·responseFormat 유지·ANALYSIS_PROVIDER 제거 유지 확정): 구 MockAnalysisProvider를 LLM 기반 공식 엔진(LlmAnalysisProvider)으로 교체 — Prompt Engine("product-analysis" 템플릿, 규칙 기반 초안 JSON 포함) + LLM Gateway(responseFormat "json") + Company Brain(상품 이름 기준 PROJECT 스코프) 사용, 응답 엄격 파싱(실패 시 재시도→FAILED), AnalysisResult 모델·API·1:N 이력 변경 없음, ANALYSIS_PROVIDER 환경 변수 제거(LLM_PROVIDER로 일원화) — 해석 확인 CTO_REQUEST #19
- [x] **TASK-0503 — Prompt Engine** (`9f44b87`): PromptTemplate+PromptEngine(@acos/core, 선언적 템플릿 레지스트리·결정적 렌더링), 상세페이지 프롬프트를 content-generation 템플릿으로 분리, 모든 AI 기능 공용 설계(등록→render→LLM Gateway), GET /prompt/templates — 0502 승인 결정 반영(웹 버튼 엔진 전환, 구 경로 Deprecated), 해석 확인 CTO_REQUEST #18
- [x] **TASK-0502 — Content Generation Engine** (`e147aef`): READY ProductObject + Company Brain(지식/결정/설정/금지어) + LLM Gateway로 Markdown 상세페이지 생성 → Content 저장, POST /projects/:id/contents/generate (구 mock 경로는 보존) — 해석 확인 CTO_REQUEST #17
- [x] **TASK-0501 — LLM Gateway Foundation** (`9a5a3e2`): LlmProvider Port + LlmGateway(검증·재시도, @acos/core) + MockLlmProvider 기본, Provider 어댑터 3종(OpenAI·Anthropic·Gemini, 공식 SDK), LLM_PROVIDER 환경변수 교체(키 미설정 시 mock 폴백), GET /llm · POST /llm/complete — 해석 확인 CTO_REQUEST #16

## 완료 — Sprint 4 (Goal: Company Brain Integration — CTO 공식 종료, 2026-07-27)

- [x] **TASK-0404 — READY Validation Engine** (`817ead4`): CompanyBrainService로 Knowledge/Memory/Decision/SOP를 읽어 READY 전환 가능 여부 판정 — 검사 6종, PASS/WARNING/FAIL 3단계(전체=최악 값), 금지어는 Memory(GLOBAL, banned-words) 기반, POST /projects/:id/ready-validation — 해석 확인 CTO_REQUEST #15

- [x] **TASK-0403 — Company Brain Query Service** (`7c2264a`): CompanyBrainService(읽기 전용, 조회 순서 Memory→Knowledge→Decision→SOP 고정), POST /company-brain/query({query, scope?, scopeId?, limit?} → 고정 순서 4개 섹션), 소스별 부분 일치 매칭 — 해석 확인 CTO_REQUEST #14
- [x] **TASK-0402 — Structured Memory Foundation** (`ce37a80`, CTO 승인 · scope Enum+규칙 반영 `2afd089`): 표준 Memory를 scope(GLOBAL/COMPANY/PROJECT/PRODUCT Enum)/scopeId(PROJECT·PRODUCT 실존 검증)/key/value(Json)/description 구조화 저장소로 재정의, (scope,scopeId,key) 유니크, CRUD API /memory — ProjectMemory는 사람용 메모·작업기록으로 유지
- [x] **TASK-0401 — Knowledge Foundation** (`0e028ec`, CTO 승인 · category Enum 반영 `d8e24d0`): Knowledge 엔티티(title/content/category?) + KnowledgeRepository Port(@acos/core, Company Brain 4번째 축), 회사 전역 확정, category Enum 8종(RULE/POLICY/GUIDE/BRAND/LEGAL/QUALITY/FAQ/OTHER), CRUD API /knowledge

## 제안 (스펙 대기 — 구현하지 않음)

- [ ] 실제 Provider/Generator 연결 (OCR/Analysis/Vision/Content 중 CTO 지정 — API 키·모델 스펙 필요, CTO_REQUEST #6)
- [ ] Content 채널별 포맷/배포 (발행 파이프라인 자체는 TASK-0703으로 구현됨)

## 완료 — Sprint 3 (CTO 최종 승인, 2026-07-27)

- [x] **TASK-0307 — Memory Engine Foundation** (`86cef6c`): Memory 엔티티+MemoryStore Port+MemoryEngine(remember/recall/revise/forget, @acos/core Company Brain), Prisma 어댑터, CRUD API /projects/:id/memories — Workflow Engine은 사용 가능하되 소유하지 않음(미연결, CTO 지시), 해석 확인 CTO_REQUEST #11
- [x] **TASK-0306 — Decision Log Foundation** (`a36c1b0`, CTO 승인 · Enum 반영 `7864c91`): Decision 엔티티(id/title/description/reason/decisionType/author/projectId/createdAt) + DecisionRepository Port(@acos/core, Company Brain), Prisma 어댑터, CRUD API /projects/:id/decisions, decisionType Enum 8종 고정(CTO 결정), docs/architecture/decision.md
- [x] **TASK-0305 — SOP Engine Foundation** (`5c67d54`, CTO 조건부 승인 반영): Company Brain의 SOP 정의(SopDefinition) + Execution Layer의 WorkflowEngine(@acos/core, 순차 실행·실패 시 후속 SKIPPED·단계 간 출력 전달), 기본 SOP product-content(OCR→조립→READY 검수→상세페이지, 기존 서비스 재사용), SopRun 실행 이력(Project 1:N), POST/GET /projects/:id/sop-runs
- [x] **TASK-0302 — Product Object 상태 전이** (`9374aae`): DRAFT⇄READY, →ARCHIVED(종결) 전이 규칙(@acos/core) + READY 필수 조건 검증(제목+OCR/Vision 요약), PATCH …/product-object/:version/status
- [x] **TASK-0303 — 상세페이지 콘텐츠 파이프라인** (`3a7fb00`): ContentGenerator Port + MockContentGenerator(Markdown), Content 모델 Project/ProductObject 소속 재구성, READY Product Object에서만 생성, /projects/:id/contents API
- [x] **TASK-0304 — 웹 UI Project 반영** (`0dd85bf`): /projects 목록·상세, 파이프라인 실행 버튼(조립→READY→상세페이지), 홈 내비게이션

## 완료 — Sprint 1~2

- [x] **Sprint 1 — Foundation** (`4048d62`): pnpm+Turborepo 모노레포, Next.js 16(web:3000) / NestJS 11(api:4000, /health), Docker Compose(PostgreSQL·Redis·MinIO), Prisma 초기 설정, web+api 동시 실행
- [x] **TASK-0201 — 사진 업로드** (`12bb3ae`): 드래그 앤 드롭·다중·진행률 UI, MinIO 저장, `Image` 모델, 업로드 API, 오류 처리
- [x] **TASK-0202 — OCR Foundation Architecture** (`4cf6882`): OCR 도메인 @acos/core 이전(Port/Adapter), MockOCRProvider 기본, Image:OCRResult 1:N 이력, PENDING/RUNNING/SUCCESS/FAILED, Jest 도입, docs/architecture/ocr.md
- [x] **TASK-0203 — Product Object Foundation** (`de5442d`): ProductObject 모델(버전 관리, DRAFT/READY/ARCHIVED), ProductObjectBuilder(OCR+Vision mock 조립), /projects/:id/product-object API, JSON Schema, docs/architecture/product-object.md
- [x] **(자체정의) Product CRUD + 웹 플로우** (`d842338`): /products CRUD, 업로드→Product 생성→목록/상세 웹 페이지 — CTO_REQUEST #4 처리 방침 대기
- [x] **(자체정의) TASK-0204 — AI Analysis Foundation** (`d49157a`): AnalysisProvider 교체 구조, Mock 기본, apply 옵션, docs/architecture/analysis.md
- [x] **(운영) AGENTS 협업 규칙 + CTO 보고 체계** (`81aab6f`, `20c87f0`): AGENTS.md, reports/CTO_REPORT.md·CTO_REQUEST.md
- [x] **TASK-0301 — Project Domain Foundation** (`55130af`): Project 최상위 루트 엔티티 도입(CTO_REQUEST #1 결정 반영), Project 1:N Product, ProductObject.projectId → projects.id 재지정, 데이터 보존 백필 마이그레이션, Projects CRUD API, 조립 시 프로젝트 전체 상품 집계
- [x] **TASK-0205 — Vision Provider Foundation** (`4fb0cf4`): VisionProvider Port + MockVisionProvider(@acos/core), VISION_PROVIDER 환경변수 선택, Product Object 조립에 주입(실패 시 null 폴백), docs/architecture/vision.md — CTO "다음" 지시로 제안 최상단 항목 승격
