# TASKS — AI Product Content OS 작업 대장

> AGENTS.md와 함께 작업의 기준이 되는 문서다.
> - CTO(아키텍트)가 TASK 스펙을 이 문서(또는 채팅 지시)로 내려주면 **미완료** 섹션에 등록된다.
> - "다음 진행해" 지시 시 **미완료 섹션의 최상단 TASK**를 구현한다.
> - 새로운 기능을 임의로 추가하지 않는다 — 스펙 없는 항목은 "제안(스펙 대기)"에만 둔다.
> - TASK 완료 = 품질 게이트(Build/Test/TS/ESLint) 통과 + `/reports/CTO_REPORT.md` 갱신.

## 미완료 (스펙 확정, 구현 대기)

- (없음 — TASK-1601 완료. CTO 리뷰/승인 대기 중이며 승인 전 다음 TASK를 시작하지 않는다)

## 완료 — Sprint 16

- [x] **TASK-1601 — Production Verification & Operational Readiness**: "잘 돌고 있는가"에서 **"지금 무너지면 되살릴 수 있는가"** 로 넘어간다. **Real SMTP Validation**(`POST /ops/notifications/verify-smtp` — 실제 SMTP에 연결·인증까지 하되 **메일은 보내지 않는다**(점검이 수신함을 채우면 사람이 점검 메일을 무시하게 된다). 미설정은 실패가 아니라 **미구성**으로 알리고, 결과에 `ALERT_EMAIL_TO`를 담지 않는다 — 수신자 주소는 비밀이다. **이 세션에서 처음으로 살아 있는 SMTP 서버와 실제 EHLO→AUTH→QUIT를 주고받아 검증했다** — TASK-1401부터 남아 있던 "메일 경로 미검증" 부채를 갚았다) + **Backup Automation**(`pg_dump --format=custom`, 매일 `OPS_CHECK_BACKUP_AT` 기본 03:00 로컬. **크기를 기록한다** — pg_dump는 부분 실패에도 0에 가까운 파일을 남길 수 있고 그걸 성공으로 세면 복구 계획이 통째로 거짓이 된다(1KB 미만은 `failed`). 보존 기간이 지나도 **최근 `BACKUP_KEEP_MINIMUM`개는 남긴다** — 정리가 마지막 백업을 지우면 안 된다. 실패해도 예외를 던지지 않고 이력에 남긴다. 화면·API에는 **파일명만** 노출한다) + **Restore Verification**(매일 `OPS_CHECK_RESTORE_AT` 기본 03:30 로컬, 최신 덤프를 **별도 DB**(`BACKUP_RESTORE_DB_URL`)에 복원하고 테이블 수를 센다. **운영 DB에는 절대 복원하지 않는다** — 검증하려다 데이터를 잃는 것이 최악이다. 대상이 없으면 `configured:false`, **미구성과 실패는 다르다**. `pg_restore`는 무해한 경고에도 비영점 종료하므로 판정은 종료 코드가 아니라 **복원 후 테이블 수**로 한다. **복원해 보지 않은 백업은 백업이 아니다** — 이력이 없으면 `missing`이고 그것은 통과가 아니다) + **Disaster Recovery Checklist**(배포 체크리스트(1202)와 목적이 다르다 — 그쪽은 "지금 배포해도 되는가", 이쪽은 "지금 무너지면 되살릴 수 있는가". `복구 가능`은 백업·복원·DB·저장소에 실패가 없을 때만 true이고, Redis·경보 채널은 복구 가능성을 좌우하지 않는다. 복구 절차 숙지·연락 체계는 자동 판정이 불가능하므로 **`manual`로 남긴다** — 모르는 것을 통과로 처리하지 않는다) + **Provider Smoke Automation**(예약 자리(`OPS_CHECK_SMOKE_AT`)는 있지만 **기본은 꺼져 있다** — 실제 과금되기 때문(CTO 결정 1301-①). "지금 점검"(`runAll`)도 **꺼진 작업은 건너뛴다**) + **Operations Dashboard / Redis Health Dashboard**(웹 `/admin/operations` ADMIN 전용 — 체크리스트·백업 이력·복원 이력·Redis·SMTP, "지금 백업"·"지금 복원 검증"·"메일 경로 확인" 버튼, Playwright 8종 → 웹 e2e 69) + **CTO 결정 1501-①~④ 반영**(① 일 1회 점검을 **운영 서버 로컬 시각**으로(`TZ`) ② Redis 장애 시 **LLM 호출은 계속 허용**, Scheduler만 중단, 30분(`OPS_LOCK_OUTAGE_THRESHOLD_MS`) 이상 지속되면 Critical 반복 ③ **Dead Letter도 지우지 않고** 90일 후 `ARCHIVED` ④ Grace Factor 기본 3배 유지, `OPS_SCHEDULER_GRACE_<JOB>`으로 Job별 조정) — core 순수 로직(`disaster-recovery.ts`: judgeBackup/judgeRestore/buildDisasterRecoveryChecklist/summarizeDisasterRecovery, `alerts.ts`: detectLockOutageAlert, `schedule.ts`: 로컬 시각 판정·resolveGraceFactor)+api 어댑터(`BackupService`) 분리, **마이그레이션 29**(backup_runs·restore_runs·notification_queue.archivedAt·NotificationQueueStatus.ARCHIVED), 새 문서 `docs/operations/disaster-recovery.md`, 라이브 검증(**실 PostgreSQL + 실 Redis + 실 SMTP + 실 pg_dump/pg_restore** — 백업 4회 성공(수동 1·예약 3, 96~98KB)·복원 검증 4회 모두 **테이블 30개 확인**·초기 상태는 `recoverable:false`(백업·복원 이력 없음)에서 백업·복원 후 `true`로 전환·SMTP 실연결 검증·**Redis 중단 중 LLM 호출 HTTP 200**이고 예약만 중단되며 Critical 경보 발생, 복구 시 RESOLVED, 재중단 시 재발생), **자체 발견 결함 4건 수정**(runAll이 꺼진 과금 작업을 실행 / Prisma `?schema=public`을 pg_dump가 거부 / 기동 로그·화면이 "04:00 UTC"라고 표시(실제는 로컬) / 경보 문구의 마크다운 `**`가 그대로 노출), 해석 확인 CTO_REQUEST #49

## 완료 — Sprint 15 (Goal: High Availability & Operations Reliability — CTO 공식 종료, 2026-07-29)

- [x] **TASK-1501 — High Availability & Operations Reliability**: TASK-1401이 남긴 부채 두 건(알림 큐잉·점검 정지 감지)을 갚고, 보관을 사람 손에서 떼어 낸다. **Scheduler Stopped Alert**(CTO 결정 1401-① — Redis 장애 시 **단일 모드로 자동 폴백하지 않는다**(폴백하면 여러 인스턴스가 동시에 점검을 돌린다). 대신 멈춘 사실을 critical로 알린다. **감시자는 잠금 없이 돈다** — 멈춘 원인이 대개 잠금을 못 잡는 것이라 감시까지 잠금을 요구하면 정작 알려야 할 때 알리지 못한다(여러 인스턴스가 동시에 감지해도 경보 `key`가 같아 중복되지 않는다). 판정은 **관대하게** — 간격의 3배(`OPS_SCHEDULER_GRACE_FACTOR`)를 넘겨야 멈춘 것으로 본다(한 번 늦었다고 경보하면 사람이 무시하게 된다). 한 번도 안 돌았으면 **기동 시점부터** 센다(방금 뜬 서버를 장애라 하지 않는다). 원인이 잠금이면 그렇게 적는다) + **Persistent Notification Queue / Retry Worker / Dead Letter Queue**(CTO 결정 1401-② — TASK-1401의 재시도는 프로세스 안에서만 돌아 재시작하면 사라졌다. 이제 경보는 `notification_queue`에 담기고 워커가 꺼내 보낸다 — **재시작을 견딘다**. **워커는 리더만 돌린다**(여러 인스턴스가 같은 항목을 집으면 같은 알림이 여러 번 간다), 잠금을 못 잡으면 조용히 넘긴다(정상 동작이라 소음이 될 이유가 없다). **Dead Letter로 가는 경우는 둘** — 되돌릴 수 없는 실패(4xx)와 최대 시도 소진. **지우지 않고** `requeue`로 시도 횟수를 되돌려 다시 보낸다(설정을 고친 뒤 쓰는 절차). 큐 적재 실패는 알림이 **아예 사라지는** 것이라 로그로 크게 남긴다) + **Alert Archive 예약화**(CTO 결정 1401-③ — 네 번째 예약 점검 `alert-archive`로 편입, **매일 04:00 UTC**(`OPS_CHECK_ARCHIVE_AT`). 시각 기반 점검을 표현하려고 스케줄러를 **단일 티커**로 재구성 — 점검마다 타이머를 두면 "매일 몇 시"를 표현할 수 없고 재기동할 때마다 시점이 밀린다. **그 시각 전에는 한 번도 안 돌았어도 돌지 않는다**("새벽에 돌리라"는 지시를 기동 시점에 어기지 않기 위해). 보관은 경보를 만들지 않는다 — 정리 작업이다) + **운영 기본 채널 정책**(CTO 결정 1401-④ — Slack Warning 이상 / **Email Critical 이상** / Webhook Warning 이상 / 해소 포함. 메일만 Critical인 이유는 메일이 지우기 번거롭고 쌓이면 읽지 않게 되기 때문. `ALERT_*_MIN_LEVEL`·`ALERT_*_RESOLVED`로 전부 변경 가능하고 알 수 없는 값은 기본값으로 되돌린다) + 웹 `/admin/production` 알림 큐 섹션(대기·전송 완료·Dead Letter·워커 상태·"지금 보내기"·"실패분 다시 보내기", 잠금 불가 시 "예약 점검이 돌지 않습니다" 표시, Playwright 3종 → 웹 e2e 60) — core 순수 로직(schedule.ts: parseDailyAt/shouldRun 시각 기반/isSchedulerStopped, alerts.ts: detectSchedulerAlerts, notification.ts: resolveChannelPolicy/decideQueueOutcome/isDue/summarizeQueue)+api 어댑터(NotificationQueueService) 분리, **마이그레이션 28**(notification_queue + NotificationQueueStatus), 라이브 검증(**실 Redis + 실 DB** — 큐 적재 후 워커가 503 두 번을 넘겨 2회째에 SENT · **워커를 끈 채 경보 발생 → 프로세스 강제 종료 → 재시작하니 큐에서 꺼내 전송(재시작 생존)** · 404 주소는 1회 시도 후 DEAD이고 requeue로 attempts 0 복귀 · **Redis 중단 시 예약 실행 0회(자동 폴백 없음)이고 정지 경보 CRITICAL 발생, Redis 복구 시 점검 재개·경보 RESOLVED** · 보관 점검이 "삭제하지 않습니다"로 실행), 해석 확인 CTO_REQUEST #48

## 완료 — Sprint 14 (CTO 공식 종료, 2026-07-29)

- [x] **TASK-1401 — Production Operations Platform** (`859886a`, CTO 승인 — Redis 장애 시 **단일 모드 자동 폴백 금지**하고 대신 Scheduler Stopped Alert 구현(→TASK-1501에서 이행), **Persistent Notification Queue**(Retry Worker + Dead Letter Queue) 구현(→TASK-1501에서 이행), **Archive를 Scheduler에 편입**하고 기본 하루 1회 새벽 실행(→TASK-1501에서 이행), 운영 기본 채널 정책(Slack Warning 이상 / Email Critical 이상 / Webhook Warning 이상 / Resolve 포함)을 환경변수로 변경 가능하게 유지(→TASK-1501에서 이행), Sprint 14 공식 종료): TASK-1302가 남긴 두 부채를 갚는다 — **예약 점검의 인스턴스별 중복 실행**과 **알림이 한 번 실패하면 아무도 모르는 채로 끝나던** 문제. **Distributed Scheduler / Redis Leader Election / Distributed Lock**(CTO 결정 1302-② — 예약 실행은 `acos:lock:scheduler:<job>` 임차를 잡은 인스턴스만 수행하고 **수동 실행은 잠금을 요구하지 않는다**(사람이 지금 확인하려는 것). **임차는 반드시 만료된다**(`OPS_LOCK_TTL_MS` 기본 30초) — TTL 없는 락은 리더가 죽으면 영원히 잠기고 그것이 가장 나쁜 실패다. **만료 직전이 아니라 TTL의 1/3 지점에서 미리 갱신**(만료 직전 갱신은 네트워크 지연 한 번에 리더십을 잃는다), **소유자 확인 Lua 스크립트로만 갱신·해제**(비교 없이 지우면 남의 임차를 해제해 리더가 둘이 된다), **Redis가 끊기면 잠그지 못한 것으로 본다**(모르는 채 "잡았다"고 하면 중복 실행이 조용히 일어난다). `REDIS_URL` 미설정 시 단일 인스턴스 모드이고 **그 사실을 API·화면에 드러낸다**) + **Notification Center — Slack / Email / Webhook**(채널마다 최소 심각도와 해소 알림 여부를 따로 정한다 — 모든 warning을 밤중에 받으면 사람이 알림을 끈다. **해소는 심각도와 별도 스위치**(critical만 받는 사람도 해소는 받고 싶을 수 있다), **채널 하나가 죽어도 나머지는 보낸다**(알림 체계가 단일 장애점이 되면 안 된다), **주소는 어떤 응답에도 담지 않는다**) + **Webhook Retry**(지수 백오프 1s→2s→4s 상한 30초, 최대 `ALERT_RETRY_MAX_ATTEMPTS` 기본 4회(최초 포함). **4xx는 재시도하지 않는다**(429 제외 — 같은 요청은 같은 답을 받는다. 매달려 있어 봐야 로그만 늘어난다), 네트워크 오류·5xx·429만 재시도. **모든 시도 결과를 `notification_deliveries`에 기록**해 "왜 아무도 못 받았는가"를 추적, 전송 실패가 경보 감지를 실패시키지 않는다) + **Alert History**(보관 포함 전체 이력 + 종류·심각도·상태·기간 필터 + **평균 해소 시간** — 경보가 많은 것보다 **오래 방치되는 것**이 더 나쁜 신호인데 건수만 세면 안 보인다) + **Alert Archive**(CTO 결정 1302-④ — **삭제하지 않는다**. 해소 후 `ALERT_ARCHIVE_AFTER_DAYS`(기본 90일) 경과 시 `ARCHIVED`로 이동. **활성 경보는 절대 보관하지 않고**(문제가 있는데 화면에서 사라지면 그게 사고다), **해소 시각을 모르면 건드리지 않는다**(유예가 지났는지 알 수 없다)) + **CTO 결정 1302-① 이행**(종류별 재알림 간격 `ALERT_COOLDOWN_BUDGET/PROVIDER/UNPRICED/CONFIG_MS` — `종류별 ?? 전체 기본 ?? 30분`, 30분 공식 표준 유지) + **CTO 결정 1302-③ 이행**(운영에서 `GATE_STRICT`·`GATE_ALERTS` **기본 ON**, 끄려면 명시적 `=0` — 안전한 쪽이 기본이어야 사람이 잊었을 때 사고가 나지 않는다. 그 외 환경은 꺼짐 기본) + 웹 `/admin/production` 확장(채널 구성·예약 조율(분산/단일·점검별 리더)·전송 시도 이력·보관 정리 버튼, Playwright 3종 → 웹 e2e 57) — core 순수 로직(leader.ts: decideLease/ownsLease/renewAfter, notification.ts: shouldNotify/decideRetry/채널 본문, alert-archive.ts: planArchive/summarizeHistory)+api 어댑터(DistributedLockService·NotificationService) 분리, **마이그레이션 27**(AlertStatus.ARCHIVED·alerts.archivedAt·notification_deliveries), 라이브 검증(**실 Redis + 실제 두 인스턴스** — 15초 간격 ~68초에 예약 실행 5회(잠금 없으면 ~9회)·리더 1명·**리더 강제 종료 시 40초 내 다른 인스턴스가 인계**·503 두 번 반환하는 수신 서버에 **2회째 시도로 성공**하고 attempts=2 기록·채널 시험 실제 전송 성공·최근 해소는 보관 0건/100일 전 해소는 1건 보관(행은 유지)·게이트 운영 기본값 STRICT=on·ALERTS=on·비밀 주소 미노출), 해석 확인 CTO_REQUEST #47

## 완료 — Sprint 13 (CTO 공식 종료, 2026-07-29)

- [x] **TASK-1302 — Production Automation & Alerting** (`942ff5f`, CTO 승인 — Alert 정책(Cooldown 30분·Resolve 알림·Severity 상승 즉시 알림·unknown 경보 제외)을 공식 표준으로 유지하되 **종류별 Cooldown 환경변수 확장 허용**(→TASK-1401에서 이행), 다중 인스턴스는 현재 중복 실행 구조 유지하고 **Redis Distributed Lock은 다음 Sprint**(→TASK-1401에서 이행), 운영에서는 **GATE_STRICT=ON·GATE_ALERTS=ON을 기본값으로**(→TASK-1401에서 이행), **Alert는 삭제하지 않고 Resolve 후 90일 보관 후 Archive**(→TASK-1401에서 이행), Sprint 13 공식 종료): TASK-1301의 점검들은 **사람이 화면을 열어야** 결과를 볼 수 있었다 — 이 TASK는 그것을 주기적으로 돌리고 문제가 생기면 **사람을 찾아가게** 한다. **Scheduled Cost Verification / Provider Validation / Health Check**(간격 `OPS_CHECK_*_INTERVAL`, `15m`·`1h`·`30s`·ms 모두 해석하고 해석 불가 시 기본값으로 — 잘못 적은 값 때문에 점검이 멈추거나 폭주하는 것보다 안전하다. `off`로 개별·전체 중단. 기본 15분/15분/1시간 — **Health Check가 가장 드문 이유**는 실 Provider 호출이 섞이는 점검이기 때문. **점검마다 책임지는 경보 종류를 나눔** — 해소 판정은 "이번에 감지되지 않았다"로 하므로 비용 점검이 Provider 경보까지 해소하면 죽어 있는 Provider가 조용히 사라진다. **예약 점검은 Live Check를 하지 않음**(CTO 결정 1301-① — 자동 과금 방지, health-check 점검은 쌓인 Execution만 읽고 새 호출을 만들지 않는다). 겹쳐 돌지 않음, 타이머 `unref()`, **점검 자체의 실패도 이력에 기록**(조용히 안 도는 점검이 가장 위험)) + **Budget / Provider Failure / Unpriced Model / Configuration Alert**(4종 — 예산은 임계 warning·초과 critical(무제한이면 경보 없음), Provider는 degraded warning·down critical이되 **`unknown`은 경보하지 않음**(모르는 것을 문제라 하지 않는다), unpriced는 warning이고 **호출 차단은 하지 않음**(CTO 결정 1301-⑤ — 막으면 안전하지만 새 모델 도입이 불가능해진다), 설정 오류·Provider 키 문제는 critical(기동 시점 Fail Fast만으로는 뜬 뒤의 변화를 알 수 없다)) + **중복·해소 판정**(`key`가 같으면 같은 문제 — 쿨다운(`ALERT_COOLDOWN_MS` 기본 30분) 안에서는 재알림 억제. **같은 내용이 반복되면 사람이 경보를 무시하기 시작하고 그 순간 경보 체계는 없는 것과 같아진다**. 심각도 상승(warning→critical)은 쿨다운 무관 알림, **해소도 알림**(났다는 사실만 알리면 지금 문제가 있는지 알 수 없다), 해소된 경보는 지우지 않고 RESOLVED로 보존) + **전달 채널**(로그 항상 + 웹훅 `ALERT_WEBHOOK_URL`. **웹훅 실패가 점검을 실패시키지 않음** — 알림 채널이 죽었다고 감지까지 멈추면 상황이 더 나빠진다. 웹훅 주소는 API로 노출하지 않음) + **CI/CD Deployment Gate**(`scripts/deployment-gate.mjs` — CTO 결정 1202-④ 이행. `/health/ready` 자동 판정, exit `0` 가능 / `1` 불가 / **`2` 판정 불가**(확인하지 못한 것을 통과로 처리하지 않는다), `GATE_STRICT=1` 수동 항목 차단·`GATE_ALERTS=1` critical 경보 차단) + **CTO 결정 1301-③ 이행**(진단 호출을 운영 통계에서 분리 — **feature를 추가하지 않고**(0601 승인의 4종 유지) `executions.diagnostic` 메타데이터로 구분. Production Monitoring에서 제외하되 `diagnosticCalls`로 **숨기지 않고 표시**) + **CTO 결정 1301-② 이행**(모니터링 기준 환경변수화 `LLM_MONITOR_HEALTHY_RATE`/`DEGRADED_RATE`/`MIN_SAMPLES`/`P95_WARN_MS` — 확정 기본값 0.95/0.5/5/20000 유지, 해석 불가·**순서가 뒤집힌 기준**(degraded>healthy)은 기본값으로 되돌림) + 웹 `/admin/production` 경보 섹션(활성 경보·예약 점검 구성·"지금 점검" 버튼, Playwright 4종 → 웹 e2e 57) + **구현 중 발견해 고친 표시 결함 2건**(① 경보 배지가 주의 경보만 있을 때 "이상 없음"인데 바로 아래에 경보를 나열해 **배지와 목록이 모순** — 1202에서 고친 것과 같은 부류라 회귀 테스트를 남김 ② 비용 검증 메시지의 마크다운 강조(`**…**`)가 화면에 평문으로 그대로 찍힘 — core에서 제거하고 "마크다운을 넣지 않는다" 테스트 추가) — core 순수 로직(alerts.ts: detect*/reconcileAlerts/summarizeAlerts, schedule.ts: parseIntervalMs/resolveSchedules/shouldRun)+api 어댑터(AlertService·ScheduledChecksService) 분리, **마이그레이션 26**(executions.diagnostic·alerts·check_runs), 라이브 검증(미인증 401 2/2·예약 점검이 스스로 돌아 미산정 경보 자동 감지(trigger=schedule)·같은 경보 재알림 억제(occurrences 누적, notified 0)·예산 초과 critical raise → 복구 시 resolve·진단 호출 3건 제외하고 사용자 호출 8건만 집계·게이트 exit 0/1/2 3경로), 해석 확인 CTO_REQUEST #46

- [x] **TASK-1301 — Real AI Provider Production Integration** (`6d2bce6`, CTO 승인 — Live Check는 기본 실행하지 않고 운영자가 명시적으로 실행하도록 유지, 모니터링 기본값(Healthy 95% / Degraded 50% / 최소 표본 5 / p95 20초) 유지하되 향후 환경변수로 조정 가능하도록 설계(→TASK-1302에서 이행), Health Check와 Live Check는 운영 통계에서 분리하되 **Feature를 추가하지 말고 Execution 메타데이터 기반으로** 구현(→TASK-1302에서 이행), Cost Verification은 수동 실행 유지하고 다음 Sprint에서 Scheduler 추가(→TASK-1302에서 이행), Unpriced 모델은 Alert 대상으로 유지하고 호출 차단은 하지 않음): **API Key Validation**(Provider별 키 형식 규칙을 core 한 곳에 선언 `packages/core/src/llm/api-key.ts` — openai `sk-`/anthropic `sk-ant-`/gemini 길이만, 공통 20자 이상. **플레이스홀더를 별도 상태로 탐지**(`sk-xxxx…`·`your-api-key`·`changeme` — 형식 검사만으로는 통과해 버리는 배포 사고의 단골). **키 값은 어떤 경로로도 노출하지 않음** — 앞 6자 힌트와 길이만) + **CTO 결정 1202-② 이행**(설정에서 참조하는 Provider(`LLM_PROVIDER`/`LLM_ROUTE_*`/`LLM_FAILOVER_PRIORITY`/`LLM_EXPERIMENT_*`)의 키를 **운영 조건부 필수**로 env-spec에 선언(`requiredWhen`) + 형식 검사(`validate`) — 배포 준비 검증과 **Fail Fast 기동**에 그대로 반영되어 참조하는데 키가 없으면 운영에서 서버가 뜨지 않는다. 쓰지 않는 Provider의 키는 불필요. 부분 문자열 오탐 방지(`openai-legacy`가 있다고 `openai` 참조로 보지 않음)) + **Live Check**(`?live=1` — Provider마다 최소 완성 호출 1회. **실제 과금이 발생하므로 기본은 형식 검사만** 하고 실행 여부를 `liveChecked`로 명시. 예외가 나도 보고서로 회수해 blocker로 표시) + **Cost Verification**(기록된 Execution 비용을 가격표로 재계산해 대조 — `unpriced`(가격표 없음 → **비용이 null로 남아 예산 상한이 무력화**되므로 가장 크게 다룸)/`mismatch`(단가 변동)/`missing-usage`(usage 없음)를 **조치가 다르므로 구분**, 응답에 가격표 동봉해 바로 조치 가능) + **Production Monitoring**(Provider별 성공률·지연 분포·비용·경보 — **표본이 적으면 판정하지 않음**(기본 5회 미만 `unknown`, 1회 실패로 "장애"라고 말하지 않는다), **p50/p95/p99**(평균만 보면 꼬리 지연을 놓친다. **최근접 순위**라 관측되지 않은 값을 지어내지 않음), 상태 healthy≥95%/degraded≥50%/down, 전체는 **가장 나쁜 Provider**를 따름, 실패 호출의 `cost=null`은 미산정으로 세지 않음(실패는 usage가 없는 게 정상 — 그러지 않으면 경보가 늘 울린다)) + **Vision Production**(이미지 전달 형식이 Provider마다 다름 — OpenAI `image_url`(data URL)/Anthropic `image` block(base64+media_type)/Gemini `inlineData`. 하나만 맞으면 라우팅·Failover로 Provider가 바뀌는 순간 **이미지가 조용히 사라지고 "이미지 없이 추측한 결과"가 정상처럼 기록**되므로 세 Provider 전부 같은 시나리오 회귀 테스트 + 이미지 없을 때 헛된 첨부 미생성) + **Provider Smoke Test 확장**(API Key 검증 + **키가 설정된 모든 Provider** Health Check(기본 하나만 확인하면 전환되는 순간에야 장애를 처음 안다) + Vision 커버리지 필수화 + 비용 검증·운영 모니터링 판정(critical 경보 시 실패). ADMIN 권한이 없어 건너뛴 항목은 `…`로 표시하고 **실패로 세지 않음** — 확인하지 못한 것을 통과라고 하지 않기 위해) + 웹 **`/admin/production`**(키 검증 표·Live Check 버튼(과금 경고 명시)·모니터링·비용 검증, Playwright 4종 → 웹 e2e 50) + API 3종 **전부 ADMIN 전용**(조회도 보호 — 결정 1201-⑤와 같은 판단, 설정 지형과 비용이 드러나므로) — core 순수 로직(api-key.ts / cost-verification.ts / production-monitor.ts)+api 어댑터(ProviderProductionService) 분리, 스키마 변경 없음, 라이브 검증(미인증 401·플레이스홀더 키 탐지 및 anthropic 조건부 필수 blocker 2건·Live Check 실패 회수(샌드박스 egress 차단 403)·mock 호출 6건 모니터링 healthy·비용 검증 일치·스모크 13/13 PASS), 해석 확인 CTO_REQUEST #45

## 완료 — Sprint 12 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-1202 — Production Readiness & Deployment** (`95e0fb4`, CTO 승인 — 운영에서 필수 설정 오류가 있으면 **Fail Fast**(기동하지 않음) 확정, 운영 필수 항목은 WEB_URL/DATABASE_URL/Storage(S3)/AUTH_ADMIN_EMAIL/AUTH_ADMIN_PASSWORD 공식 표준 유지하되 **실제 AI Provider 연결 시 Provider API Key 추가**(→TASK-1301에서 이행), 실 Provider Smoke Test와 Backup 확인은 Manual Check 유지, 다음 Sprint에서 CI/CD가 `/health/ready`로 배포 가능 여부 자동 판정, Sprint 12 공식 종료): **Environment Validation**(환경변수 선언 `packages/core/src/ops/env-spec.ts`을 **단일 원천**으로 두고 검증·대시보드·런북이 같은 선언을 사용 — 이름·설명·운영 필수 여부·형식 검사·기본 동작·비밀 여부·운영 권고를 한 곳에 선언. 필수 누락은 운영에서만 error, **형식 오류는 환경 무관 error**(잘못된 값은 어디서도 동작하지 않음), 운영 권고 위반은 warning) + **Startup Validation**(기동 시 검증 후 **운영에서 오류가 있으면 기동 차단·exit 1** — 잘못된 설정으로 뜬 서버가 조용히 오작동하는 것이 더 위험. 개발에서는 경고만 남기고 기동) + **Deployment Checklist**(기계 판정 — 환경·DB·마이그레이션·저장소·관리자 계정·실제 Provider·예산·Failover 자동, 스모크·백업은 정직하게 manual. blocking 실패가 있으면 배포 불가로 판정) + **Configuration Verification**(설정 현황 표 — **비밀 값은 설정 여부만** 노출) + **Health Dashboard**(웹 **`/admin/health`** — 배포 가능 여부·차단 사유·체크리스트·구성 요소·환경 검증 오류/권고·설정 현황, Playwright 4종 → 웹 e2e 46) + **Production Runbook**(docs/operations/production-runbook.md — 배포 전 준비·배포·직후 확인·일상 운영·모니터링 지점·권한 요약) + **Recovery Guide**(docs/operations/recovery-guide.md — 증상별 진입점, 기동 실패/DB/저장소/Provider/예산/실험/설정 미반영/롤백/사고 후 정리) + API(`GET /health` 기존 `OK` 계약 유지(웹 상태 표시가 의존), `GET /health/live` 구조화 생존 확인(무인증·내부 구성 비노출), **`GET /health/ready` ADMIN 전용**(결정 1201-⑤)) + 구성 요소 점검은 **독립 실패**(저장소가 죽어도 DB 상태를 볼 수 있어야 장애 대응이 된다) + StorageService.check() 추가 + **구현 중 발견해 고친 표시 결함**(개발 환경에서 예산·Failover 미설정이 상태는 `통과`인데 설명은 "비용 폭주"·"서비스 중단" 경고 문구여서 상태와 설명이 모순 — 환경별 문구로 분리하고 회귀 테스트 추가) — core 순수 로직(env-spec.ts: validateEnvironment/describeEnvironment, deployment-checklist.ts: buildDeploymentChecklist/summarizeChecklist)+api 어댑터(ReadinessService) 분리, 스키마 변경 없음, 라이브 검증(운영 모드 필수 누락 → 기동 차단 exit 1·정상 기동 시 "환경 검증 통과 (22개 항목)"·s3rver 중단 상태를 저장소 실패로 정확히 탐지하고 배포 불가 판정·복구 후 통과 8/실패 0/직접확인 2로 배포 가능·미인증 401), 해석 확인 CTO_REQUEST #44
- [x] **TASK-1201 — Provider Administration Console** (`893151e`, CTO 승인 — 설정 우선순위 DB Override → Environment Variable → Default 공식 표준 확정(Code-first 유지), 다중 인스턴스 전파는 TTL 기반 유지(Redis Pub/Sub는 후속 Sprint), Routing 규칙(LLM_ROUTE_*)은 현재 Sprint 범위 제외, 배포 시 Override 자동 삭제하지 않고 운영자가 명시적으로 해제, /admin/* 모든 조회와 변경은 ADMIN 인증 요구): 설정 원칙은 **Code-first(환경변수) 유지**하고 콘솔은 그 위의 **오버라이드**로 설계(`유효값 = 오버라이드(DB) ?? 환경변수 ?? 기본값` — 오버라이드가 없으면 기존 동작과 완전히 동일, 해제하면 환경변수로 복귀, 각 값의 **출처**(override/env/default)와 복귀값을 화면에 표시) + **Provider Enable/Disable**(`provider.<name>.enabled` — 끄면 라우팅·실험·Failover 후보에서 제외, **마지막 하나는 끌 수 없음**(호출 전멸 방지, 400)) + **Model Management**(`model.<feature>` — LLM_MODEL_* 보다 우선) + **Budget Management**(`budget.daily`/`monthly`/`alertRatio`) + **Experiment Management**(`experiment.<feature>` — 정의 문자열, 해석 가능한 값만 허용) + **Audit Log**(admin_audit_log — 누가·무엇을·이전→이후, GET /admin/audit) + 검증은 **저장 시점**(알 수 없는 Provider·음수 예산·해석 불가 실험 정의·지원하지 않는 키 → 400. 잘못된 값이 저장되면 다음 호출부터 라우팅이 깨지므로) + 읽기 성능(설정은 LLM 호출마다 참조되므로 DB 왕복 없이 **인메모리 동기 조회**, 기동 시 적재·쓰기 시 즉시 갱신·TTL(`ADMIN_SETTINGS_TTL_MS` 기본 10초) 경과 시 백그라운드 재적재) + **전부 ADMIN 전용**(전역 WriteProtectionGuard는 쓰기만 막으므로 **조회에도 AuthGuard 부착** — 콘솔 GET이 예산·모델 구성과 감사 이력(수행자 이메일)을 노출하기 때문. 라이브 검증 중 미인증 GET이 200으로 열려 있는 것을 발견해 수정하고 회귀 테스트 추가) + 웹 **`/admin/console`**(Provider 토글·모델/예산/실험 입력·출처 배지·변경 이력, Playwright 6종 → 웹 e2e 42) + **CTO 결정 1102-① 이행**(최소 표본 기본 30 유지 + `LLM_EXPERIMENT_MIN_SAMPLES`로 조정 가능) + **CTO 결정 1102-③ 이행**(관측 기간은 **Definition Signature 변경 시에만 초기화**, START/STOP은 기간 유지 — experiment_states에 signature·signatureChangedAt 추가) + 결정 1102-②④는 현행 유지 확정 — core 순수 로직(admin/settings.ts: validateSetting/resolveSetting/enabledProviders)+api 어댑터(AdminSettingsService) 분리, **마이그레이션 24·25**(experiment_signature_window, admin_settings·admin_audit_log), 라이브 검증(모델 오버라이드→라우팅 즉시 반영·openai 비활성→후보에서 제외·예산 10→99 반영·실험 정의 콘솔 설정·가드레일 4종 400·EDITOR 403·미인증 401), 해석 확인 CTO_REQUEST #43

## 완료 — Sprint 11 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-1102 — Experiment Analytics & Recommendation** (`9615770`, CTO 승인 — 최소 표본 기본 30회 유지하되 환경변수 조정 가능하도록 설계(→TASK-1201에서 이행), 추천 우선순위 성공률 → 호출당 비용 → 평균 지연을 공식 표준 확정, 관측 기간은 START/STOP에서 유지하고 Definition Signature 변경 시에만 초기화(→TASK-1201에서 이행), 다중 비교 보정 미도입, Sprint 11 공식 종료): **Variant Performance Summary**(변형별 호출·성공/실패·성공률·평균 지연(호출 수 가중 평균)·비용·호출당 비용·토큰, 성공률에 **Wilson 95% 신뢰구간** 동반 — 적은 표본을 과신해 표시하지 않기 위해. 근거는 배정이 아니라 **실행(Execution)**, 관측 기간은 마지막 START 이후) + **Success Rate / Latency / Cost Comparison**(가중치 최대 변형을 기준(baseline)으로 성공률 %p·지연 ms·호출당 비용 USD 차이와 성공률 신뢰도) + **Winner Recommendation**(품질 우선 — ① 전 변형 최소 30회 미달이면 추천 보류 ② 성공률 1·2위 차이 신뢰도 95% 이상이면 1위 승자(확정) ③ 성공률 동률이면 호출당 비용 ④ 비용도 같으면 지연 ⑤ 어느 축도 차이 없으면 보류) + **Confidence Score**(양측 2-비율 z검정, core 순수 함수 normalCdf(erf 근사)/wilsonInterval/proportionConfidence) + **Analytics Dashboard**(GET /llm/experiments/:feature/analytics + 웹 /experiments 실험 카드 내 추천 배지·근거 문장·기준 대비 비교표, Playwright 2종 → 웹 e2e 36) + **CTO 결정 1101-④ 이행**(권한 분리 — Start/Stop은 EDITOR 이상, **Promote/Rollback은 ADMIN 전용** @RequireRole. Express 5의 path-to-regexp가 `:action(start|stop)` 문법을 지원하지 않아 4개 명시 라우트로 분리) + **CTO 결정 1101-⑤ 이행**(정의 변경 재배정 Audit — experiment_assignment_events에 from/to 변형·서명·사유(DEFINITION_CHANGED/VARIANT_UNAVAILABLE) 기록, Assignment Dashboard에 표시. 최초 배정은 재배정이 아니므로 미기록) + 결정 1101-①②③은 현행 유지 확정 — **마이그레이션 23**(experiment_assignment_events), 라이브 검증(표본 1·3회 시 추천 보류 + Wilson 구간 0.21~1.00로 과신 방지 → 34·102회 축적 후 신뢰도 100% 확정 추천, 정의 변경 재배정 Audit 2건, EDITOR promote/rollback 403·ADMIN 200), 해석 확인 CTO_REQUEST #42
- [x] **TASK-1101 — Sticky Assignment & Experiment Lifecycle** (`5514b33`, CTO 승인 — 상태 레코드 없으면 RUNNING 기본 유지, Sticky Assignment는 Project 기반 공식 표준 확정, Winner Promotion은 운영자 수동 절차 유지, 권한은 Start/Stop EDITOR 이상·Promote/Rollback ADMIN 전용으로 변경(→TASK-1102에서 이행), 정의 변경 재배정 시 Audit 이력(→TASK-1102에서 이행)): **Project 기반 Sticky Assignment**(같은 프로젝트는 항상 같은 변형 — 배정을 저장소가 아닌 **결정적 해시**(FNV-1a + fmix32 최종 혼합)로 정해 저장 실패·재기동·다중 인스턴스와 무관하게 일관. FNV만 쓰면 proj-1/proj-2 같은 순번 키가 뭉쳐 한쪽으로 쏠리는 문제를 혼합 단계로 해결하고 회귀 테스트로 고정. 실험 정의 **서명**(변형=가중치)이 바뀌면 재배정, 이름·종류 변경은 서명 불변(결정 1003-③). projectId는 Content/Analysis/Vision 호출 경로에서 전달, 없으면 기존 무상태 추첨) + **Experiment Start/Stop**(STOPPED는 실험 미적용하고 기존 라우팅으로 처리, 행이 없으면 RUNNING으로 보아 TASK-1003 동작 보존) + **Winner Promotion**(승자 변형으로 전 트래픽, 현재 정의에 없는 변형은 400 거부, 승자 Provider 사용 불가 시 라우팅으로 강등) + **Rollback**(직전 전이의 이전 상태로 복원, ROLLBACK 자체는 대상 제외해 무한 왕복 방지) + **Assignment Dashboard**(GET /llm/experiments/assignments + 웹 /experiments — 상태 배지·조작 버튼·전이 이력·프로젝트별 배정 표, Playwright 4종 → 웹 e2e 34) + 전이 감사(experiment_events — action/from·to 상태/변형/actor/note, 상태 갱신과 한 트랜잭션, 전역 WriteProtectionGuard로 EDITOR 이상·미인증 401) + **CTO 결정 1003-①~④ 확정 반영**(unknown 오류 Failover 제외 공식 표준 / 표기법 `이름\|종류\|변형=가중치` 유지 / kind는 운영 메타데이터로 알고리즘 무영향 / 배정(Assignment)과 실행(Execution) 분리 유지 — 대시보드에 둘 다 표시) — core 순수 로직(experiment-lifecycle.ts: hashToUnitInterval/assignSticky/applyLifecycleAction/resolveLifecycle)+api 어댑터(ExperimentLifecycleService) 분리, **마이그레이션 22**(experiment_states·experiment_events·experiment_assignments), 라이브 검증(같은 프로젝트 6회 연속 동일 변형·서로 다른 프로젝트는 다른 변형·STOP→라우팅(anthropic)·PROMOTE→승자(mock)·잘못된 변형 400·ROLLBACK PROMOTED→STOPPED·미인증 401), 해석 확인 CTO_REQUEST #41

## 완료 — Sprint 10 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-1003 — Routing Experiment & Traffic Control** (`8a6aa57`, CTO 승인 — unknown 오류는 Failover 대상 아님 공식 표준 확정, 실험 표기법 `이름|종류|변형=가중치` 단일 형식 유지, kind는 운영 메타데이터로 Routing 알고리즘에 영향 없음, 배정(Assignment)과 실행(Execution)은 분리하고 Dashboard에 둘 다 유지, Sprint 10 공식 종료): Percentage/A·B/Canary/Weighted Routing(LLM_EXPERIMENT_CONTENT/ANALYSIS/VISION = `이름|종류|변형=가중치,…` — 이름·종류 생략 가능, 가중치 생략 시 균등, 변형은 `provider` 또는 `provider:model`. **4종을 하나의 가중 추첨 원리로 구현**하고 종류는 운영자의 의도 선언으로 대시보드 표시·추정에 사용) + Dynamic 해석(라우팅과 동일하게 호출 시점마다, 재기동 불필요) + Graceful degradation(사용 불가 변형 제외 후 가중치 재정규화, 전부 불가 시 실험 미적용하고 기존 라우팅으로 처리 — 실험 설정이 호출을 실패시키지 않음) + 우선순위(호출자 model > 실험 변형 > 라우팅 규칙 > LLM_MODEL_* > Provider 기본) + Experiment Dashboard(GET /llm/experiments + 웹 **/experiments** — 종류 배지·적용 여부·설정 비율·배정 비율·실제 배정·변형별 실행 지표, Playwright 4종 → 웹 e2e 30) + Experiment Metrics(/executions/stats의 **byVariant** — 변형 `feature→provider:model`별 호출·성공률·지연·토큰·비용, A/B 비교 원천) + **배정 ≠ 실행 구분**(변형이 불건강해지면 Failover가 체인을 재정렬하므로 추첨 결과와 실제 실행이 다를 수 있음 — 대시보드가 두 값을 나란히 표시) + **CTO 결정 1002-① 이행**(Failover 오류 분류 확정 — timeout/server_error/rate_limit/network만 대상, budget/validation/auth(401·403·잘못된 키)/invalid_request/unknown은 제외. 상태코드→오류코드→메시지 순 판정, SDK별 status/statusCode/response.status 모두 인식, 분류명 로그) + **CTO 결정 1002-④ 이행**(GET /llm/health 진단 호출을 attempts/failovers/exhausted/skipped/byProvider에서 분리하고 metrics.healthChecks로 별도 집계 — 건강 상태에는 계속 반영) — core 순수 로직(experiment.ts: parseExperiment/pickVariant/describeExperiment, failover.ts: classifyFailoverError)+api 어댑터(experiment-config.ts) 분리, 스키마 변경 없음, 라이브 검증(canary 80/20 실 배정 12/6·변형별 실행 지표 비교·auth 오류 Failover 미발생·health 계측 분리), 해석 확인 CTO_REQUEST #40
- [x] **TASK-1002 — Provider Failover Engine** (`b1c39bc`, CTO 승인 — Failover 대상 오류 목록 공식 확정(Timeout/5xx/Rate Limit/네트워크만, Budget·Validation·인증(401/403)·잘못된 API Key·잘못된 요청 제외 → TASK-1003에서 이행), Failover 시 전환 Provider 기본 모델 사용 현행 유지, Health 기본값(연속 3회·Cooldown 60초·Half-open) 공식 표준 확정, GET /llm/health를 운영 계측에서 분리 지시 → TASK-1003에서 이행): Provider Priority(LLM_FAILOVER_PRIORITY — 라우팅 Provider가 항상 1순위, 그 뒤 우선순위 순서·중복 제거·사용 불가 제외, **미설정이면 Failover 비활성**으로 기존 동작 보존) + Retry Policy(Provider별 게이트웨이 지수 백오프 LLM_MAX_ATTEMPTS 소진 후 다음 Provider로 전환, 2순위부터는 호출자 model을 버리고 각 Provider 기본 모델 사용 — 모델명은 Provider 간 비호환) + Timeout Policy(LLM_TIMEOUT_MS 기본 120000·0=무제한 — 초과 시 LlmTimeoutError로 전환) + Health Check Integration(ProviderHealthTracker — 연속 실패 LLM_FAILOVER_HEALTH_THRESHOLD(기본 3)회 도달 시 LLM_FAILOVER_HEALTH_COOLDOWN_SEC(기본 60초) 동안 체인 뒤로 밀림(제외 아님), 쿨다운 경과 시 half-open 재시도·성공 시 즉시 회복, GET /llm/health?provider=는 Failover 미사용 진단 경로) + Failover Metrics(GET /llm/failover — 우선순위·타임아웃·Provider별 건강 상태·attempts/failovers/exhausted/skipped·Provider별 성공·실패, 웹 /routing 하단 섹션 + Playwright 2종 → 웹 e2e 26) + **Failover 제외 대상**(예산 초과 429는 markNoFailover로 명시·Execution 미기록, 요청 검증 오류 400 — 둘 다 skipped 집계, CTO 지시) + **이력 Provider 일치**(CTO 결정 1001-③ — AnalysisRecognition.providerName 신설, AnalysisRun.provider·VisionSummary.source가 실제 라우팅/Failover된 Provider `llm:<provider>`를 기록해 Execution과 동일 의미) — core 순수 로직(failover.ts: buildFailoverChain/withTimeout/isFailoverEligible/ProviderHealthTracker)+api 어댑터(failover-config.ts) 분리, 스키마 변경 없음, 라이브 검증(openai 무효키 → mock 전환 성공·AnalysisRun.provider=llm:mock·연속 실패 2회 → 불건강·다음 호출에서 체인 뒤로), 해석 확인 CTO_REQUEST #39
- [x] **TASK-1001 — Cross-Provider Routing Engine** (`03dca0f`, CTO 승인 — Routing 단위는 Feature 3종(Content/Analysis/Vision) 공식 표준 확정(프로젝트별·사용자별은 후속 Sprint), Fallback은 설정 해석 시 기본 Provider 자동 전환 유지·실행 중 오류는 Failover 대상(→TASK-1002에서 이행), 이력 Provider 필드는 실제 Routing된 Provider 기록으로 통일(→TASK-1002에서 이행)): Feature별 Provider Mapping(LLM_ROUTE_CONTENT/ANALYSIS/VISION — `provider` 또는 `provider:model`) + Dynamic Routing(호출 시점 해석 — 재기동 불필요, 사용 불가 Provider는 기본으로 폴백 + 경고 로그, Failover는 CTO 지시로 범위 제외) + Routing Dashboard(GET /llm/routing + 웹 /routing — 결정 근거 배지 feature/default/fallback·폴백 사유·환경변수명) + Routing Metrics(/executions/stats의 byRoute — 경로 feature→provider별 호출·성공률·지연·토큰·비용) + Provider 인스턴스 맵(createLlmProviderMap — 키 설정된 Provider 전부, LlmService 단일 관문에서 경로별 게이트웨이 선택) — 우선순위(호출자 model > 규칙 :model > LLM_MODEL_* 동일 Provider만 > Provider 기본), 라이브 검증(분석→anthropic 실제 호출·기록, vision→gemini 폴백), 해석 확인 CTO_REQUEST #38

## 완료 — Sprint 9 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0903 — Anthropic & Gemini Provider Integration** (`f9cbda5`, CTO 승인 — Anthropic JSON은 System Prompt + JSON Parsing + Retry 방식 공식 표준 확정(Structured Output API는 추후 별도 Sprint 검토), 가격표는 공식 운영 모델만 등록(Preview 모델 제외), Sprint 9 공식 종료): Anthropic 공식 연결(JSON 전용 system 지시 강화 — Claude 4.6+ prefill 400이므로 미사용, 멀티모달 image block, 잘림 방어 stop_reason=max_tokens, 클라이언트 주입) + Gemini 공식 연결(responseMimeType application/json, inlineData 멀티모달, modelVersion 스냅샷 기록 → 접두사 매칭 비용, 잘림 방어 finishReason=MAX_TOKENS) + Provider Factory(Registry 기반 테이블 드리븐 provider.factory.ts — 키 단일 정의·mock 폴백, 새 Provider는 Registry·어댑터·가격표 3곳만) + Unified Execution(claude-opus-5/sonnet-5/haiku-4.5·gemini-2.5-flash 단가 등록, Registry 전 모델 가격표 등록을 테스트가 보증) + Provider Comparison Dashboard(/providers 비교 표 — 호출·성공률·평균 지연·토큰·비용·비용/호출) — 라이브 배선 검증(Anthropic 실 API 401 도달·Gemini mock 폴백), 해석 확인 CTO_REQUEST #37
- [x] **TASK-0902 — Cost Governance & Multi-Provider Foundation** (`5b39867`, CTO 승인 — 예산 정책 공식 표준 확정(80% Alert · 100% 초과 429 차단), Model Routing은 Provider 내부 모델 선택만 지원(Cross-Provider는 Anthropic/Gemini 공식 연결 이후), Budget은 환경변수 Code-first 유지(ADMIN 화면은 후속 Sprint)): Daily/Monthly Budget(LLM_DAILY/MONTHLY_BUDGET_USD, UTC 경계, 미설정=무제한 — 초과 시 LlmService 단일 관문 429 차단·Execution 미기록) + Cost Alert(80% 임계 LLM_BUDGET_ALERT_RATIO — 상태 전이 로그·대시보드 배지, core evaluateBudgetWindow 순수 로직) + Provider Registry(Code-first LLM_PROVIDER_REGISTRY — official/adapter-ready/mock·키 설정 여부만 노출, GET /llm/providers) + Model Routing(LLM_MODEL_CONTENT/ANALYSIS/VISION — 호출자 명시 우선, 선택된 Provider 내 모델 선택) + Provider Dashboard(웹 /providers — Registry·라우팅·예산 카드·Provider별 통계, Playwright 3종 → web e2e 21) + GET /llm/budget — 스키마 변경 없음, 해석 확인 CTO_REQUEST #36
- [x] **TASK-0901 — Real Provider Integration: OpenAI Production** (`8d095e3`, CTO 승인 — 출력 상한 공식 표준(Content 4096/Analysis 2048/Vision 2048, 환경변수 조정 유지), JSON은 length 시 FAILED·Text는 부분 결과 허용 확정, 실 Provider Smoke 정책 확정(운영/스테이징 배포 직후 1회 필수·모델 변경 시·인증/쿠키 변경 시 재실행 — 현재 런북·스크립트가 공식 운영 절차)): feature별 운영 출력 상한(LlmService 단일 관문 — content 4096/analysis 2048/vision 2048, LLM_*_MAX_TOKENS 조정) + JSON 잘림 방어(finish_reason=length+json → 명확한 오류·Execution FAILED) + OpenAI Production 통합 검증(주입 클라이언트로 실 응답 형태 재현 — Content 텍스트/Analysis json_object 엄격 파싱/Vision image_url 첨부/스냅샷 모델 접두사 비용/잘림 FAILED, openai-production.spec.ts 4종) + Production Smoke 10단계 확장(쿠키 전용 운영 모드 로그인 지원·운영 판정: byProvider/byFeature 커버리지·실키 cost>0) + 운영 런북(환경 세트) — 실키 네트워크 검증은 운영/스테이징 스모크 전용(0603 승인 ④), 해석 확인 CTO_REQUEST #35

## 완료 — Sprint 8 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0804 — Login Protection & Security Hardening** (`5ce8a20`, CTO 승인 — 기본 보안 정책 공식 표준 확정(Rate Limit 30회/60초·Lockout 5회/15분·Password 8자+영문+숫자, 환경변수 조정 유지), Rate Limit 인메모리 유지(다중 인스턴스 시 Redis 확장), Unlock 버튼 미구현(ADMIN Reset이 해제 절차), Idle Timeout 미구현(절대 TTL만), Sprint 8 공식 종료): Rate Limit(이메일 키 슬라이딩 윈도우 → 429, AUTH_LOGIN_MAX_ATTEMPTS/WINDOW_SEC 기본 30/60초) + Account Lockout(연속 5회 실패 → 15분 DB 잠금 users.lockedUntil, 성공 시 초기화·ADMIN 재설정 시 즉시 해제 — 마이그레이션 21) + Password Complexity(8자+영문+숫자, core 단일 정의 — 생성/변경/재설정 공통) + Failed Login Audit(LOGIN_FAILED·ACCOUNT_LOCKED, 액션 8종 + /admin/users 잠김 배지) + Cookie 전용 운영 모드(AUTH_COOKIE_ONLY — 운영/스테이징 기본, 본문 토큰 제외·쿠키만, 웹 credentials include) + Session Timeout(AUTH_SESSION_TTL_HOURS 기본 168h — 세션·쿠키 수명 통일) + Playwright 3종(웹 e2e 18종) — 해석 확인 CTO_REQUEST #34
- [x] **TASK-0803 — Password Management & Operational Security** (`40bd4d7`, CTO 승인 — 개발은 본문 토큰+httpOnly 쿠키 병행 유지·운영은 쿠키 전용 전환(→0804에서 이행), Reset은 ADMIN 직접 지정 유지(메일 기반은 메일 인프라 도입 후) 확정): 비밀번호 변경(PATCH /auth/password — 본인 확인·현재 세션 외 폐기·감사 PASSWORD_CHANGED) + 비밀번호 재설정(POST /auth/users/:id/password-reset, ADMIN — 대상 전 세션 폐기·자기 자신 불가·감사 PASSWORD_RESET) + httpOnly/Secure/SameSite 세션 쿠키(로그인 발급·Bearer→쿠키 순 인식·로그아웃 만료, AUTH_COOKIE_SECURE/AUTH_COOKIE_SAMESITE, core 순수 로직) + /llm/health 운영/스테이징 EDITOR+ 보호(AUTH_PROTECT_HEALTH, CTO 결정 0802-③) + /account UI·/admin/users 재설정 인라인(Playwright 3종, 웹 e2e 15종) — 스키마 변경 없음, 해석 확인 CTO_REQUEST #33
- [x] **TASK-0802 — User Management UI & Full Write Protection** (`3383e12`, CTO 승인 — @Public 예외 3종(Login·Company Brain Query·READY Validation)만 유지·새 쓰기 API 기본 보호, 권한 정책(Write EDITOR+/User Mgmt ADMIN/Logout VIEWER+/GET 유지) 확정, /llm/health는 운영/스테이징 EDITOR+ 인증(개발 비보호 가능)→0803에서 이행): 전면 쓰기 보호(WriteProtectionGuard APP_GUARD — 모든 POST/PATCH/PUT/DELETE 인증, 기본 EDITOR+, 읽기 성격 POST는 @Public, 조회 GET 비보호 유지) + 사용자 관리(목록/생성/역할 변경/비활성화 — 자기 자신 불가, 비활성화 시 세션 즉시 폐기) + Audit 확장(user_audit_log 4종 액션) + /admin/users UI(Playwright 3종, 웹 e2e 12종) + 웹 쓰기 호출 토큰 첨부·스모크 로그인 단계 — 해석 확인 CTO_REQUEST #32
- [x] **TASK-0801 — Authentication & Authorization Foundation** (`400b4ae`, CTO 승인 — Foundation 적용 범위(발행 전이·사용자 관리) 확정→0802에서 전면 확대 지시, 역할 3종 공식 표준, 토큰은 개발 localStorage·운영 httpOnly/Secure/SameSite 쿠키 전환 확정): User Entity(역할 3종)+DB 세션(Bearer 256비트, 7일)+scrypt 해시(@acos/core)+RBAC(AuthGuard/@RequireRole 계층 비교 — 발행 전이 EDITOR+, 사용자 생성 ADMIN)+Actor Audit(content_status_history.actor에 수행자 이메일)+Login UI(/login, localStorage 토큰, Playwright 3종) — 관리자 부트스트랩(AUTH_ADMIN_*), 적용 범위는 발행 전이·사용자 관리(전면 강제는 CTO 결정 대기), 해석 확인 CTO_REQUEST #31

## 완료 — Sprint 7 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0704 — Publishing Web UI & Audit History** (`dcc4817`, CTO 승인 — Audit는 from/to/timestamp 공식 표준(Actor는 인증 도입 후 추가→0801에서 이행), Publishing UI는 프로젝트 상세 유지 확정): 감사 이력(content_status_history — 전이 1건당 1레코드, 전이와 한 트랜잭션, GET …/contents/:id/history 최신순) + publishedAt 최초 발행 시점 보존 반영(0703 승인 ②) + 발행 Web UI(상태별 색상 Status Badge·allowedTransitions 기반 상태 변경 버튼·publishedAt UTC 표기·감사 이력 목록) + Playwright e2e 2종(전 구간 전이·되돌리기, 스텁 CORS) — 해석 확인 CTO_REQUEST #30
- [x] **TASK-0703 — Real Provider Smoke & Publishing Pipeline** (`1c28a52`, CTO 승인 — 전이 규칙 공식 표준 확정, publishedAt 최초 발행 시점 보존, 실키 스모크는 운영/스테이징 전용(개발은 mock 리허설만) 확정): 발행 파이프라인(PATCH /projects/:id/contents/:contentId/status — DRAFT→REVIEW→PUBLISHED→ARCHIVED, 전이 규칙 @acos/core canTransition 공식 사용, PUBLISHED는 isPublishable 검증+publishedAt 기록, 데이터 보존 마이그레이션) + 운영 스모크 테스트(scripts/real-provider-smoke.mjs 8단계 자동 판정 + docs/operations 절차 문서, mock 리허설 8/8 PASS — 실키 수행은 운영/스테이징) — 해석 확인 CTO_REQUEST #29
- [x] **TASK-0702 — Dashboard Filter & Web Testing** (`1f9ab8b`, CTO 승인 — stats 필터 공식 API·hour 31일 정책·자유 입력 유지(자동완성 후속)·Playwright 공식 게이트(모든 웹 기능 통과 필수) 확정): 대시보드 필터(Feature/Provider/Model/From/To UTC, GET 폼, stats+timeline 적용, interval 전환 시 유지) + Stats API 필터 확장(feature/provider/model) + hour 조회 최대 31일 제한(from 미지정 시 최근 31일 창, 초과 400) + Playwright CI 품질 게이트(web pnpm test — Dashboard/Filter/Empty/Error 스모크 4종, 모드 전환형 스텁 API, 사전 설치 chromium) — 해석 확인 CTO_REQUEST #28
- [x] **TASK-0701 — Execution Dashboard Web UI** (`eb202c6`, CTO 승인 — CSS 차트 유지(외부 라이브러리 미도입)·필터 추가·hour 31일 제한·Playwright CI 게이트 지시 → TASK-0702로 이행): /executions 운영 대시보드 — Stats/Timeline API 소비, KPI 카드 4종(성공률은 UI에서 % 표시), Timeline Chart(hour/day/week 전환·성공/실패 스택 막대·UTC 축·빈 버킷 UI 보간), Feature/Provider/Model 통계 테이블, 서버 컴포넌트+CSS 차트(외부 라이브러리 없음), API 미연결 안내 — 브라우저(Playwright) 검증, 해석 확인 CTO_REQUEST #27

## 완료 — Sprint 6 (CTO 공식 종료, 2026-07-28)

- [x] **TASK-0605 — Execution Timeline** (`7dbb42c`, CTO 승인 — UTC·ISO Week·hour/day/week 공식 표준, 빈 버킷은 UI 보간, hour 기간 제한은 다음 Sprint 확정): GET /executions/timeline?interval=hour|day|week — 호출 수·성공/실패율·토큰·비용·지연을 시간 버킷(UTC date_trunc, 오름차순, 데이터 있는 버킷만)으로 제공, feature/provider/model 정확 일치 + from/to 필터, DB (버킷,status) $queryRaw 집계(전 값 바인딩·interval 화이트리스트) → core buildExecutionTimeline 병합, 잘못된 interval/날짜 400 — 해석 확인 CTO_REQUEST #26
- [x] **TASK-0604 — Image Guard & Preprocessing** (`6c00dfb`, CTO 승인 — 기본 정책(20MB/1024px/5MB/JPEG q82)·위반 스킵 후 계속·업로드 원본 무변경(호출 시점 전처리) 확정): Vision Provider 호출 전 이미지 검증(MIME 허용 목록·원본 20MB·빈 파일)·리사이즈(최대 변 1024px, 비율 유지·확대 없음)·최적화(JPEG q82, 투명 PNG 유지)·EXIF 제거(Orientation은 픽셀 반영 후 삭제)·출력 5MB 제한 — core Port(ImageGuardPolicy/ImagePreprocessor) + sharp 어댑터(apps/api), 위반 이미지는 스킵(분석 계속, raw.skippedImages 기록)·인프라 오류는 null 폴백 유지, VISION_IMAGE_* 환경변수 조정 — 해석 확인 CTO_REQUEST #25
- [x] **TASK-0603 — Provider Integration (OpenAI)** (`34d1426`, CTO 승인 — 가격표 Code-first·접두사 매칭·cost null 유지, json_object 공식 구현, Health(실 ping+Execution 기록) 유지, 실키 검증은 운영/스테이징 확정): OpenAI Provider 공식 연결 — API Key(OPENAI_API_KEY, 없으면 mock 폴백), Health Check(GET /llm/health, 실호출 기반·Execution dev 기록), responseFormat→response_format json_object 매핑, Multimodal(image_url, 클라이언트 주입 단위 검증), Execution Cost(gpt-4o/$2.5·$10, gpt-4o-mini 단가 등록 + 스냅샷 모델 최장 접두사 매칭) — 실키 호출은 샌드박스 egress 제한으로 미검증(무효 키 배선 검증 완료), 해석 확인 CTO_REQUEST #24
- [x] **TASK-0602 — Execution Dashboard** (`162f165`, CTO 승인 — 지표 구성 유지·비율 0~1 API 반환(%는 UI)·from/to 공식 채택·화면은 다음 Sprint·일별 시계열은 Sprint 6 후반 확정): GET /executions/stats?from=&to= — 호출 수·성공률·실패율·토큰·비용(USD)·지연(가중 평균·최대)을 전체(totals) + feature/provider/model별로 집계, DB (차원,status) groupBy → @acos/core 순수 병합 로직(buildExecutionStats, DB 없이 단위 테스트), 기간 필터·날짜 검증 400, DB 변경 없음 — 해석 확인 CTO_REQUEST #23
- [x] **TASK-0601 — Execution Domain** (`fcbf6ce`, CTO 승인 — feature 4종·400 비기록·본문 비저장·가격표 Code-first·미등록 모델 cost null·FK 없는 독립 도메인 확정): 모든 LLM 호출(Content·Analysis·Vision·개발용)을 호출 1건당 Execution 1건으로 기록 — Execution 모델(feature/provider/model/token/cost USD/latencyMs/status/error) + 마이그레이션, ExecutionTracker(@acos/core, 기록 실패는 호출 미실패), 기록 지점은 LlmService.complete 단일화(feature 태깅), 비용은 코드 선언 가격표(mock 0, 미등록 모델 null), GET /executions 조회 API — 해석 확인 CTO_REQUEST #22

## 완료 — Sprint 5 (Goal: AI Execution — CTO 공식 종료, 2026-07-28)

- [x] **TASK-0506 — Legacy Generator Integration** (`574523a`, CTO 승인 — Wrapper 구조 유지·CONTENT_GENERATOR 제거 유지·Deprecated Generator는 Sprint 6 이후 제거 검토 확정): Deprecated 구 Generator 경로(POST /projects/:id/contents)를 제거하지 않고 EngineContentGenerator(Wrapper)를 통해 공식 Content Generation Engine 호출로 통합 — API 계약 유지, generateMarkdown() 생성 코어 공용화(두 경로 본문 동일 검증), CONTENT_GENERATOR 환경 변수 제거(MockContentGenerator는 @deprecated 보존), 0505 승인 결정 반영(VISION_MAX_IMAGES 환경 변수화) — 해석 확인 CTO_REQUEST #21
- [x] **TASK-0505 — Vision Multimodal Integration** (`d63a975`, CTO 승인 — 이미지 상한 유지+환경변수화·Mock Echo 공식 적용·VISION_PROVIDER 제거 유지·용량 제한은 실연결 전 구현 확정): 구 MockVisionProvider를 LLM 기반 멀티모달 공식 엔진(LlmVisionProvider)으로 교체 — Image Bytes(base64, 최대 5장) + Prompt Engine("vision-analysis" 템플릿) + LLM Gateway(images 멀티모달 확장 + 어댑터 3종 이미지 매핑) + Company Brain 사용, 응답 엄격 파싱(실패 시 재시도→null 폴백 유지), VisionSummary 모델·저장 위치 변경 없음, VISION_PROVIDER 환경 변수 제거(LLM_PROVIDER로 일원화) — 해석 확인 CTO_REQUEST #20
- [x] **TASK-0504 — Analysis Engine Integration** (`74e9fbb`, CTO 승인 — Mock JSON Echo 공식 전략·responseFormat 유지·ANALYSIS_PROVIDER 제거 유지 확정): 구 MockAnalysisProvider를 LLM 기반 공식 엔진(LlmAnalysisProvider)으로 교체 — Prompt Engine("product-analysis" 템플릿, 규칙 기반 초안 JSON 포함) + LLM Gateway(responseFormat "json") + Company Brain(상품 이름 기준 PROJECT 스코프) 사용, 응답 엄격 파싱(실패 시 재시도→FAILED), AnalysisResult 모델·API·1:N 이력 변경 없음, ANALYSIS_PROVIDER 환경 변수 제거(LLM_PROVIDER로 일원화) — 해석 확인 CTO_REQUEST #19
- [x] **TASK-0503 — Prompt Engine** (`9f44b87`): PromptTemplate+PromptEngine(@acos/core, 선언적 템플릿 레지스트리·결정적 렌더링), 상세페이지 프롬프트를 content-generation 템플릿으로 분리, 모든 AI 기능 공용 설계(등록→render→LLM Gateway), GET /prompt/templates — 0502 승인 결정 반영(웹 버튼 엔진 전환, 구 경로 Deprecated), 해석 확인 CTO_REQUEST #18
- [x] **TASK-0502 — Content Generation Engine** (`e147aef`): READY ProductObject + Company Brain(지식/결정/설정/금지어) + LLM Gateway로 Markdown 상세페이지 생성 → Content 저장, POST /projects/:id/contents/generate (구 mock 경로는 보존) — 해석 확인 CTO_REQUEST #17
- [x] **TASK-0501 — LLM Gateway Foundation** (`9a5a3e2`): LlmProvider Port + LlmGateway(검증·재시도, @acos/core) + MockLlmProvider 기본, Provider 어댑터 3종(OpenAI·Anthropic·Gemini, 공식 SDK), LLM_PROVIDER 환경변수 교체(키 미설정 시 mock 폴백), GET /llm · POST /llm/complete — 해석 확인 CTO_REQUEST #16

## 완료 — Sprint 4 (Goal: Company Brain Integration — CTO 공식 종료, 2026-07-27)

- [x] **TASK-0404 — READY Validation Engine** (`817ead4`): CompanyBrainService로 Knowledge/Memory/Decision/SOP를 읽어 READY 전환 가능 여부 판정 — 검사 6종, PASS/WARNING/FAIL 3단계(전체=최악 값), 금지어는 Memory(GLOBAL, banned-words) 기반, POST /projects/:id/ready-validation — 해석 확인 CTO_REQUEST #15

- [x] **TASK-0403 — Company Brain Query Service** (`7c2264a`): CompanyBrainService(읽기 전용, 조회 순서 Memory→Knowledge→Decision→SOP 고정), POST /company-brain/query({query, scope?, scopeId?, limit?} → 고정 순서 4개 섹션), 소스별 부분 일치 매칭 — 해석 확인 CTO_REQUEST #14
- [x] **TASK-0402 — Structured Memory Foundation** (`ce37a80`, CTO 승인 · scope Enum+규칙 반영 `2afd089`): 표준 Memory를 scope(GLOBAL/COMPANY/PROJECT/PRODUCT Enum)/scopeId(PROJECT·PRODUCT 실존 검증)/key/value(Json)/description 구조화 저장소로 재정의, (scope,scopeId,key) 유니크, CRUD API /memory — ProjectMemory는 사람용 메모·작업기록으로 유지
- [x] **TASK-0401 — Knowledge Foundation** (`0e028ec`, CTO 승인 · category Enum 반영 `d8e24d0`): Knowledge 엔티티(title/content/category?) + KnowledgeRepository Port(@acos/core, Company Brain 4번째 축), 회사 전역 확정, category Enum 8종(RULE/POLICY/GUIDE/BRAND/LEGAL/QUALITY/FAQ/OTHER), CRUD API /knowledge

## 제안 (스펙 대기 — 구현하지 않음)

- [ ] 실제 Provider/Generator 연결 (OCR/Analysis/Vision/Content 중 CTO 지정 — API 키·모델 스펙 필요, CTO_REQUEST #6)
- [ ] Content 채널별 포맷/배포 (발행 파이프라인 자체는 TASK-0703으로 구현됨)

## 완료 — Sprint 3 (CTO 최종 승인, 2026-07-27)

- [x] **TASK-0307 — Memory Engine Foundation** (`86cef6c`): Memory 엔티티+MemoryStore Port+MemoryEngine(remember/recall/revise/forget, @acos/core Company Brain), Prisma 어댑터, CRUD API /projects/:id/memories — Workflow Engine은 사용 가능하되 소유하지 않음(미연결, CTO 지시), 해석 확인 CTO_REQUEST #11
- [x] **TASK-0306 — Decision Log Foundation** (`a36c1b0`, CTO 승인 · Enum 반영 `7864c91`): Decision 엔티티(id/title/description/reason/decisionType/author/projectId/createdAt) + DecisionRepository Port(@acos/core, Company Brain), Prisma 어댑터, CRUD API /projects/:id/decisions, decisionType Enum 8종 고정(CTO 결정), docs/architecture/decision.md
- [x] **TASK-0305 — SOP Engine Foundation** (`5c67d54`, CTO 조건부 승인 반영): Company Brain의 SOP 정의(SopDefinition) + Execution Layer의 WorkflowEngine(@acos/core, 순차 실행·실패 시 후속 SKIPPED·단계 간 출력 전달), 기본 SOP product-content(OCR→조립→READY 검수→상세페이지, 기존 서비스 재사용), SopRun 실행 이력(Project 1:N), POST/GET /projects/:id/sop-runs
- [x] **TASK-0302 — Product Object 상태 전이** (`9374aae`): DRAFT⇄READY, →ARCHIVED(종결) 전이 규칙(@acos/core) + READY 필수 조건 검증(제목+OCR/Vision 요약), PATCH …/product-object/:version/status
- [x] **TASK-0303 — 상세페이지 콘텐츠 파이프라인** (`3a7fb00`): ContentGenerator Port + MockContentGenerator(Markdown), Content 모델 Project/ProductObject 소속 재구성, READY Product Object에서만 생성, /projects/:id/contents API
- [x] **TASK-0304 — 웹 UI Project 반영** (`0dd85bf`): /projects 목록·상세, 파이프라인 실행 버튼(조립→READY→상세페이지), 홈 내비게이션

## 완료 — Sprint 1~2

- [x] **Sprint 1 — Foundation** (`4048d62`): pnpm+Turborepo 모노레포, Next.js 16(web:3000) / NestJS 11(api:4000, /health), Docker Compose(PostgreSQL·Redis·MinIO), Prisma 초기 설정, web+api 동시 실행
- [x] **TASK-0201 — 사진 업로드** (`12bb3ae`): 드래그 앤 드롭·다중·진행률 UI, MinIO 저장, `Image` 모델, 업로드 API, 오류 처리
- [x] **TASK-0202 — OCR Foundation Architecture** (`4cf6882`): OCR 도메인 @acos/core 이전(Port/Adapter), MockOCRProvider 기본, Image:OCRResult 1:N 이력, PENDING/RUNNING/SUCCESS/FAILED, Jest 도입, docs/architecture/ocr.md
- [x] **TASK-0203 — Product Object Foundation** (`de5442d`): ProductObject 모델(버전 관리, DRAFT/READY/ARCHIVED), ProductObjectBuilder(OCR+Vision mock 조립), /projects/:id/product-object API, JSON Schema, docs/architecture/product-object.md
- [x] **(자체정의) Product CRUD + 웹 플로우** (`d842338`): /products CRUD, 업로드→Product 생성→목록/상세 웹 페이지 — CTO_REQUEST #4 처리 방침 대기
- [x] **(자체정의) TASK-0204 — AI Analysis Foundation** (`d49157a`): AnalysisProvider 교체 구조, Mock 기본, apply 옵션, docs/architecture/analysis.md
- [x] **(운영) AGENTS 협업 규칙 + CTO 보고 체계** (`81aab6f`, `20c87f0`): AGENTS.md, reports/CTO_REPORT.md·CTO_REQUEST.md
- [x] **TASK-0301 — Project Domain Foundation** (`55130af`): Project 최상위 루트 엔티티 도입(CTO_REQUEST #1 결정 반영), Project 1:N Product, ProductObject.projectId → projects.id 재지정, 데이터 보존 백필 마이그레이션, Projects CRUD API, 조립 시 프로젝트 전체 상품 집계
- [x] **TASK-0205 — Vision Provider Foundation** (`4fb0cf4`): VisionProvider Port + MockVisionProvider(@acos/core), VISION_PROVIDER 환경변수 선택, Product Object 조립에 주입(실패 시 null 폴백), docs/architecture/vision.md — CTO "다음" 지시로 제안 최상단 항목 승격
