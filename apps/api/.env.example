DATABASE_URL="postgresql://postgres:postgres@localhost:5432/acos?schema=public"
REDIS_URL="redis://localhost:6379"
S3_ENDPOINT="http://localhost:9000"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET="acos"
PORT=4000
# CORS 허용 출처. 쉼표로 여러 값을 넣을 수 있다 — 로컬 SSH 터널(3000)과
# 로컬 검증용 Web(3100)을 동시에 띄울 때 서버를 내리지 않고도 둘 다
# 통과한다(WEB_URL을 지우면 코드 기본값이 두 포트를 자동으로 허용한다 —
# apps/api/src/main.ts 참고, PROJECT_MEMORY.md M-39).
WEB_URL="http://localhost:3000,http://localhost:3100"
