# CTO_REPORT — AI Product Content OS 공식 기술 보고서

> 이 문서는 AGENTS.md의 TASK 완료 절차에 따라 모든 TASK 완료 시 갱신된다.
> 형식(섹션 구성)은 항상 동일하게 유지한다:
> 1. 보고 요약 → 2. 품질 게이트 → 3. **변경 사항** → 4. **테스트 결과**
> → 5. **아키텍처 변경**(+현황) → 6. 데이터 모델 → 7. API 표면
> → 8. 리스크·기술 부채 → 9. **다음 권장 사항**
> (굵은 항목 4가지는 AGENTS.md가 요구하는 필수 포함 항목)

---

## 1. 보고 요약

| 항목 | 값 |
| --- | --- |
| 보고 기준 TASK | **TASK-3501 — Enterprise Production Cutover Platform** (Sprint 35) · **승인 정책 3501-①~⑤ 반영 완료** |
| 보고일 | 2026-07-30 |
| 브랜치 | `claude/ai-product-content-os-setup-jb5oai` |
| 핵심 성과 | **키가 틀린 것과 길이 막힌 것은 다르다** — 도달 점검 · `pnpm cutover` · 게이트 자체 검증 · 공지 어댑터 확장 · 운영 UX |
| **가장 중요한 발견** | 처음 판정이 프록시의 403을 "닿음"으로 세고, 과거 라이브 검증이 심어 둔 성공 기록 7건과 합쳐 LLM을 **`verified`(전환 완료)로 판정**했다. 세 번째 상태 `ambiguous`를 만들어 고쳤다 (§4 자체 발견 결함 ①) |
| **완료하지 못한 것** | **지시 2·3의 실제 전환.** 자격 증명이 오지 않았고, 더해서 **`api.openai.com`이 프록시에 막혀 있다**는 사실을 발견했다 — 키를 받아도 이 환경에서는 전환되지 않는다 (§8-1, CTO_REQUEST #68 ①) |
| 구현 중단 상태 | **TASK-3501 완료 후 즉시 중단** — CTO 승인 전 다음 TASK 미착수 |
| 스펙 확인 필요 | 자격 증명 **+ 네트워크** 개방 · #67 미결 2건 · 성공 기록의 근거 강화 → **CTO_REQUEST #68** |

## 2. 품질 게이트

| 게이트 | 명령 | 결과 |
| --- | --- | --- |
| Build | `pnpm build` | ✅ 6/6 워크스페이스 성공 |
| Test | `pnpm test` | ✅ **1,952** — core 1,039(+28) · api 740(+22) · **web e2e 173(+7)** — 전체 통과 |
| TypeScript | `pnpm typecheck` | ✅ 오류 0 |
| ESLint | `pnpm lint` | ✅ 오류 0, 경고 0 |
| Playwright | `npx playwright test` | ✅ 173 통과 (2.2분) |
| **품질 게이트 자체 검증** | **`pnpm check:ci-gates`** (신설) | ✅ 필수 게이트 6개 순서 일치 · 브라우저 설치 단계 있음 |
| Major Migration 교차 검증 | `pnpm check:major-migrations` | ✅ 2건 일치 (이번 주기 신규 1건은 major 아님 — §6) |
| Live Verification | 실 PostgreSQL + 실 Redis + 실 S3 + Vision 스텁 + **공지 스텁 3곳(acos·flat·csv)** + GitHub Actions 스텁 | ✅ §4 |
| **GitHub Actions** | 실제 워크플로 실행 | ✅ **run 30586977670 통과 (3분 49초)** — 새 게이트(품질 게이트 자체 검증)까지 7단계 전부 success |
| **`/ops/cutover`** | **`pnpm cutover`** (신설) | ⚠️ **exit 1 — 0/4 확인됨.** 전환은 아직 끝나지 않았고, 판정이 그 사실을 말한다 |

## 3. 변경 사항 (이번 보고 주기)

### 승인된 정책 3501-①~⑤ (이어서 진행)

| 정책 | 구현 |
| --- | --- |
| **①** 전환은 운영·Staging에서 | `judgeProductionCutover`가 환경을 받아 `applicable`을 판정. 개발에서는 화면·CLI가 "전환 대상이 아닙니다"라고 말하고 재촉하지 않음 (**판정 자체는 감추지 않음**) |
| **②** 공지 발효 시각이 예약 기본값 | 적용 시 `effectiveFrom`을 비우면 공지가 밝힌 시각으로 예약. **운영자 값이 이기고**, **과거 시각은 기본값이어도 거부** |
| **③** 최종 승인자는 다른 ADMIN | 변경 없음 — 결정을 문서와 테스트로 고정 |
| **④** 호출 대상 기록 | `executions`·`ocr_results`에 `endpoint`·`baseUrl`·`calledAt`. **호출하는 그 자리에서** 남김 |
| **⑤** `verified`는 실제 호출 대상 근거 | `baseUrl`이 공식 주소인 성공만 셈. 옛 기록의 `null`은 **"모른다"** |

**①이 막는 것**: 개발에서 상시 빨간색을 띄우면 사람은 그 빨간색을 무시하게
되고, **정작 운영에서 떴을 때도 무시합니다.** 그래서 개발에서는 판정을 참고용
으로 두되 **감추지는 않습니다** — 항목별 상태는 그대로 보여 줍니다.

**②가 막는 것**: 사람이 공지를 다시 읽어 옮겨 적는 일. 옮겨 적기 실수는
"언제부터 이 단가인가"를 틀리게 만듭니다. 다만 기본값은 **제안일 뿐 결정이
아니어서** 운영자가 넣은 값이 이깁니다.

**④·⑤가 막는 것**: 이번 TASK의 자체 발견 결함 ①이 드러낸 구멍입니다. 판정이
"최근 성공한 실행 기록이 있는가"만 보면, 그 행이 **무엇을 상대로** 만들어졌는지
알 수 없습니다. 이제 호출 대상이 행 안에 있고, 판정은 그것을 근거로 삼습니다.

### TASK-3501 — Enterprise Production Cutover Platform

**1. CTO_REQUEST #67 승인 사항 반영 (지시 1)**

지시 2·3을 "전환하라"는 답으로 읽었습니다. 다만 **자격 증명 자체는 도착하지
않았습니다**(#67 ①) — §8-1에 그대로 적습니다.

**#67 ②(공지 발효 시각 자동 예약)·③(최종 승인자 자격)은 여전히 답이 없어
구현하지 않았습니다.** #67 ④(CI에서 Live Verification 범위)는 지시 4가
"품질 게이트 자동 검증"으로 답했다고 읽고, **게이트 자체 검증**을 넣되
Live Verification은 CI에 넣지 않았습니다(실 자격 증명이 필요하고 매 푸시마다
과금됩니다).

**2. 도달 점검 — 자격 증명 이전의 조건 (지시 2·3)**

키를 받아 넣어도 방화벽·프록시가 공식 주소를 막고 있으면 전환은 되지 않습니다.
그리고 **그때 나오는 오류는 키 문제처럼 보입니다** — 사람은 있지도 않은 키
문제를 몇 시간씩 찾습니다.

| 판정 | 뜻 |
| --- | --- |
| `reachable` | 그 호스트가 응답했다 — **401·404도 닿은 것**이다(우리가 보는 것은 길이지 권한이 아니다) |
| `blocked` | 연결 자체가 되지 않았다 → 의존성 판정이 **`unreachable`** 이 된다 |
| **`ambiguous`** | **403이 왔다 — 누가 막았는지 알 수 없다** (Provider의 거절일 수도, 프록시일 수도) |

- **자격 증명을 보내지 않습니다** — 도달만 보면 되고, 키를 보내면 그 요청이
  과금될 수도 있습니다.
- **점검하지 않았으면 "닿는다"고도 "막혔다"고도 말하지 않습니다.**
- 결과 캐시 60초 — 방화벽 설정은 초 단위로 바뀌지 않습니다.

**3. `pnpm cutover` — 전환 판정을 명령 하나로**

| 종료 코드 | 뜻 |
| --- | --- |
| 0 | 운영 전환이 끝났다 |
| 1 | 아직 끝나지 않았다 (남은 항목과 **다음 할 일**을 출력) |
| **2** | **판정 불가** — 확인하지 못한 것은 통과가 아닙니다 |

이 스크립트는 전환을 **수행하지 않고 확인만** 합니다. 자격 증명을 넣어 주거나
판정을 통과시키는 우회로는 두지 않았습니다(결정 2201-③의 결).

**4. 게이트가 게이트를 검증합니다 (지시 4)**

TASK-3401에서 본 실패는 "게이트가 적혀 있는데 돌지 않은 것"이었습니다. 그
실패에는 다른 얼굴이 있습니다 — **적혀 있지도 않게 되는 것.** 워크플로에서
게이트 한 줄이 사라져도 CI는 여전히 초록으로 끝납니다: **없어진 검사는
실패하지 않기 때문입니다.**

`pnpm check:ci-gates`가 워크플로 파일을 읽어 필수 게이트·순서·브라우저 설치
단계를 확인하고(판정은 `@acos/core`), **그 단계 자체가 워크플로 안에**
있습니다. 판정 불가(파일·모듈을 못 읽음)는 **exit 2**입니다.

안정화도 함께 했습니다: 같은 브랜치의 앞선 실행 취소(`cancel-in-progress`),
작업 시간 상한 20분, 권한 `contents: read`.

**5. Provider 공지 어댑터 확장 (지시 5)**

- **`csv` 형식** — 표로 공개하는 곳도 있습니다. **빈 칸을 0으로 읽지
  않습니다**: 0은 "무료"라는 뜻이 되어 버립니다.
- **소스가 책임지는 단가 선언**(`PRICE_SOURCE_KEYS_<PROVIDER>`) — 소스를 나눈
  대가로 생긴 빈 곳을 메웁니다. 공지 하나가 죽으면 그 공지가 알려 주던 단가는
  그냥 사라졌고, 어느 공지가 죽었는지는 알아도 **"그래서 어떤 단가를 확인하지
  못했는가"** 에 답할 수 없었습니다. 이제 `unverifiedKeys`로 이름을 말합니다.
  **선언하지 않으면 빈 목록입니다** — 모르는 것을 아는 척하지 않습니다.

**6. 운영 UX (지시 6)**

- 전환 화면이 **지금 할 일 하나**를 크게 보여 줍니다 — 항목 넷을 나란히 두면
  사람은 "그래서 뭐부터?"에서 멈춥니다.
- 도달 점검을 **세 상태로 갈라** 보여 줍니다(닿음 / **가릴 수 없음** /
  닿지 못함).
- 공지 표에 **책임 단가**와 **확인하지 못한 단가**를 함께 보여 줍니다.

## 4. 테스트 결과

**core 신규 17건**

- `production-cutover.spec.ts`(+19건) — **공식 주소로 부른 성공만 근거** ·
  **성공이 있어도 공식이 없으면 통과 아님**(LLM·OCR) · 호출 대상 해석(재정의
  유무·Vision·모르는 Provider) · **`null`은 공식이 아님** · 환경별 전환 대상 ·
  막힘은 키 이야기보다 먼저 · Vision·S3도
  같다 · 키가 없을 때도 길이 막힌 사실을 덧붙임 · **점검하지 않았으면 아무
  말도 하지 않음** · 스텁을 가리키는 중이면 길 이야기를 먼저 하지 않음 ·
  **403은 닿았다고도 막혔다고도 하지 않음** · 403이면 성공 기록만으로
  통과시키지 않음 · **같은 문장을 두 번 적지 않음**
- `price-source.spec.ts`(+7건) — `csv` 어댑터(표 읽기 · **빈 칸을 0으로 읽지
  않음** · 머리글 없음) · 책임 키 선언·미선언 · **죽은 소스의 단가를 이름으로**
- `ci-workflow.spec.ts`(+2건) — 게이트 자체 검증 단계 · 동시 실행 취소·시간
  상한·최소 권한

**api 신규 15건**

- `egress.service.spec.ts`(신규 11건) — 무엇을 점검할지(미구성·Provider별·
  mock·저장소는 공식일 때만) · **401은 닿음 · 403은 모름 · 터널 실패는 막힘**
  · 타임아웃 · **자격 증명 미전송** · 캐시
- `ops.spec.ts`(+4건) — 길이 막히면 그것부터 말함 · 점검 안 했으면 말하지
  않음 · 죽은 공지의 책임 단가 · 미선언이면 말하지 않음

**web e2e 신규 5건**

- 운영 전환 UX 3건 — **지금 할 일 하나** · 도달 점검 세 상태 · 전환이 끝나면
  안내가 사라짐
- 공지 책임 단가 2건 — 확인하지 못한 단가를 이름으로 · 소스별 책임 단가

### 라이브 검증 (실 PostgreSQL + 실 Redis + 실 S3 + Vision 스텁 + **공지 스텁 3곳** + GitHub Actions 스텁)

| 확인 | 결과 |
| --- | --- |
| **형식 3종** | `acos`(google) · `flat`(openai) · **`csv`(clova)** 를 각각 읽고 각각 판정 |
| **책임 단가** | 소스별로 표시 — clova: `clova` · google: `google-vision` · openai: `gpt-4o, gpt-4o-mini` |
| **죽은 공지의 빈 곳** | openai 공지가 HTTP 503이 되자 **확인하지 못한 단가 `gpt-4o, gpt-4o-mini`를 이름으로** 보고 |
| **csv 변경 감지** | 표의 단가가 바뀌자 `sourceId: clova` 제안 후보로 잡히고 **공지가 밝힌 발효 시각(2026-10-01)** 이 근거에 남음 |
| **도달 점검** | `api.openai.com` → **`ambiguous`**(HTTP 403) · `vision.googleapis.com` → `reachable`(HTTP 404) |
| **`api.openai.com`이 막혀 있다** | 프록시가 `CONNECT tunnel failed, response 403`으로 거절 — **자격 증명을 받아도 이 환경에서는 전환되지 않는다** |
| **`pnpm cutover`** | **exit 1** · `0/4 확인됨` · 항목별 상태와 **다음 할 일** 출력 |
| **CI 판정** | 스텁 이력을 초록으로 바꾸면 `verified`(근거 `run 30571812575`) |
| **게이트 자체 검증** | `pnpm check:ci-gates` → 필수 게이트 6개 순서 일치 (exit 0) |
| **정책 ④ 기록** | OCR 1회 실행이 `endpoint=http://127.0.0.1:9100/v1/images:annotate` · `baseUrl=http://127.0.0.1:9100` · `calledAt`을 **실제로 DB에 남김**(옛 행은 null 그대로) |
| **정책 ⑤ 판정** | Vision 주소를 공식으로 되돌리자 판정이 **"최근 성공한 OCR 23건이 있지만 공식 주소로 만들어진 것은 없습니다"** 로 바뀜 — 23건 전부 스텁 상대이거나 옛 기록이다 |
| **정책 ① 환경** | `NODE_ENV=production` → `applicable: true` · `development` → `false`("전환 대상이 아닙니다") |
| **정책 ② 기본값** | 공지 시각이 예약 기본값으로 들어가고, 운영자 값이 우선하며, 과거 시각은 **기본값이어도 400** |

### GitHub Actions 실제 실행

TASK-3401의 커밋에서 **13회 연속 실패를 끊고 처음으로 초록**이 됐고, 그 뒤로
**3회 연속 초록**입니다.

| run | 커밋 | 결과 | 소요 |
| --- | --- | --- | --- |
| 30583635245 | `baebcdf` (TASK-3401) | ✅ success | 3분 59초 |
| 30584192750 | `67edadc` (보고서) | ✅ success | — |
| **30586977670** | **`c3b0ecd` (TASK-3501)** | **✅ success** | **3분 49초** |

이번 실행에는 **새 게이트**가 들어 있습니다 — `품질 게이트 자체 검증`이
러너에서 실제로 돌아 통과했습니다(0초). 동시 실행 취소·시간 상한·최소 권한도
함께 적용된 상태입니다.

### 자체 발견 결함 3건

**① 프록시의 403을 "닿음"으로 세어 LLM을 `verified`로 판정했습니다.**

라이브에서 `LLM_PROVIDER=openai`로 두고 판정을 돌리자 화면이
**"openai 공식 주소로 최근 성공한 실 호출이 7건 있습니다 — 전환 완료"** 라고
말했습니다. 사실은 이랬습니다:

- 그 403은 **Provider가 아니라 프록시**가 막은 것이었고,
- 그 "성공 기록 7건"은 **과거 라이브 검증이 심어 둔 행**(`gpt-5-preview` 같은
  존재하지 않는 모델)이었습니다.

우리가 TASK-3401에서 잡으려던 바로 그 실패 — **붙지 않은 시스템을 붙었다고
보고하는 것** — 을 새 판정이 다시 만들고 있었습니다. 세 번째 상태
`ambiguous`를 만들어 **모르는 것을 닿는다고 말하지 않게** 고쳤습니다.

이 사건은 더 깊은 한계도 드러냅니다: **성공 기록 안에 "그때 상대가
누구였는지"가 없습니다.** 지금 설정이 스텁이면 잡아내지만, 과거에 스텁이었고
지금은 공식 주소인 경우는 잡지 못합니다 → CTO_REQUEST #68 ④.

**② 판정 문구가 "가릴 수 없습니다"를 두 번 적었습니다** — TASK-3401에서 고친
겹침의 재발입니다. 점검 문구가 이미 말하고 있으면 되풀이하지 않습니다.

**③ 화면이 403을 "닿지 못함"으로 표시했습니다** — 모르는 것을 막혔다고 말하면
사람은 방화벽을 뒤지다 시간을 버립니다. "가릴 수 없음"으로 갈랐습니다.

## 5. 아키텍처 변경 및 현황

```
도달 점검 — 지시 2·3
   EgressService.hosts()      지금 설정이 가리키는 공식 주소만
        ↓ (자격 증명 없이 GET /)
   401·404 → reachable
   403     → **ambiguous** (누가 막았는지 모른다)
   그 외 실패 → blocked
        ↓
   judgeProductionCutover({..., egress})
        blocked   → unreachable  ("키가 틀린 것이 아니라 길이 막혀 있습니다")
        ambiguous → unverified   ("가릴 수 없습니다 — 실제 호출 1회로 확인")
        없음      → 이 정보를 쓰지 않는다

게이트 — 지시 4
   .github/workflows/ci.yml ──▶ judgeCiWorkflow ──▶ pnpm check:ci-gates
        ↑                                                    │
        └────────── 그 단계 자체가 워크플로 안에 있다 ◀───────┘

공지 — 지시 5
   resolvePriceSources(env)   형식 acos|flat|csv · 책임 키 선언
   summarizePriceSources()    전체 = 가장 나쁜 소스 · **unverifiedKeys**
```

① **모르는 것을 아는 척하지 않습니다.** 403을 "닿음"으로도 "막힘"으로도
   말하지 않는 것이 이번 TASK의 중심입니다.
② **판정 이전의 조건이 있습니다.** 키·설정보다 먼저 길이 열려 있어야 합니다.
③ **검사도 사라질 수 있습니다.** 그래서 검사를 검사합니다.
④ **소스를 나누면 빈 곳이 생깁니다.** 죽은 공지가 책임지던 단가를 이름으로
   말해야 그 빈 곳이 보입니다.
⑤ **통과시키는 우회로를 두지 않습니다.** 자격 증명이 없으면 `ready`는 false로
   남습니다.

**유지되는 핵심 결정**: Live Check 기본 꺼짐(1301-①) · 버킷 분리(1701-②) ·
자동 복구 없음(2201-②) · 우회 플래그 없음(2201-③) · 복구 판정 단일
원천(2301-①) · CI 게이트 순서(2101-③) · tesseract는 개발용(2401-⑤) · 근거 기한
30일(2901-③) · 가격표 절차(3101-①) · Append Only(3101-②) · 감지 → 승인 →
적용(3201-①) · 운영 자기 승인 금지(3201-②) · 발효 시각(3201-③) · 예측은 Alert
만(3201-④) · 적용 즉시 무효화(3201-⑤) · 공지 실패는 경고(3301-①) · 2단계
승인(3301-②) · 취소는 기록(3301-③) · 주기는 Provider별(3301-④) · Provider별
공지(3301-⑤) · 실패 장기화 승격(3301-⑥) · `/ops/*` ADMIN 전용

**현황**: 모노레포(web·api·core/shared/agents/ui), **마이그레이션 43건**, drift 없음

## 6. 데이터 모델

**마이그레이션 1건** — `20260811000000_execution_call_target` (CTO 정책 3501-④).

| 대상 | 내용 |
| --- | --- |
| `executions.endpoint`·`baseUrl`·`calledAt` | 실제 호출 대상 — "이 성공이 누구를 상대로 만들어졌는가" |
| `ocr_results.endpoint`·`baseUrl`·`calledAt` | 같은 이유 (OCR도 같은 판정의 근거다) |
| 인덱스 2개 | 판정이 `provider`+`baseUrl`+기간으로 센다 |

전부 nullable입니다 — 옛 기록에는 채울 값이 없습니다. **`null`은 "공식
주소였다"가 아니라 "모른다"** 로 읽습니다(정책 3501-⑤).

**Major Migration으로 지정하지 않았습니다.** nullable 컬럼 추가와 인덱스
추가뿐이고, 파괴적 변경이 없으며 재해 복구 절차를 바꾸지 않습니다
(결정 1901-① 기준).

## 7. API 표면

**엔드포인트 변경 없음.** 기존 계약이 넓어졌습니다.

- `GET /ops/cutover` — **`egress[]`**(호스트별 `status`·`reachable`·`detail`)
  추가. 의존성 상태에 **`unreachable`** 추가.
- `POST /ops/pricing/detect` · `GET /ops/pricing` — `source.sources[]`에
  **`keys`**(그 공지가 책임지는 단가), `source`에 **`unverifiedKeys`** 추가.

**환경 변수 추가**: `PRICE_SOURCE_KEYS_<PROVIDER>` ·
`PRICE_SOURCE_FORMAT_*`에 **`csv`** 추가.

**스크립트 추가**: `pnpm check:ci-gates`(게이트 자체 검증) ·
`pnpm cutover`(전환 판정).

## 8. 리스크·기술 부채

1. **실제 전환은 이번에도 하지 못했습니다.** 자격 증명이 오지 않았고, 더해서
   **`api.openai.com`이 프록시에 막혀 있습니다** — 키를 받아도 이 환경에서는
   전환되지 않습니다. **자격 증명과 네트워크를 함께** 열어 주셔야 합니다
   (CTO_REQUEST #68 ①)
2. **옛 기록에는 호출 대상이 없습니다** — 정책 3501-④가 붙은 뒤의 호출부터
   기록됩니다. 그전 기록은 `null`(모름)이고 근거로 세지 않으므로, 전환 직후에는
   **실제 호출 1회**가 있어야 `verified`가 됩니다. 의도된 동작입니다
3. **403의 모호함은 원리적으로 남습니다** — 실제 호출 1회로만 가릴 수
   있습니다. 판정은 그 사실을 말하고 다음 할 일로 안내합니다
4. **공지 형식은 여전히 우리가 정한 계약입니다** — `acos`·`flat`·`csv` 셋 다
   기계가 읽을 수 있는 형태를 전제합니다. 실제 Provider의 가격 페이지는
   HTML입니다
5. **공지가 밝힌 발효 시각을 자동으로 예약하지 않습니다**(#67 ② 미결)
6. **최종 승인자는 "다른 ADMIN"이면 누구나** 됩니다(#67 ③ 미결)
7. **Live Verification은 CI에서 돌지 않습니다** — 실 자격 증명이 필요하고 매
   푸시마다 과금됩니다
8. **CI 소요가 4분입니다** — 대부분이 web e2e입니다. 테스트가 더 늘면 job
   분리가 필요해집니다
9. **운영 저장소가 아직 S3가 아닙니다** · **백업 성능 기준은 개발 규모에서만
   검증**(1901-④ 대기)
10. **디스크 할당량이 빠듯합니다** — 검증 중 여유가 1GB 수준입니다

## 9. 다음 권장 사항 (Sprint 35 후속 후보)

1. **CTO_REQUEST #68 확인** — 특히 ①(자격 증명 **+ 네트워크**)과
   ④(성공 기록의 근거 강화)
2. **네트워크 개방 후 도달 점검 재실행** — `pnpm cutover`가 그 자리에서
   `unreachable`을 지웁니다
3. **공지 형식 어댑터 확장** — 실제 Provider 공지를 붙일 때 필요한 형식 추가
4. **CI job 분리** — core/api와 web e2e를 나눠 게이트 시간을 줄이기
5. **프로젝트별 비용 배분** — "어느 프로젝트가 돈을 쓰는가"는 아직 답할 수
   없습니다
