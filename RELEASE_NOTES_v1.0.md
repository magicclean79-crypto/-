# AI Product Content OS v1.0 — 릴리스 노트

> **상태: Release Candidate.** 이 문서는 v1.0에 무엇이 들어 있는지를
> 적습니다. **출시 가능 여부와 권고는 `reports/CTO_RELEASE_REPORT.md`에
> 있습니다** — 그쪽을 먼저 읽어 주세요.

| | |
| --- | --- |
| 버전 | **1.0.0** |
| 날짜 | 2026-08-01 |
| 데이터베이스 | 테이블 48 · 마이그레이션 57 (Major 2) |
| API | 컨트롤러 23 · 엔드포인트 약 177 |
| 화면 | 24 |
| 테스트 | **2,960** (core 1,736 · api 972 · web e2e 252) |

---

## v1.0이 하는 일

상품 이미지를 넣으면 **상세페이지 초안을 만들고, 사람이 검토해 발행하는
것까지**를 한 줄로 잇습니다.

```
프로젝트 → 이미지 업로드 → OCR(글자 읽기) → 상품 분석 → Product Object
        → 상세페이지 생성 → 검토(거버넌스) → 발행
```

각 단계는 **되돌릴 수 있고**, 무엇이 왜 막혔는지 화면이 말합니다.

---

## 주요 기능

### 콘텐츠 생산

- **이미지 업로드·OCR** — Google Cloud Vision(운영 표준) · Tesseract(개발) ·
  mock. 어느 엔진이 돌고 있는지 화면에 나옵니다.
- **상품 분석** — 이미지와 OCR 본문을 함께 넣는 멀티모달 분석.
- **Product Object** — 분석 결과를 사람이 고쳐 확정하는 중간 산출물.
  **READY가 되어야만** 상세페이지를 만들 수 있습니다.
- **상세페이지 생성** — 회사 지식·결정·금지어(Company Brain)를 프롬프트에
  넣어 Markdown을 만듭니다.
- **발행 파이프라인** — `DRAFT → REVIEW → PUBLISHED → ARCHIVED`.
  되살려 재발행할 수 있고, 그때 거버넌스를 **다시** 거칩니다.
- **콘텐츠 거버넌스** — 금지어·필수 고지·상품 상태를 발행 직전에 봅니다.
  **막든 통과하든 기록**합니다.

### AI 운영

- **Provider 4종** — mock · OpenAI · Anthropic · Google Gemini.
  라우팅 · Failover · A/B 실험 · Sticky 배정.
- **비용 거버넌스** — 일/월 예산 상한, 초과 시 **호출 차단**. LLM과 OCR을
  **합해서** 봅니다.
- **단가 거버넌스** — 제안 → 검토 → 승인 → 적용의 4단계. 적용 이력은
  Append Only이며 **과거 기록을 다시 계산하지 않습니다**.
- **토큰 상세** (v1.0 신규) — 캐시 읽기·쓰기·생각 토큰까지 세어 비용을
  냅니다. Provider마다 usage의 뜻이 달라 **틀리는 방향도 달랐습니다**.

### 작업 신뢰성

- **묶음 작업 4종** — OCR · 분석 · 생성 · 발행. 중간에 죽어도 **끝난
  단계는 다시 사지 않습니다.**
- **실패 분류** — 다시 해 볼 만한 실패와 사람이 고쳐야 하는 실패를
  가릅니다. **모르는 실패는 재시도하지 않습니다.**
- **자동 이어하기** — 죽은 프로세스가 남긴 작업을 큐가 되살립니다.
  **기본은 꺼짐**입니다(`JOB_AUTO_RESUME=on`).
- **작업 로그** — 등급 4단계, 작업 id로 묶어 봅니다. **비밀은 적기 전에
  이름으로 가립니다.**
- **성능 계측** — 단계별 시간·토큰·비용. **모르는 값을 0으로 채우지
  않습니다.**

### 운영

- **백업·복구** — 자동 백업, 원격 복제, **복원 검증**, 분기 복구 리허설.
- **경보** — Slack · Email · Webhook · Teams. 재시도 4회 + **재전송**
  (15분 → 1시간 → 4시간).
- **예약 점검** — 비용 검증 · 설정 검증 · 상태 점검 · 가격 감지 ·
  일일 진단. 멈추면 **감시가 알립니다.**
- **분산 잠금** — 인스턴스를 여러 개 띄워도 점검이 중복 실행되지 않습니다
  (Redis; 없으면 단일 인스턴스 모드이며 **그 사실이 화면에 뜹니다**).
- **운영 대시보드** — Validation · Attribution · Notification · Recovery를
  한 화면에서 봅니다. **여기서 새로 판정하지 않습니다.**

### 보안

- 세션 쿠키(httpOnly) · 운영은 **쿠키 전용 모드**
- **운영·스테이징에서 `Secure` 자동 활성** (v1.0에서 고침 — 아래)
- 로그인 Rate Limit · 계정 잠금 · 감사 로그
- 역할 3종(ADMIN · EDITOR · VIEWER)
- 환경 검증 실패 시 **운영에서는 기동하지 않습니다**

---

## v1.0에서 고친 것 (Release Candidate 점검)

출시 점검에서 **Critical 3건**을 찾아 고쳤습니다.

### 1. 세션 쿠키가 운영에서 `Secure` 없이 나갈 수 있었습니다

환경 선언(`ENV_SPECS`)에는 `AUTH_COOKIE_SECURE`가 **"운영에서는 자동
활성"** 이라고 적혀 있었는데, 코드는 `?? false`였습니다. **선언과 코드가
서로 다른 말을 하고 있었습니다.**

그리고 그 차이는 조용했습니다 — 값을 지정하지 않았을 때 환경 검증이
**경고조차 내지 않았습니다.** 운영은 쿠키 전용 모드가 기본이므로 세션
쿠키가 **유일한 자격 증명**이고, 그것이 평문 경로로 나갈 수 있었습니다.

이제 코드가 선언대로 동작합니다. HTTP로만 서비스하는 환경은
`AUTH_COOKIE_SECURE=0`을 **명시**해야 하며, 그때는 검증이 경고합니다 —
끄는 것은 되지만 **모르는 채로 꺼져 있는 것**은 안 됩니다.

### 2. 릴리스 신원

모든 워크스페이스가 `0.1.0`을 보고하고 있었습니다. v1.0을 출시하면서
어떤 빌드가 v1.0인지 확인할 방법이 없는 상태였습니다. **1.0.0**으로
올렸습니다.

### 3. 출시 문서

이 문서를 포함한 5종을 새로 썼습니다 (아래 "함께 보는 문서").

---

## v1.0에 **없는** 것

숨기지 않고 적습니다. 자세한 것은 `KNOWN_LIMITATIONS.md`에 있습니다.

- **실 AI Provider로 검증된 적이 없습니다.** 지금까지 모든 성공 기록은
  mock 또는 우리 계약 스텁을 상대로 만들어졌습니다. 이것이 **출시를
  막는 가장 큰 항목**입니다.
- 실행 중인 API가 **자기 버전을 말하지 않습니다.**
- 인스턴스를 **둘 이상 띄운 상태를 실제로 확인하지 못했습니다.**
- 로그인 외 엔드포인트에는 요청 수 제한이 없습니다.
- 발행된 콘텐츠가 **외부 채널로 나가는 경로가 없습니다** (파일·API로
  가져가야 합니다).

---

## 업그레이드

첫 정식 릴리스입니다. 설치·마이그레이션 절차는 `MIGRATION_GUIDE.md`.

---

## 함께 보는 문서

| 문서 | 언제 읽는가 |
| --- | --- |
| `reports/CTO_RELEASE_REPORT.md` | **출시해도 되는지** 판단할 때 |
| `MIGRATION_GUIDE.md` | 설치하거나 올릴 때 |
| `KNOWN_LIMITATIONS.md` | "이건 왜 안 되지?" 싶을 때 |
| `OPERATIONS_RUNBOOK.md` | 당직 중에 |
| `USER_GUIDE.md` | 처음 써 볼 때 |
| `docs/operations/` | 각 주제를 깊게 볼 때 |
