# 알려진 한계 (v1.0)

> 이 문서는 **v1.0이 못 하는 것**을 적습니다. 여기 적힌 것은 버그가 아니라
> **우리가 알고 남겨 둔 것**입니다. 모르고 남은 것은 여기 없습니다 —
> 그래서 이 목록이 완전하다고 말하지 않습니다.

등급은 출시 판단 기준입니다:

| 등급 | 뜻 |
| --- | --- |
| **Critical** | 이대로 출시하면 안 된다 |
| **High** | 출시는 되지만 v1.1에서 먼저 해야 한다 |
| **Medium** | 불편하거나 부정확하다 |
| **Low** | 알고만 있으면 된다 |

---

## Critical — 출시를 막는 것

### C-1. 실 AI Provider로 검증된 적이 없습니다

**이 제품의 이름은 "AI Product Content OS"인데, 실제 AI Provider를 상대로
한 번도 돌아 본 적이 없습니다.**

지금까지의 모든 성공 기록은 `mock` 또는 우리가 만든 계약 스텁을 상대로
만들어졌습니다. 계약 스텁은 **요청·응답의 모양**만 흉내 냅니다 — 실제
Provider가 우리 요청을 어떻게 대하는지, 키가 유효한지, 응답 품질이
쓸 만한지는 **증명하지 않습니다.**

막고 있는 것은 코드가 아니라 **사람이 줘야 하는 넷**입니다:

| 필요한 것 | 없으면 |
| --- | --- |
| Provider 자격 증명 | 실 호출 불가 |
| 네트워크 허용 목록 | `api.openai.com` 등이 프록시에서 403 |
| 검증 환경 | 어디서 돌릴지 정해지지 않음 |
| 운영 호스트 목록 | "운영에서 성공했다"를 셀 근거 없음 |

지금 상태:

```
POST /ops/validation-run/execute  →  HTTP 403
SELECT count(*) FROM validation_runs;  →  0
pnpm cutover  →  exit 1 (운영 활성화 1/3)
```

**열한 스프린트 연속** 같은 상태입니다.

> 이것을 "코드는 준비됐다"로 읽으면 안 됩니다. 준비된 것은 **경로**이고,
> 그 경로로 무엇이 나오는지는 아무도 본 적이 없습니다.

---

## High — v1.1에서 먼저 할 것

### H-1. 실행 중인 API가 자기 버전을 말하지 않습니다

패키지는 `1.0.0`이지만, 돌고 있는 서버에 물어볼 방법이 없습니다. 장애
때 **"지금 무엇이 떠 있는가"** 에 답하지 못합니다.

v1.0에서 고치지 않은 이유: 기존 응답에 칸을 더하는 것은 API 변경이고,
이번 Sprint는 Code Freeze입니다.

### H-2. 인스턴스를 둘 이상 띄운 상태를 확인하지 못했습니다

분산 잠금과 작업 큐의 조건부 갱신은 **설계와 단위 테스트로는 맞습니다.**
그런데 **실제로 두 프로세스를 경쟁시켜 보지는 못했습니다.**

"맞을 것"과 "맞다"는 다릅니다. 다중 인스턴스로 운영하려면 이것부터
확인해야 합니다.

### H-3. `.env.example`이 전체를 담고 있지 않습니다

코드가 읽는 환경 변수는 133개, `.env.example`에 있는 것은 33개입니다.
실질적인 단일 원천은 `ENV_SPECS`(약 90개, 설명·필수 여부·검증 포함)이고
화면에서도 볼 수 있지만, **저장소를 처음 여는 사람은 `.env.example`을
읽습니다.**

### H-4. 로그인 외 엔드포인트에 요청 수 제한이 없습니다

로그인은 Rate Limit과 계정 잠금이 있습니다. 나머지 엔드포인트는 없습니다.
인증을 요구하므로 익명 남용은 막히지만, **자격 증명을 가진 쪽의 과다
호출**은 막지 못합니다.

### H-5. 발행된 콘텐츠가 시스템 밖으로 나가는 경로가 없습니다

`PUBLISHED`가 되어도 마켓·CMS로 보내는 기능은 없습니다. 사람이 화면에서
가져가야 합니다. (TASK-4602 조사 결과 1 — 두 스프린트째 남아 있습니다.)

---

## Medium — 부정확하거나 불편한 것

### M-1. 토큰 상세의 의미를 실 Provider로 확인하지 못했습니다

캐시 읽기·쓰기·생각 토큰의 계산은 **각 Provider의 문서를 믿고 있는
것**입니다. 실제로 그렇게 청구되는지는 청구서를 봐야 압니다.

`judgeCiCoverage`가 이 항목을 "우리가 확인할 수 없습니다"로 표시합니다.

### M-2. 가격표에 모델이 부족합니다

7종만 등록돼 있습니다. 표에 없는 모델로 호출하면 **비용이 집계되지 않고
예산 상한도 걸리지 않습니다.** `GET /llm/pricing-health`가 그 모델 이름과
건수를 보여 줍니다.

**저희가 채우지 않았습니다** — 공개 요금표를 확인할 수 없는 상태에서 넣은
숫자는 없는 것보다 나쁩니다. 사람이 근거를 들고 등록해야 합니다.

### M-3. 단계별 비용은 요청 id에 기댑니다

한 작업의 비용은 **같은 요청이 부른 호출**을 모아 냅니다. `/jobs/*` 요청은
작업만 돌리므로 지금은 섞일 자리가 없지만, 그것은 **경로에 달린
성질**이지 계산이 보장하는 것이 아닙니다.

### M-4. 작업 층 밖에 남은 AI 경로

Vision 단독 경로와 거버넌스 스캔은 아직 체크포인트·계측 밖입니다.

### M-5. 운영 표면이 제품 표면보다 큽니다

`ops.controller.ts` 하나가 라우트 69개를 가지고 있습니다. 동작에는 문제가
없지만, 다음에 무엇을 더하면 "어디에 무엇이 있는가"에 답하기 어려워집니다.

### M-6. 표본이 적어 판정을 유보하는 화면이 많습니다

귀속률·성능 추세 등은 표본이 기준에 못 미치면 **판정하지 않습니다**.
"판정 유보"는 **빠르다는 뜻도 정상이라는 뜻도 아닙니다.** 트래픽이 쌓이면
자연히 해소됩니다.

### M-7. Teams Adaptive Card는 실제 Teams에서 확인하지 못했습니다

코드와 봉투는 준비돼 있고 기본값은 MessageCard입니다. 바꾸는 것도
되돌리는 것도 `TEAMS_CARD_FORMAT` 한 줄입니다.

---

## Low — 알고만 있으면 되는 것

- **`silent` 알림 채널** — 최근 24시간 안에 보낼 일이 없었던 채널은
  "닿는지 모른다"입니다. 실패도 통과도 아닙니다.
- **담당자 연락처가 환경변수 표**입니다 — 사람이 바뀌면 배포가 필요합니다.
- **순수 IPv6 상대의 프록시 신뢰 판정**을 라이브로 확인하지 못했습니다
  (이 컨테이너에 IPv6 스택이 없습니다).
- **옛 기록 10건에 마크다운 별표**가 남아 있습니다 (TASK-4001 이전 초안).
- **운영 저장소가 아직 S3가 아닙니다** — 개발은 s3rver입니다.
- **`.turbo/cache`가 자랍니다** — 상한을 두는 것을 권합니다.
- **재전송은 원본 본문을 복원하지 않습니다** — 이력에 본문을 담지 않는
  규칙(1401)을 지켰기 때문입니다.

---

## 이 목록에 없는 것

**모르고 남은 것은 여기 없습니다.** v1.0까지 라이브 검증에서만 드러난
결함이 여러 건 있었고(TASK-4603 4건, TASK-4701 3건), 그 전부가 **단위
테스트를 통과한 상태**였습니다. 같은 종류가 더 있다고 보는 편이 안전합니다.

지금 CI에서 도는 라이브 검사는 15건 중 11건입니다. 나머지 4건은 자격
증명·과금·사람 눈이 필요해 **사람이 돌려야 합니다.**
