{
  "name": "ai-product-content-os",
  "version": "0.1.0",
  "private": true,
  "description": "AI Product Content OS - AI 기반 제품 콘텐츠 운영 시스템",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "eslint .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:unit": "turbo run test --filter=@acos/core --filter=api",
    "test:e2e": "turbo run test --filter=web",
    "check:major-migrations": "node scripts/check-major-migrations.mjs",
    "check:ci-gates": "node scripts/check-ci-gates.mjs",
    "cutover": "node scripts/cutover.mjs",
    "clean": "turbo run clean && rm -rf node_modules",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "prisma:generate": "pnpm --filter api prisma:generate",
    "prisma:migrate": "pnpm --filter api prisma:migrate",
    "prisma:studio": "pnpm --filter api prisma:studio"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "turbo": "^2.10.7",
    "typescript-eslint": "^8.65.0"
  }
}
